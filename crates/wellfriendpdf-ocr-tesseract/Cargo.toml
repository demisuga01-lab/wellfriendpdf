[package]
name = "wellfriendpdf-ocr-tesseract"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
publish = false
description = "Tesseract OCR backend for wellfriendpdf-engine, driving the external `tesseract` process (no linked C)."
repository = "https://github.com/demisuga01-lab/wellfriendpdf-sdk"

[dependencies]
wellfriendpdf-engine = { version = "0.1.0", path = "../engine" }
