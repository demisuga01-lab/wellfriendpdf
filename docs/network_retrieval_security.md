# Network Retrieval Security

The network policy rejects credentials in URLs, unsupported schemes, loopback, private, link-local, multicast, unspecified, CGNAT, documentation/test networks, metadata endpoints, and redirects to forbidden destinations by default.
