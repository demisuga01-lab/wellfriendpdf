# Font Resource Dedup

Font dedup separates font bytes from mapping semantics. Identical embedded programs may share only when widths, vertical metrics, Encoding, CMap, CIDToGIDMap, ToUnicode, descriptor fields, subset glyph coverage, and writing mode are compatible. compression and Office closeout does not rebuild merged subsets.
