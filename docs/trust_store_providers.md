# Trust Store Providers

The deterministic default uses explicit caller trust anchors and intermediates. Platform trust stores are optional provider surfaces and are never silently consulted. AIA-fetched roots are not promoted to anchors.
