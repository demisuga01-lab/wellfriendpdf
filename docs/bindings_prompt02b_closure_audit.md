# Combined Prompt 02B Closure Audit

This audit closes only the bounded leftovers from Combined Prompt 02. It does
not start Prompt 03 work.

| Row | Prior Prompt 02 status | Prompt 02B target | Implementation result | Tests/evidence | Remaining limit | Blocks Prompt 03 |
| --- | --- | --- | --- | --- | --- | --- |
| Java Maven package smoke | Maven unavailable on host; metadata only | Maven test/package smoke with JAR output | Maven is run through `scripts/prompt02b_java_package_smoke.ps1`; script downloads Apache Maven 3.9.9 into `target/prompt02b-tools` when `mvn` is absent | `target/prompt02-binding-parity/java-package-smoke.json`; commands `mvn -version`, `mvn clean test`, `mvn package` | Uses target-local Maven fallback on hosts without `mvn` | no |
| Java Gradle package smoke | Gradle unavailable | Close Gradle decision without adding a second build system | Superseded by Prompt 02C: Java SDK now has real Gradle build/package support, target-local Gradle bootstrap, Gradle JAR smoke, and Maven/Gradle equivalence | `docs/bindings_prompt02c_gradle_closure.md`; `target/prompt02-binding-parity/gradle-jar-smoke.json`; `target/prompt02-binding-parity/java-maven-gradle-equivalence.json`; matrix row `prompt02b.java.gradle_policy` now `implemented_public` | None after Prompt 02C | no |
| JAR package verification | Java compile/smoke only; no package content check | Produce, inspect, and run from JAR | `oxide-sdk-0.1.0.jar` is produced and inspected as a ZIP; required public classes present; test/native/build-junk entries rejected | `target/prompt02-binding-parity/java-package-smoke.json`; JAR path `bindings/java/target/oxide-sdk-0.1.0.jar`; `PackageSmoke.java` runtime smoke | `jar` CLI absent on PATH, so inspection uses `System.IO.Compression.ZipFile` equivalent | no |
| .NET password-open parity | Partial; C ABI open was byte-only | Public password overloads | `OxideDocument.Open(string, string? password = null)` and `OxideDocument.Open(byte[], string? password = null)` call the password-aware C ABI | `OxideSmokeTests.PasswordOpenRoutesThroughNativeAbiWithoutLeakingSecrets`; `dotnet test ... --filter PasswordOpenRoutesThroughNativeAbiWithoutLeakingSecrets` | Encrypted fixture is covered at C ABI; .NET tests cover API path and no-secret echo with unencrypted/malformed input | no |
| Java password-open parity | Partial; C ABI open was byte-only | Public password overloads | `Oxide.Document.open(Path, String)` and `Oxide.Document.open(byte[], String)` pass UTF-8 password bytes through FFM | `OxideSmokeTest`; `PackageSmoke.java`; `java --enable-preview --enable-native-access=ALL-UNNAMED ... OxideSmokeTest` | Encrypted fixture is covered at C ABI; Java tests cover API path and no-secret echo with unencrypted/malformed input | no |
| C ABI open-with-password entry point | Missing | Stable optional-password open function | Added `oxide_document_open_from_bytes_with_password` beside existing ABI entry point; null and empty password are distinguishable by pointer/length shape | `crates/oxide-capi/include/oxide.h`; `crates/oxide-capi/src/lib.rs`; `cargo test -p oxide-capi capi_open_with_password` | No C ABI file-path open; callers can read files and pass bytes | no |
| progress callback plumbing | Unsupported/vague in docs | Honest final posture | Shared feature report returns `progress.status = progress_not_supported`; no binding exposes fake callbacks | `sdk::tests::font_signature_decode_dedup_feature_envelopes`; .NET and Java feature-report assertions; docs matrix rows `diag.progress_posture` and `prompt02b.progress_posture` | Engine has no report/output facade progress events yet | no |
| cancellation token plumbing | Unsupported/vague in docs | Honest final posture | Shared feature report returns `cancellation_not_supported_for_prompt02_bindings` and names cancellable render internals that are not exposed through Prompt 02 bindings | `sdk::tests::font_signature_decode_dedup_feature_envelopes`; .NET and Java feature-report assertions; docs matrix rows `diag.cancel_posture` and `prompt02b.cancellation_posture` | Engine render internals have `CancelToken`, but WASM/.NET/Java Prompt 02 report/output APIs do not accept tokens | no |
| WASM progress/cancellation status | Unsupported | Explicit unsupported status | WASM exposes feature availability only; no no-op progress/cancel API | `docs/wasm_sdk_prompt02.md`; `feature_report_json`; matrix rows `prompt02b.progress_posture`, `prompt02b.cancellation_posture` | Browser AbortSignal/progress callbacks wait for a token-aware shared facade operation | no |
| .NET progress/cancellation status | Unsupported | Explicit unsupported status | .NET exposes feature report status; no `IProgress` or `CancellationToken` overloads where ignored | `OxideSmokeTests.FeatureReportRecordsPrompt02BProgressAndCancellationPosture`; `docs/dotnet_sdk_prompt02.md` | Future token-aware engine facade required | no |
| Java progress/cancellation status | Unsupported | Explicit unsupported status | Java exposes feature report status; no interruption/callback API where ignored | `OxideSmokeTest`; `docs/java_sdk_prompt02.md` | Future token-aware engine facade required | no |
| leak checker / sanitizer evidence | No dedicated local leak checker | Run strongest local stress and record CI fallback | C ABI/.NET/Java repeated open/report/dispose stress tests added; existing Linux sanitizer workflow remains the ASan/TSan/UB gate | `target/prompt02-binding-parity/memory-smoke.json`; `.github/workflows/sanitizers.yml`; `scripts/prompt02b_memory_gate.ps1` | Valgrind and `cargo-llvm-cov` unavailable on this Windows host; ASan/TSan not claimed local | no |
| Prompt 02 gap matrix update | Maven/Gradle/JAR/password/progress/cancel/leak rows partial or absent | Matrix rows updated with final statuses | Generator records password-open public, Maven/JAR public, Prompt 02C Gradle support public, progress/cancellation unsupported-reported, memory evidence closed | `scripts/gen_prompt02_binding_matrix.py`; `docs/bindings_prompt02_gap_matrix.md`; `target/prompt02-binding-parity/binding-gap-matrix.json` | Other non-public Prompt 02 feature rows remain bounded future feature work, not 02B/02C closure blockers | no |
| Prompt 02 final closure status | Final report said partial | Close Prompt 02 without caveat | Prompt 02B scope is complete after validation and clean commit `Close combined prompt 02 binding parity gaps` | This audit, generated matrix, smoke artifacts, validation log in final response, final commit | None for Prompt 02B | no |
