# Certificate Path Building

Path construction uses explicit trust anchors, caller intermediates, CMS certificates, and optionally AIA-fetched untrusted intermediates. It is bounded by depth, candidates, graph edges, issuer fetches, and deterministic ordering.
