# roadmap closure 02B Closure Audit

This audit closes only the bounded leftovers from roadmap closure 02. It does
not start Release Packaging work.

| Row | Prior Binding Parity status | Java Packaging target | Implementation result | Tests/evidence | Remaining limit | Blocks Release Packaging |
| --- | --- | --- | --- | --- | --- | --- |
| Java Maven package smoke | Maven unavailable on host; metadata only | Maven test/package smoke with JAR output | Maven is run through `scripts/java_packaging_java_package_smoke.ps1`; script downloads Apache Maven 3.9.9 into `target/java_packaging-tools` when `mvn` is absent | `target/binding_parity-binding-parity/java-package-smoke.json`; commands `mvn -version`, `mvn clean test`, `mvn package` | Uses target-local Maven fallback on hosts without `mvn` | no |
| Java Gradle package smoke | Gradle unavailable | Close Gradle decision without adding a second build system | Superseded by Gradle Packaging: Java SDK now has real Gradle build/package support, target-local Gradle bootstrap, Gradle JAR smoke, and Maven/Gradle equivalence | `docs/bindings_gradle_packaging_gradle_closure.md`; `target/binding_parity-binding-parity/gradle-jar-smoke.json`; `target/binding_parity-binding-parity/java-maven-gradle-equivalence.json`; matrix row `java_packaging.java.gradle_policy` now `implemented_public` | None after Gradle Packaging | no |
| JAR package verification | Java compile/smoke only; no package content check | Produce, inspect, and run from JAR | `wellfriendpdf-sdk-0.1.0.jar` is produced and inspected as a ZIP; required public classes present; test/native/build-junk entries rejected | `target/binding_parity-binding-parity/java-package-smoke.json`; JAR path `bindings/java/target/wellfriendpdf-sdk-0.1.0.jar`; `PackageSmoke.java` runtime smoke | `jar` CLI absent on PATH, so inspection uses `System.IO.Compression.ZipFile` equivalent | no |
| .NET password-open parity | Partial; C ABI open was byte-only | Public password overloads | `WellfriendDocument.Open(string, string? password = null)` and `WellfriendDocument.Open(byte[], string? password = null)` call the password-aware C ABI | `WellfriendPdfSmokeTests.PasswordOpenRoutesThroughNativeAbiWithoutLeakingSecrets`; `dotnet test ... --filter PasswordOpenRoutesThroughNativeAbiWithoutLeakingSecrets` | Encrypted fixture is covered at C ABI; .NET tests cover API path and no-secret echo with unencrypted/malformed input | no |
| Java password-open parity | Partial; C ABI open was byte-only | Public password overloads | `WellfriendPdf.Document.open(Path, String)` and `WellfriendPdf.Document.open(byte[], String)` pass UTF-8 password bytes through FFM | `WellfriendPdfSmokeTest`; `PackageSmoke.java`; `java --enable-preview --enable-native-access=ALL-UNNAMED ... WellfriendPdfSmokeTest` | Encrypted fixture is covered at C ABI; Java tests cover API path and no-secret echo with unencrypted/malformed input | no |
| C ABI open-with-password entry point | Missing | Stable optional-password open function | Added `wellfriendpdf_document_open_from_bytes_with_password` beside existing ABI entry point; null and empty password are distinguishable by pointer/length shape | `crates/wellfriendpdf-capi/include/wellfriendpdf.h`; `crates/wellfriendpdf-capi/src/lib.rs`; `cargo test -p wellfriendpdf-capi capi_open_with_password` | No C ABI file-path open; callers can read files and pass bytes | no |
| progress callback plumbing | Unsupported/vague in docs | Honest final posture | Shared feature report returns `progress.status = progress_not_supported`; no binding exposes fake callbacks | `sdk::tests::font_signature_decode_dedup_feature_envelopes`; .NET and Java feature-report assertions; docs matrix rows `diag.progress_posture` and `java_packaging.progress_posture` | Engine has no report/output facade progress events yet | no |
| cancellation token plumbing | Unsupported/vague in docs | Honest final posture | Shared feature report returns `cancellation_not_supported_for_binding_parity_bindings` and names cancellable render internals that are not exposed through Binding Parity bindings | `sdk::tests::font_signature_decode_dedup_feature_envelopes`; .NET and Java feature-report assertions; docs matrix rows `diag.cancel_posture` and `java_packaging.cancellation_posture` | Engine render internals have `CancelToken`, but WASM/.NET/Java Binding Parity report/output APIs do not accept tokens | no |
| WASM progress/cancellation status | Unsupported | Explicit unsupported status | WASM exposes feature availability only; no no-op progress/cancel API | `docs/wasm_sdk_binding_parity.md`; `feature_report_json`; matrix rows `java_packaging.progress_posture`, `java_packaging.cancellation_posture` | Browser AbortSignal/progress callbacks wait for a token-aware shared facade operation | no |
| .NET progress/cancellation status | Unsupported | Explicit unsupported status | .NET exposes feature report status; no `IProgress` or `CancellationToken` overloads where ignored | `WellfriendPdfSmokeTests.FeatureReportRecordsJavaPackagingProgressAndCancellationPosture`; `docs/dotnet_sdk_binding_parity.md` | Future token-aware engine facade required | no |
| Java progress/cancellation status | Unsupported | Explicit unsupported status | Java exposes feature report status; no interruption/callback API where ignored | `WellfriendPdfSmokeTest`; `docs/java_sdk_binding_parity.md` | Future token-aware engine facade required | no |
| leak checker / sanitizer evidence | No dedicated local leak checker | Run strongest local stress and record CI fallback | C ABI/.NET/Java repeated open/report/dispose stress tests added; existing Linux sanitizer workflow remains the ASan/TSan/UB gate | `target/binding_parity-binding-parity/memory-smoke.json`; `.github/workflows/sanitizers.yml`; `scripts/java_packaging_memory_gate.ps1` | Valgrind and `cargo-llvm-cov` unavailable on this Windows host; ASan/TSan not claimed local | no |
| Binding Parity gap matrix update | Maven/Gradle/JAR/password/progress/cancel/leak rows partial or absent | Matrix rows updated with final statuses | Generator records password-open public, Maven/JAR public, Gradle Packaging Gradle support public, progress/cancellation unsupported-reported, memory evidence closed | `scripts/gen_binding_parity_binding_matrix.py`; `docs/bindings_binding_parity_gap_matrix.md`; `target/binding_parity-binding-parity/binding-gap-matrix.json` | Other non-public Binding Parity feature rows remain bounded future feature work, not 02B/02C closure blockers | no |
| Binding Parity final closure status | Final report said partial | Close Binding Parity without caveat | Java Packaging scope is complete after validation and clean commit `Close roadmap closure 02 binding parity gaps` | This audit, generated matrix, smoke artifacts, validation log in final response, final commit | None for Java Packaging | no |
