# Signature Validation Fuzzing

Signature Validation fuzz targets `signature_validation` and `signature_evidence` compile and pass bounded smoke under the 4 GiB cap using nightly dev builds with debug metadata disabled, 256 codegen units, no trace compares, and branch folding enabled.
