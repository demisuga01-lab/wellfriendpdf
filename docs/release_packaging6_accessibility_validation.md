# release validation Accessibility Validation

document security accessibility APIs were exercised through focused Rust tests, CLI
smokes, workspace tests, and coverage.

Evidence:

- `target/release_validation-enterprise-validation/document_security-accessibility-results.json`
- `target/release_validation-enterprise-validation/final-validation-matrix.json`

The validation retains documented limits for external PDF/UA tooling that was
not available on the VPS.
