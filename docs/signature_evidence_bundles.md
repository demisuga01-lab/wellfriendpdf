# Signature Evidence Bundles

Evidence bundles export retrieved intermediates, OCSP responses, CRLs, source URLs, hashes, policy identity, and validation metadata without private keys or secrets. Offline replay revalidates evidence rather than trusting provenance.
