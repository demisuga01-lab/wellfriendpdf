# Prompt 12 Prepress CMM Audit

Prompt 12 adds a first-class prepress color layer on top of the Prompt 11B
native CMM work. The default build remains portable and uses the existing
fallback preview path. The `native-cmm-lcms2` feature remains the only path that
may use LittleCMS.

Starting checkpoint:

- Expected HEAD: `077e33d`
- Expected commit: `077e33d Close combined prompt 11B native littlecms cmm backend`
- Expected starting worktree: clean

Prompt 12 artifacts are generated by:

```powershell
python scripts\prompt12_prepress_cmm_audit.py --run-smoke
```

The artifact root is `target/prompt12-prepress-cmm`.

Implemented public surface:

- `ColorReport.prompt12_prepress_cmm_device_link_separation_plates`
- `feature-report.report.prompt12_prepress_cmm_device_link_separation_plates`
- ICC profile class, hash, channel, PCS, native, and fallback diagnostics
- sparse separation framebuffer report data
- spot and DeviceN plate summaries and preview hashes
- rendering intent and BPC cache-key posture

Boundaries:

- RGB page output remains visual preview, not press proof.
- Prompt 12 preserves plate identity and tint data; Prompt 13 owns full
  overprint compositing.
- PDF/X certification-grade validation is not claimed.
