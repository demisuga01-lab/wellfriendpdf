# Signature Validation Network Security

Network failures are never converted into revocation good. Local tests cover blocked addresses, redirects, response limits, cache replay, cancellation, and syntax-only WASM rejection.
