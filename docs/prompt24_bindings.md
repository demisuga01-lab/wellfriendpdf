# Prompt 24 Bindings

Rust, CLI, Python, C ABI, .NET, Java, and WASM expose the shared semantic validation model. Bindings provide trust/evidence/retrieval handles or exact capability limits and do not collapse indeterminate states into valid.
