# Final universal renderer implementation report

**2026-09-07 source-closure continuation:** progressive rendering now supports live replacement of the complete schema-v1 render contract through `ProgressiveRenderJob::revise_render_contract`, including output region, tile grid/order, scheduler, visibility, cache, and obsolete-publication state. The path is exposed through server `render_contract_json`/`contract_json`, C ABI/header, Python, WASM/TypeScript, .NET, and Java. The root README has also been rebuilt around current source APIs and source-built package requirements, with a reserved front section for the future benchmark campaign and no new benchmark claim.

**Repository:** `E:\wellpdfsdk`
**Code-validation commit:** `dd9e9d7b64bd13598b143d54d24a5ecd331a9ec8`
**Historical validation note:** earlier entries in this report reference prior VPS evidence. The 2026-08-11 and later continuation is local-only; no VPS, SSH, corpus, benchmark, competitor run, deployment, release tag, or package publication is used for the current pass.
**Evidence root:** `/home/ubuntu/wellpdf/results/final-prebenchmark-verification-20260805T230133Z`
**Scope:** Final pre-benchmark implementation/verification pass. This report distinguishes repaired and verified work from unresolved universal-architecture requirements.

2026-08-28 local continuation, progressive render-contract parity follow-up: progressive render jobs now accept and retain a canonical schema-v1 `RenderContract` rather than deriving identity solely from page, DPI, and `RenderMode`. `RenderContract::with_render_tile` derives per-tile contracts, `ContentEngine::progressive_render_job_with_contract*` exposes the Rust core entry points, `ProgressiveRenderJob` stores the normalized base contract, preserves clipped contract output regions by tiling in page device coordinates and assembling at clip-relative offsets, and hashes scheduler/token/tile work from that contract. `PageRenderer::render_page_display_list_tile_cancellable_with_contract_and_cache` applies the contract-aware tile path. Exactness for retained-tile refusals now comes from `ExactnessPolicy`, so a compatibility render-mode token with a high-quality exact contract reports the high-quality refusal policy correctly. Server `render_contract_json` / `contract_json`, C ABI/header, Python, WASM/TypeScript, .NET, and Java source surfaces now expose contract-backed progressive constructors. This closes the source-level full-page and clipped progressive contract parity gap while leaving external runtime package matrices and noncanonical foreign progressive surfaces out of this local source pass. Evidence: focused progressive contract tests including clipped-output comparison, progressive refusal/fallback tests, server progressive integration test, C ABI contract-constructor test, Rust checks/clippies for engine/server/C/Python/WASM, .NET build, and direct Java source compile.

2026-08-28 local continuation, progressive retained-tile refusal event follow-up: progressive retained display-list tile failures now populate the structured fallback-event report fields before the job enters `Failed`. `render/progressive.rs::render_next` records `unsupported_display_list_retained_tile_refusal` for the typed unsupported retained-replay path, including call site, trigger, output, degradation, Standard/high-quality availability, replacement, final policy, and tile identity. This keeps the refusal auditable without publishing fallback pixels or restoring immediate rendering. Evidence: `progressive_refuses_unsupported_display_list_tile` focused test filter, `fallback_report_keeps_codes_and_structured_policy_details`, `cargo fmt --all --check`, engine check, and engine clippy.

2026-08-28 local continuation, exact PostScript shading-domain regionalization follow-up: SVG/PS vector fallback now preserves the PDF shading `/Domain` on axial/radial vector shading records, and PostScript exact Type 2/Type 3 sidecar output no longer requires the shading dictionary domain to be `[0 1]`. `render/postscript.rs` emits `/Domain [...]` only when an exact PostScript sidecar function is present, so sampled compatibility gradients keep normalized stop-offset behavior. Nonlinear SVG shadings remain typed whole-page/refusal boundaries because SVG cannot represent the nonlinear PDF function exactly. Evidence: `nonlinear_type2_function_with_non_unit_shading_domain_is_exact_postscript_regional`, `ps_shading_domain_clause_is_exact_function_only`, `cargo fmt --all --check`, focused tests, engine check, and engine clippy.

2026-08-28 local continuation, SMask transfer-function cache-identity follow-up: ExtGState SMask group cache keys now include a deterministic `/TR` transfer-function label derived before cache lookup. Absent or `/Identity` transfer functions share the identity no-op label; direct function objects use the structural PDF-object fingerprint path; indirect transfer-function refs embed `smask-transfer:ref:{object}:{generation}:`, which is also emitted by `source_cache_markers_for_object` for source-cache pruning. This prevents one cached group alpha mask, after transfer-curve post-processing, from being reused for a different `/TR`, and prevents malformed `/TR` entries from bypassing typed validation through a stale cache hit. Evidence: `smask_group_cache_key_includes_contract_output_identity`, `smask_group_cache_key_includes_non_device_color_space_parameters`, `alpha_smask_transfer_zero_backdrop_is_not_double_applied`, `unsupported_smask_transfer_function_returns_typed_refusal`, `malformed_smask_transfer_function_returns_typed_refusal`, `transaction_invalidation_registers_source_cache_markers`, `cargo fmt --all --check`, `cargo test -p wellfriendpdf-engine smask_group_cache_key --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine smask_transfer --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine transaction_invalidation_registers_source_cache_markers --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, and `git diff --check`.

2026-08-28 local continuation, ICC transform render-contract cache-scope, profile-digest, proof-cache, byte-accounting, and LRU follow-up: `ColorTransformOptions` now carries a render-contract/output-profile cache scope, and `IccTransformKey` includes a transform kind plus that scope beside backend, full SHA-256 profile digest, profile length, source/destination channel types, rendering intent, and black-point-compensation policy. Active page rendering salts ICCBased and output-intent proof CMM transforms from the schema-v1 render-contract fingerprint, so display, print, proof, and other contract/output-profile variants cannot share an otherwise identical cached transform, and same-length profile bytes cannot alias through a shortened 64-bit hash. Native LittleCMS output-intent proofing now uses the same bounded ICC transform cache, admission, eviction, and metrics path instead of constructing an uncached proof transform per call. ICC transform cache entries are now byte charged, admitted only inside entry and byte caps, evicted under byte pressure, rejected when over budget, refreshed on hit for LRU eviction order, and exposed through `ColorReport` with admissions, rejections, bytes used, and max bytes. Standalone CMM helpers keep scope `0`. Evidence: `transform_cache_key_includes_render_contract_scope`, `transform_cache_key_uses_full_profile_digest_identity`, `color_transform_options_include_render_contract_scope`, `output_intent_proof_transform_uses_bounded_cache`, `transform_cache_rejects_entries_over_byte_budget`, `transform_cache_hit_refreshes_lru_eviction_order`, `reports_tint_transform_cache_limits`, `cargo fmt --all`, `cargo test -p wellfriendpdf-engine transform_cache --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine output_intent_proof_transform_uses_bounded_cache --lib --features native-cmm-lcms2 --jobs 1 -- --test-threads=1 --nocapture`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, `cargo clippy -p wellfriendpdf-engine --lib --features native-cmm-lcms2 --jobs 1 -- -D warnings`, and `git diff --check`.

2026-08-28 local continuation, font source-marker cache pruning follow-up: document font byte/resolver cache keys now include `font:ref:{object}:{generation}:` markers for preserved page/Form font resource refs and nested indirect refs such as FontDescriptor/FontFile/CMap descendants. `RenderDocumentCache` source-scoped pruning removes matching font byte and font resolver cache entries when a mapped font or font-source object changes, while retaining unrelated font cache entries and exact raster-tile invalidation. This closes a source-cache stale-font boundary where the resource dictionary key could remain stable while a referenced font stream/source object changed. Evidence: `document_font_cache_key_includes_source_reference_markers`, `source_scoped_font_cache_pruning_uses_source_reference_markers`, `cargo fmt --all`, `cargo test -p wellfriendpdf-engine font_cache --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-28 local continuation, image decode tile/component write-set follow-up: transaction write-set normalization now covers source-region, image-region, region-decode, reduction, image-component, component-selection, codec-tile, image-tile, JPX-tile, JPX-component, progressive-image, and image-progression refs. These snake_case and binding-style camelCase fields accept structured object/generation payloads, map known refs through cache-owned source identities, register `image-decode:ref:{object}:{generation}:` source-cache markers, and invalidate exact dependent tiles. This does not claim codec-native ROI/progressive JPX output; it closes the source invalidation/ref-normalization side for those future-capability objects. Evidence: `nested_write_set_collector_accepts_image_decode_parameter_aliases`, `render_invalidation_plan_json_maps_image_decode_tile_component_refs`, `cargo fmt --all`, `cargo test -p wellfriendpdf-engine image_decode_parameter_aliases --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine image_decode_tile_component_refs --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-28 local continuation, optional-content Properties write-set follow-up: transaction cache-plan application now treats changed OC `/Properties` resources as first-class nested write-set refs. `render/transaction_invalidation.rs` accepts snake_case and binding-style camelCase `changed_oc_properties_refs` / `changedOCPropertiesRefs` and `changed_properties_refs` / `changedPropertiesRefs` with structured object/generation payloads, maps known refs through `RenderDocumentCache` source identities, registers a `properties:ref:{object}:{generation}:` source-cache marker, and invalidates only exact dependent tiles. Unknown refs still use the conservative reset path. Evidence: `nested_write_set_collector_accepts_optional_content_properties_aliases`, `render_invalidation_plan_json_maps_optional_content_properties_refs`, `cargo fmt --all`, `cargo test -p wellfriendpdf-engine optional_content_properties --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-28 local continuation: exact-tile resource dependency extraction now also preserves indirect `/Properties` refs and records active `/OC` marked-content property refs for tile-affecting paints, alongside named color-space resources used by referenced image `/Mask` and `/SMask` streams, retained inline images, native shading dictionaries, pattern resources, ExtGState soft-mask group color spaces, and direct color-space arrays that contain nested named color-space resources, including their transitive referenced objects. A changed OCG/OCMD property object or function behind a soft-mask, inline-image, shading, pattern, SMask group, or nested `/Indexed`/tint-space base color-space resource can now invalidate the recorded tile through source-to-source expansion instead of relying only on broader page/resource invalidation. Evidence: `page_resources_preserves_properties_reference`, `form_resource_overlay_preserves_properties_reference`, `marked_content_properties_dependency_records_oc_tile`, `marked_content_properties_dependency_restores_after_emc`, `image_xobject_smask_color_space_dependency_records_transitive_tile`, `inline_image_color_space_dependency_records_transitive_tile`, `shading_color_space_dependency_records_transitive_tile`, `pattern_color_space_dependency_records_nested_resource_tile`, `extgstate_smask_group_color_space_dependency_records_transitive_tile`, `color_space_source_dependency_records_nested_named_resource_tile`, `image_xobject_color_space_dependency_records_nested_named_resource_tile`, `inline_image_color_space_dependency_records_nested_named_resource_tile`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-27 local continuation: backend-plan resource arena reports now count packed Image XObject color-space payloads, and exact-tile resource dependency extraction records named Image XObject color-space resources plus their transitive referenced objects. A changed function behind `/ColorSpace /Name` can now invalidate the recorded image tile through source-to-source expansion instead of relying only on broader page/resource invalidation. Evidence: `backend_plan_arena_counts_image_xobject_color_space_payloads`, `image_xobject_color_space_dependency_records_transitive_tile`, and `cargo fmt --all`.

2026-08-27 local continuation: annotation/widget AP dirty-route normalization now covers nested AP-state aliases in cache-plan envelopes. `render/transaction_invalidation.rs` recognizes `changed*APRefs`, annotation/widget/form/signature AP refs, AP stream refs, normal/rollover/down appearance stream refs, appearance-state refs, and before/after appearance refs with structured object/generation variants; known refs merge into source IDs and source cache markers, while unknown AP refs still force conservative reset. Dirty-region conversion also accepts annotation/widget/appearance and before/after rect/bounds aliases for viewport-owned exact-tile conversion. Evidence: `nested_write_set_collector_accepts_annotation_widget_ap_state_aliases`, `render_invalidation_plan_json_maps_annotation_widget_ap_alias_refs`, `render_invalidation_plan_json_unknown_ap_alias_ref_resets_cache`, `dirty_regions_accept_annotation_widget_and_appearance_bounds_aliases`, and `cargo fmt --all --check`.

2026-08-27 local continuation: render-invalidation write-set normalization now accepts prompt-level renderer graph aliases for retained operations, display operations, display lists, render resources, render sublists, Form/Type 3/pattern/appearance sublists, spatial entries/indexes, backend plans, backend-plan caches, render-cache/cache entries, and render tiles. Snake_case, camelCase, and known mutation-prefixed fields map through the existing canonical source-ID path; unknown refs still reset conservatively. Evidence: `nested_write_set_collector_accepts_render_structure_aliases`, `transaction_invalidation`, fmt check, engine check, and engine clippy.

2026-08-27 local continuation: packed Image XObject descriptors now carry a pre-resolved named image color-space payload when resource-aware compilation sees `/ColorSpace /Name` or `/CS /Name` on an image XObject stream. `ResolvedXObjectHandle.image_color_space` is threaded through packed replay metadata, and `RenderStatePlanAdapter` validates that the descriptor payload still matches the image dictionary before decoding. Packed image replay therefore avoids the late first-level page-resource color-space lookup for covered image XObjects; malformed or mismatched payloads fail closed through the existing fatal retained-replay path. Evidence: `resource_aware_plan_pre_resolves_high_level_handles`, `packed_plan_replay_refuses_unresolved_resource_descriptors`, fmt check, engine check, and engine clippy.

2026-08-27 local continuation: managed non-progressive caller-owned render-cache handles now have focused local proof. The .NET smoke `RasterRenderingUsesTheCanonicalNativeSurface` passed with the configured `.work` native C ABI DLL on `PATH`, exercising `RenderCache`, cached contract PNG/report rendering, render-invalidation-plan application, and cache clearing. Java main/test sources compiled directly with Java 25 preview enabled, covering the matching `WellfriendPdf.RenderCache` source surface. This closes stale source cache-handle adoption wording for .NET/Java; broader external binding runtime/package matrices remain deferred. Evidence: focused `dotnet test` exit 0 and direct `javac --enable-preview --release 25` exit 0.

2026-08-27 local continuation: the public SDK feature report now uses source-active/deferred-validation renderer category wording instead of stale slash-later buckets. Progressive viewer queue/callback and cancellation report source-exposed C/Python/WASM/.NET/Java/server surfaces while keeping external runtime matrix validation deferred beyond this local source pass. Evidence: `font_signature_decode_dedup_feature_envelopes`, `cargo fmt --all --check`, engine check, and engine clippy.

2026-08-27 local continuation: runtime image-decode capability wording now reports unsupported codec-native ROI, codestream-tile, and native progressive pixel-output paths as unavailable when the selected codec adapter does not expose native partial support. This keeps the source contract aligned with the prompt requirement to expose capability accurately instead of faking region/progressive decode. Evidence: `renderer_capabilities_disclose_fallback_and_progressive_limits`.

2026-08-27 local continuation: runtime capability wording now distinguishes source-active binding/cache/concurrency surfaces from external runtime validation. Caller-owned surface, progressive viewer queue/callback, render-cache telemetry, and renderer thread/cache matrix entries now use deferred-validation wording where the remaining proof requires external binding/runtime matrices rather than additional source implementation in this local prompt. Evidence: `renderer_capabilities_disclose_fallback_and_progressive_limits` and server capabilities endpoint assertions.

2026-08-27 local continuation: runtime Type 3 capability reporting now matches the retained CharProc source implementation. `runtime_capabilities_for` no longer labels immutable Type 3 sublists as incomplete; it reports bounded `Type3RetainedCharProcPlan` cache coverage with contract/resource/source-keyed immutable sublists, cached refusals, child-state absorption, source-marker pruning, and ordinary-font/Unicode-alias typed refusal. Unsupported CharProc content remains a typed refusal boundary. Evidence: `renderer_capabilities_disclose_fallback_and_progressive_limits` plus focused `type3_retained_charproc` and `type3_program_cache` tests.

2026-08-27 local continuation: runtime image-decode capability reporting now advertises the bounded progressive image decode lifecycle added in source. `RuntimeCapabilityReport` states that `ProgressiveImageDecodeSession` completes native source-window, native reduction, or `requires_component_decode` work as `planned_partial_decode_complete`, while codecs without native partial/progressive pixel APIs still report explicit unavailable capability rather than fake support. Evidence: `renderer_capabilities_disclose_fallback_and_progressive_limits`.

2026-08-27 local continuation: `AlphaMask::fused_clip_window` now reuses one bounded clip-opacity row scratch buffer across the fused output window instead of allocating a new `Vec<u8>` for every scanline. The helper still fills clip opacity per row, uses `wellfriendpdf_render_simd::multiply_alpha_rows` where available, and falls back to the existing scalar alpha product. This narrows the clip + soft-mask/image-mask fusion allocation boundary without claiming full concrete-clip elimination. Evidence: `alpha_mask_fused_clip_window`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, and `cargo fmt --all`.

2026-08-27 local continuation: the direct PDFium C harness now emits a stable JSONL `failure_class` beside every error `code` and `detail`. Harness errors are grouped as `invalid_request`, `unsupported_option_combination`, `pdfium_runtime`, `bitmap_surface`, `raw_output`, `manifest_output`, or `harness_error`, while existing specific error codes remain unchanged. The compile-only MSVC source guard passed through the Visual Studio developer environment against stub public PDFium headers. Evidence: `cmd.exe /d /s /c "<VsDevCmd> && cl /std:c11 /W4 /WX /I .work\\final-universal-renderer-implementation\\pdfium-stub\\public /c tools\\pdfium-harness\\render_page.c /Fo.work\\final-universal-renderer-implementation\\pdfium\\render_page.obj"` exit 0.

2026-08-27 local continuation: packed vector plans now carry their own compiled viewport and source byte estimate. `PackedDisplayList::viewport` and `source_approx_bytes` are populated during `compile_with_resources`, `RenderSpatialIndex::compile` builds its grid from that packed viewport, `RenderPlan::execute_vector_tile_with_scratch` constructs tile-local windows from the packed plan, and `estimate_render_plan_bytes` uses the cached source byte count rather than asking the retained source display list for page dimensions or source size. This narrows the hot/cold retained-data boundary for vector tile execution and packed-plan accounting; the remaining transparent-page-group source-list dependency is closed by the packed metadata continuation below. Evidence: `packed_plan_uses_packed_viewport_for_vector_tile_execution`, `packed_vector_plan_replays_without_raw_content_cold_table`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, and `cargo fmt --all`.

2026-08-27 local continuation: transparent-page-group planning is now retained-stat driven for compiled display lists. `DisplayListStats::requires_transparent_page_group` is computed during display-list classification from ExtGState alpha/blend/SMask dictionaries and Form XObject transparency-group metadata in `PageResources`; `display_list_needs_transparent_page_group` and the contract render path consume that stat instead of fetching raw page content and rescanning `ContentOperation`s after retained compilation. Complete no-paint alpha ExtGStates still avoid page-backdrop allocation. Evidence: `alpha_ext_gstate_does_not_force_page_compatibility_run`, `complete_no_paint_alpha_extgstate_does_not_require_page_group`, `transparent_form_xobject_sets_page_group_stat_without_raw_rescan`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, and `cargo fmt --all`.

2026-08-27 local continuation: packed transparent-page-group planning now uses compile-time packed metadata and no longer retains the source display list inside `PackedDisplayList`. `compile_with_resources` captures `DisplayListStats::requires_transparent_page_group` into the packed plan, `execute_plan_with_state` and the document-cache variant read `plan.packed.requires_transparent_page_group()`, and the `source()` accessor plus `Arc<DisplayList>` payload were removed from the packed plan. Evidence: `packed_plan_carries_transparent_page_group_stat_without_source_display_list`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-27 local continuation: progressive image-decode lifecycle reports now complete bounded native source-window, reduced-resolution, or component-subset plans instead of leaving them in a nonterminal `full_decode_required` state. `ImageDecodeCapabilityReport::uses_native_bounded_decode` recognizes native sub-rectangle and native reduction plans, and plan-derived `ProgressiveImageDecodeRequest` values now carry `requires_component_decode` so native raw component-subset work can complete through the same `planned_partial_decode_complete` phase. JPEG/JPX/JBIG2/lossless paths whose selected decoder APIs expose no native progressive or partial pixel continuation still report `full_decode_required`. Evidence: `progressive_native_window_plan_completes_without_full_decode_requirement`, `progressive_native_reduction_plan_completes_without_full_decode_requirement`, `progressive_native_component_subset_plan_completes_without_full_decode_requirement`, `progressive_image_decode_session_reports_full_decode_required`, `image_decode_planning`, engine check, engine clippy, and fmt.

2026-08-27 local continuation: packed-plan state-run optimization now folds non-adjacent color-space setters across independent no-paint state operations. `PackedDisplayList` tracks color-space/color dependency invalidations so `cs ... w ... cs` can overwrite the earlier fill color-space setter, while `cs ... sc/scn ... cs` and `cs ... rg ... cs` keep both color-space descriptors because the intervening color setter depends on or changes that color state. Evidence: `packed_plan_overwrites_non_adjacent_color_space_across_independent_state`, `packed_plan_does_not_overwrite_color_space_across_generic_color_setter`, `packed_plan_does_not_overwrite_color_space_across_device_color_setter`, `packed_plan_`, engine check, engine clippy, and fmt.

2026-08-27 local continuation: packed state-run optimization now composes adjacent and no-paint non-adjacent `cm` matrix concatenations before paint. `PackedDisplayList` detects consecutive `GraphicsStateDescriptor::ConcatMatrix` descriptors, replaces the previous hot descriptor with `concat_matrix(current, previous)`, tracks a CTM slot across independent state setters, and drops separated inverse pairs whose composition is identity. This narrows RV-10 transform folding without crossing paint, scope, save/restore, clip, resource, ExtGState side-effect, or geometry boundaries. Evidence: `packed_plan_folds_adjacent_matrix_concatenation`, `packed_plan_folds_non_adjacent_matrix_concatenation_across_independent_state`, `packed_plan_folds_non_adjacent_inverse_matrix_concatenation_to_noop`, `packed_plan_`, fmt check, engine check, and engine clippy.

2026-08-27 local continuation: backend-plan arena reports now expose typed resource-arena counts in addition to generic descriptor-kind counts. `BackendPlanArenaReport.resource_arena_entries` and `BackendDocumentPlanArenaReport.total_resource_arena_entries` report pre-resolved font, image, Form, Type 3, pattern, shading, transparency-group, descriptor-level appearance placeholder, color-space including pre-resolved Image XObject color-space payloads, ExtGState, Properties, and inline-image payload counts derived from `NativeDescriptor` entries. This makes packed high-level payload coverage auditable through existing Rust SDK, CLI, server, and binding JSON report paths without re-entering parser or semantic structures; annotation/widget appearance program cache telemetry remains separate in `RenderDocumentCache`. Evidence: `backend_plan_arena_report_exposes_resource_specific_payload_counts`, `backend_plan_arena_counts_image_xobject_color_space_payloads`, `backend_plan_arena`, `cargo fmt --all --check`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

2026-08-27 local continuation: display-list CPU replay now keeps active clip state as a persistent `Arc<ClipNode>` instead of reconstructing saved clips from the current materialized buffer mask. `CpuRenderDevice` pushes the active node on `q`, composes rectangle and general path clips through `ClipDag::intersect`, and reinstalls the saved node on `Q` through a single buffer-sync helper. This closes a retained display-list save/restore clip-DAG gap while leaving broader `PixelBuffer` concrete-mask paint-boundary adoption incomplete. Evidence: `cpu_render_device_save_restore_reuses_active_clip_dag_node`, `display_list`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: `PixelBuffer` hot paint-row paths now borrow the active `ClipMask` instead of cloning it for alpha-mask, cached RGBA, solid-fill, full-buffer composite, and offset-composite rows. `blend_alpha_mask_strided`, `blend_rgba_pixels_at`, `fill_rect`, `composite_from`, and `composite_from_at` split mutable pixel-row access from read-only clip-run inspection, so partial/binary/high-quality clip branches no longer allocate a copied concrete mask before painting. This narrows the concrete-mask paint-boundary gap but does not remove concrete clip masks from raster buffer installation or complete full clip/mask fusion. Evidence: `alpha_mask`, `fill_rect`, `composite_from`, `blend_rgba_pixels_at`, focused engine check, fmt, and clippy.

2026-08-27 local continuation: runtime capabilities now expose a structured renderer concurrency/cache matrix instead of leaving thread/cache ownership as prose. `RuntimeCapabilityReport.renderer_concurrency_cache_matrix` records host/effective CPU workers, max concurrent documents, serial mutation policy, work stealing, renderer thread permit rows for parse/decode/render/shaping/progressive-publication/mutation work, cache ownership rows for display lists, render tiles, image/mask caches, font/glyph caches, decoded streams, and transaction provenance, plus cancellation and stale-publication guard surfaces. The server `/api/v1/capabilities` endpoint exposes the same matrix. This is source/API proof, not external runtime thread/cache validation. Evidence: `renderer_concurrency_matrix_reports_thread_and_cache_boundaries`, `renderer_capabilities_disclose_fallback_and_progressive_limits`, `capabilities_endpoint_exposes_renderer_cache_pressure_and_concurrency_policy`, engine check/clippy, and server integration clippy.

2026-08-27 local continuation: active page-renderer state now keeps `RenderState::current_clip` as the logical clip DAG handle for page content, retained display-list replay, packed-plan replay, Form cleanup, annotation appearance replay, tiling/shading pattern temporary clips, Type 3 CharProc scopes, SMask groups, and transparency/Form group child states. Clip application intersects against the active node, `install_clip_node` is the only active-state buffer synchronization point, and explicit render-contract identity refresh re-interns the current clip into the new scoped DAG. Evidence: `q_restore_restores_previous_clip_mask`, `content_path_clip_replay_reuses_transformed_clip_node_cache`, `clip_dag`, `display_list`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: bounded Form transparency-group backdrop surfaces now reuse the renderer offscreen pool instead of allocating or cloning every window. `PixelBuffer::copy_rect_into_buffer` copies the group window into a caller-owned reusable surface while preserving the destination's clip/SMask/knockout metadata, `take_knockout_backdrop` extracts stored backdrops for recycling, and `RenderState::handle_do_form_group` uses those helpers for non-isolated backdrop snapshots, non-isolated group buffers, and knockout backdrop seeds. This closes a surface-lifetime gap for existing bounded group rendering, not full group colour-space/backdrop/print/knockout semantic closure. Evidence: `copy_rect_into_buffer_reuses_existing_surface`, `knockout_backdrop_can_be_extracted_for_reuse`, `transparency_group`, `knockout`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: packed Form XObject replay now uses pre-resolved Form BBox/Matrix metadata from `ResolvedXObjectHandle` when building cached Form programs. The resource-aware packed dispatcher threads handle subtype, stream dictionary, BBox, and Matrix through replay metadata, so supported Form descriptors no longer re-extract those dictionary fields unless the caller lacks resource-aware handle metadata. Evidence: `resource_aware_plan_pre_resolves_high_level_handles`, `packed_plan_replay_refuses_unresolved_resource_descriptors`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: existing knockout group replacement now uses bounded row slices instead of a per-pixel loop. `PixelBuffer::knockout_from` preserves source RGB replacement semantics, source/group/soft-mask/clip-scaled alpha, clip-zero skips, and zero-soft-mask transparent replacement RGB, while reporting `knockout_row_pixels` through core/CLI compositor telemetry. This closes a source hot-path boundary for the existing knockout replacement primitive, not full PDF knockout/backdrop/group-colour-space closure. Evidence: `knockout_from_fuses_partial_clip_and_soft_mask_into_row_path`, focused engine check/clippy, and focused CLI check/clippy.

2026-08-27 local continuation: normal Compat LCD/subpixel glyph masks now use a bounded row-local compositor when clip or SMask state is active. `PixelBuffer::blend_lcd_alpha_mask_strided` computes LCD red/green/blue subpixel coverage, fuses active clip opacity and SMask bytes, writes the destination row directly with the same source-over math as the former per-pixel LCD path, and reports `lcd_row_pixels` through `PixelCompositorStats`; the per-pixel LCD helper is retained only as a test oracle. This closes a supported glyph-mask clip/SMask paint boundary but not universal concrete-mask elimination or non-normal/high-quality LCD policy. Evidence: `lcd_alpha_mask_fuses_partial_clip_and_smask_into_row_path`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: renderer invalidation now has source-to-source dependency expansion. `RenderDependencyGraph` records parent resource dependencies on nested shared resources, expands changed sources through dependent sources for page/tile invalidation, and `RenderDocumentCache` prunes source-scoped artifact caches with that expanded set. The retained display-list tile resource dependency walker records those edges for bounded resource walks, so a nested image/font/color-space/pattern/ExtGState/shading resource edit can prune parent retained resource programs instead of only child cache entries. Evidence: `changed_shared_resource_invalidates_dependent_source_tiles`, `changed_shared_resource_expands_transitive_dependents`, `shared_resource_invalidation_prunes_dependent_source_artifact_caches`, `bounded_form_xobject_records_transitive_resource_tile_dependency`, focused engine check, and focused engine clippy.

2026-08-27 local continuation: Rust/server cache owners can now merge dirty-region rectangles into `RenderInvalidationCachePlan` exact tiles before applying the plan. `RenderInvalidationCachePlan::merge_dirty_region_tiles` reuses the existing caller-supplied viewport/grid conversion, deduplicates tile entries, and then lets `apply_to_cache` use the exact-tile invalidation path so page artifacts are pruned without expanding raster invalidation to every recorded page tile. This narrows the dirty-region cache-plan gap without inferring page geometry inside the parser. Evidence: `render_invalidation_plan_merges_dirty_region_tiles_before_apply`, `transaction_invalidation`, focused engine check, fmt, and clippy.

2026-08-27 local continuation: C ABI/header, Python, and WASM/TypeScript render-contract builders now cover every remaining non-identity schema-v1 policy field instead of requiring raw JSON edits outside .NET/Java/server. C exposes `wellfriendpdf_render_contract_with_schema_policies`; Python and WASM expose method-level builders for page box, execution/backend/compositing policy, annotations/forms, optional-content identity, smoothing, color/print/prepress, exactness, and determinism. All new paths validate through the canonical Rust `RenderContract::validate`, and enum inputs accept canonical schema names plus lowercase kebab/snake aliases. Evidence: `capi_render_contract_schema_policy_builder_round_trips`, `cargo check -p wellfriendpdf-capi --lib --jobs 1`, `cargo check -p wellfriendpdf-py --lib --jobs 1`, `cargo check -p wellfriendpdf-wasm --lib --jobs 1`, and `cargo fmt --all --check`.

2026-08-26 local continuation: adaptive progressive tile scheduling now publishes a canonical `ProgressiveTileSchedulerReport` through progressive tokens and step reports. The selector still uses only the supported tile sizes `128`, `192`, `256`, `384`, and `512`; adaptive requests start from deterministic page-area and temporary-memory-budget policy, then reduce the target for source-visible render-plan/contract pressure: high-quality mode, print/proof profile, scalar backend, transparency operations, image density, clip pressure, and complexity tiers derived from display-list and packed-plan counts. Fixed tile requests remain fixed and are reported as fixed. Existing progressive JSON surfaces carry the report through Rust, server, C/Python/WASM/.NET/Java without binding-specific scheduler logic. Evidence: `adaptive_tile_size_is_deterministic_and_budget_bounded`, `adaptive_scheduler_report_uses_contract_and_plan_complexity_pressure`, `zero_tile_dimension_selects_adaptive_size`, `step_report_includes_adjacent_page_prefetch_and_viewer_queue_preview`, server adaptive-start test, and C ABI check.

2026-08-26 local continuation: render-view backend-plan arena reporting now accepts explicit schema-v1 render contracts. `RenderDocumentView::backend_plan_arena_report_for_contract` validates the supplied contract and reports per-page packed-plan arena data with arena kind, execution mode, backend selection, print profile, and output surface, so display, print, and proof CPU packed-plan identities are observable through the Rust SDK, CLI `--contract-json`, server `contract_json`, C ABI/header, Python, WASM/TypeScript, .NET, and Java source surfaces. This closes the source-level print/proof plan observability gap but not GPU/debug backend-native arenas, print-native payloads, or external runtime binding matrices. Evidence: `render_view_exposes_print_contract_backend_plan_arena_report`, `backend_plan_arena_report_accepts_explicit_print_contract`, `backend_plan_arena_report`, focused CLI/server tests, C/Python/WASM checks, .NET build, and direct Java 25 `javac` source checks.

2026-08-26 local continuation: resource-aware packed text compilation now tracks active pre-resolved font state across packed save/restore, refuses visible text-showing descriptors before a pre-resolved font is active, and pre-resolves ExtGState `/Font` selections into packed state descriptors. Packed replay now saves/restores active pre-resolved font/color/pattern resources alongside graphics, clip, and soft-mask state, so `Q` does not clear hot replay resource handles and force later name lookup. Stale packed ExtGState descriptors with `/Font` but no pre-resolved font handle fail typed at replay before text state mutates. This closes a packed text/font pre-resolution gap; universal packed coverage for every high-level operation remains incomplete. Evidence: `packed_retained_text`, `packed_text_font_tracking_restores_unresolved_state_after_q`, `packed_ext_g_state`, `packed_plan_restore_restores_pre_resolved_active_font_resource`, `packed_plan_replay_refuses_unresolved_state_resource_descriptors`, and focused engine clippy.

2026-08-26 local continuation: nested retained Form XObject, tiling-pattern tile, annotation-appearance, and Type 3 CharProc replay now restore the active pre-resolved resource side stack across scoped subprogram execution. If an inner packed plan records a fatal error after `q` and stops before `Q`, cleanup restores the outer active-resource stack and active font/color-space/pattern handles instead of leaking a stale side-stack entry into the caller. Annotation appearances also clear active pre-resolved resources when resetting to a fresh appearance graphics state, then restore the outer state on exit. Type 3 retained CharProc replay now returns `false` when retained replay recorded a fatal render error. This closes a nested retained state-isolation hole; it does not close broader universal packed coverage. Evidence: `active_resource_stack_after_inner_refusal` and `packed_plan_restore_restores_pre_resolved_active_font_resource`.

2026-08-26 local continuation: progressive image-decode reports now derive `full_decode_required` from the actual plan shape, not from whether the codec family has a native windowed path for other shapes. Full-source/full-resolution plans without native progressive decode report `full_decode_required` even for raw/CCITT-capable windowed codecs; reduced or source-windowed plans still do not. This is a reporting/lifecycle correctness fix and does not claim new native progressive, ROI, or codestream-tile JPX/JPEG/JBIG2 support. Evidence: `progressive_full_raw_plan_reports_full_decode_required_even_with_window_capability` and `progressive_image_decode_session_reports_full_decode_required`.

2026-08-26 local continuation, amended 2026-08-27: transaction render-invalidation write-set collection now recognizes additional shared-resource and document-structure aliases for form fields/values, AcroForm state, optional-content state/config, Type 3 glyph/CharProc programs, font descriptors, CMaps, image masks, soft masks/image SMask, transparency groups, widget/form/signature appearances, page/page tree, catalog, metadata/XMP, Names/name tree/page labels, StructTree, ParentTree, RoleMap, and image decode metadata including filter, Decode/DecodeParms, interpolation, matte, color-key mask, and JPEG/DCT/JPX/CCITT/JBIG2 parameter refs. These aliases are accepted both as `changed_*_refs` fields and as structured object/generation key names inside SDK/server/report envelopes. Known refs map to source cache markers; unknown refs still conservatively reset. This reduces one source-level shared-resource classification gap without claiming arbitrary transitive write-set completeness. Evidence: `nested_write_set_collector_accepts_shared_resource_aliases`, `nested_write_set_collector_accepts_render_structure_aliases`, `nested_write_set_collector_accepts_document_structure_and_group_aliases`, `nested_write_set_collector_accepts_image_decode_parameter_aliases`, and `transaction_invalidation`.

2026-08-26 local continuation: packed inline-image descriptors now pre-resolve named inline `/ColorSpace` resources during resource-aware compilation. Well-formed `/ColorSpace /Name` and `/CS /Name` operands carry a `ResolvedInlineImageColorSpace` object payload into packed replay; missing named resources compile to a typed `MissingColorSpaceResource` refusal with usage `inline image`. Built-in device spaces, including inline abbreviations `/G`, `/RGB`, and `/CMYK`, and image masks remain resource-object-free, and malformed inline parameter shapes stay on the existing renderer parser's typed refusal path. This removes one remaining packed inline-image resource-name lookup; it does not close all high-level packed payload coverage. Evidence: `inline_image_descriptor`.

2026-08-26 local continuation: Type 3 full CharProc replay now caches the compiled retained packed subplan instead of rebuilding it for each glyph render. `render/page_renderer.rs` carries a bounded `Type3ProgramCache<Type3RetainedCharProcPlan>` through document cache ownership, child SMask/transparency states, parent absorption, budget eviction, source/page pruning, and telemetry-style stats. Cache identity includes the parsed CharProc key, active retained render-contract fingerprint, merged resource fingerprint, and indirect CharProc source marker when present; cached failures retain the exact retained-plan refusal reason. This closes the Type 3 packed-sublist cache gap for the retained path; unsupported retained Type 3 constructs still fail typed rather than using immediate replay. Evidence: `type3_retained_charproc`, `type3_program_cache`, and `render_document_cache_budget_evicts_type3_parsed_program_caches`.

2026-08-26 local continuation: active CPU render contracts now accept `ExecutionMode::Research` as an identity-bearing field while keeping local deterministic CPU execution. Research-only GPU/distributed/solver/autotuning capabilities remain policy-gated in runtime capability reporting. Evidence: `contract_research_execution_mode_renders_with_distinct_identity`, `contract_reports_unsupported_semantic_fields_by_name`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

2026-08-26 local continuation: active CPU render contracts now accept `BackendSelection::ResearchHybrid` as a guarded CPU hybrid-dispatch backend. It uses the existing active CPU dispatcher rather than the scalar-only guard, keeps a distinct render-contract cache fingerprint and image-decode backend identity, and no longer appears in unsupported semantic field diagnostics. The server builder accepts `research-hybrid` and returns normalized `ResearchHybrid` contract JSON. This does not implement GPU, printer, debug-device, or fully backend-native packed payload backends. Evidence: `contract_research_hybrid_backend_renders_with_distinct_identity`, `contract_reports_unsupported_semantic_fields_by_name`, `render_contract_builder_accepts_research_hybrid_backend`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

2026-08-27 local continuation: runtime capability JSON now includes an explicit `font_resolution_and_substitution_reporting` entry for the active deterministic font policy: embedded/document faces, caller-registered provider faces, deterministic-system mappings, bundled compatibility fallback, and typed missing-font refusal. The reason string discloses registered-provider fingerprint cache identity, deterministic-system resolution source, high-quality/exact acceptance/refusal boundaries, and Rust/server/C/Python/WASM/.NET/Java/CLI source surfaces. The clip DAG also preserves dense partial-coverage alpha when materializing a dense clip state at a different output size by copying the overlap and clipping outside-source pixels, instead of producing a silent empty mask. Evidence: `renderer_capabilities_disclose_fallback_and_progressive_limits` and `materialize_dense_alpha_size_mismatch_preserves_overlap`.

2026-08-27 clip-DAG materialization continuation: `render/clip_dag.rs::ClipNode` now stores lazy materialized masks by destination dimensions instead of one mask per node. Shared Full/Empty/Rectangle/composite clip nodes can therefore be materialized at multiple output sizes without returning stale dimensions, while same-dimension calls reuse the cached `Arc<ClipMask>` and display-list restore clones from that shared result. Evidence: `materialize_cache_is_keyed_by_dimensions`, `clip_dag`, engine check, engine clippy, and workspace check.

2026-08-27 image XObject color-space/role identity continuation: `render/page_renderer.rs::image_xobject_cache_key` now includes the resolved image color-space string plus image-mask and soft-mask role bits. Same-object XObject decode cache entries no longer collide when color-space family or mask semantics differ inside one render context, while explicit color-space override objects continue through the existing hashed override key path. The helper's inline branch now also includes BPC, filters, color-space, mask roles, payload length, and payload hash instead of only page/size/length. Evidence: `image_xobject_cache_key_includes_color_space_and_mask_role`, `image_decode_cache_key`, engine check, engine clippy, and workspace check.

2026-08-27 portable image decode cache identity continuation: decoded image base keys now separate decoded-resource identity from pure page/tile placement. `render/page_renderer.rs::image_decode_cache_base_key` uses document revision, decoded source identity, and a decode-policy fingerprint for render mode, backend, print/exactness policy, CMM intent/backend, overprint, image smoothing, and resource budget, while dropping page number, viewport, tile origin, device transform, annotation/form policy, and optional-content state from the base key. Inline images use the same decode-policy identity while retaining full payload hashing. The final planner key still includes source region, target size, reduction, quality, component selection, target format, backend, decode array, DecodeParms, mask/SMask, interpolation, and decode-policy identity, so clipped or scaled placements stay distinct while compatible repeated decodes can be reused across pages, Forms, pattern cells, and viewer rerenders. Evidence: `image_decode_cache_key_uses_portable_decode_contract_identity`, `inline_image_decode_cache_key_hashes_full_payload_and_decode_policy`, `image_decode_cache_key`, `image_decode_planning`, engine check, and engine clippy.

2026-08-27 standalone display-list replay fail-closed continuation: `render/display_list.rs::render_display_list` now returns `Result<PixelBuffer>` and refuses unsupported or page-context-native display lists instead of logging warnings and returning pixels after skipping high-level native operations. The exported `replay_display_list` API now returns `Result<()>`, refuses unsupported lists, and requires a `RenderDevice::supports_native_high_level_ops` opt-in before dispatching native retained text, image, shading, pattern, inline-image, or Form XObject operations to device hooks. State/native hook methods no longer have default no-op or warning-only bodies, so replay devices must make state handling explicit and opt-in native devices must provide explicit handlers. Vector-only display lists still render through `CpuRenderDevice`; retained high-level operations must use `PageRenderer` display-list replay where resources and renderer state are available. Evidence: `standalone_display_list_replay_refuses_native_high_level_ops`, `direct_display_list_replay_refuses_native_high_level_ops_on_default_device`, `display_list`, engine check, and engine clippy.

2026-08-27 local continuation: native scaled JPEG and JPX Image XObject decode scheduling now reserves memory from the requested reduced target dimensions instead of the full uncompressed source image. Reduced decode plans can therefore pass scheduler admission under target-sized budgets and still fail through typed decoder/stream limits when the underlying object or codec is invalid. Unsupported packed graphics-state descriptors now carry only the operator needed for typed compile refusal, not raw operand vectors. Stale display-list compatibility-run counters were removed because `BX`/`EX` are typed state descriptors or malformed-sequence refusals, not fallback runs, and tile stitching now asserts tile-scoped transparent-page-group cache identity. The same slice records a clean renderer/image/font/SIMD/runtime source-marker audit for the prompt's TODO/stub/not-implemented/immediate/legacy terms, with ExtGState `/SA`, `/AIS`, and `/TK` visible-paint boundaries expressed as explicit exact-policy typed refusals. Evidence: `scaled_jpeg_xobject_scheduler_reserves_target_size_not_full_source`, `scaled_jpx_xobject_scheduler_reserves_target_size_not_full_source`, `unsupported_state`, `compile_refusal`, `graphics_state_descriptor_unknown_operator_is_unsupported`, `display_list_tile_stitch_matches_full_page`, `extgstate_`, and the scoped marker `rg`.

2026-08-26 local continuation: the canonical document-view report and render-view backend-plan arena report are now exposed beyond engine and binding shims through CLI `document-views-report` / `backend-plan-arena-report` and server `POST /api/v1/document-views/report` / `POST /api/v1/render-contract/backend-plan-arena-report`. These surfaces call the shared SDK `document_views_report_json` and `backend_plan_arena_report_json` envelopes, keeping lazy view boundaries and per-page hot-op, arena, descriptor-kind, batch, and compile-refusal counts observable without rendering. Evidence: `document_views_report_command_emits_json`, `document_views_report_route_returns_json`, `backend_plan_arena_report_command_emits_json`, and `render_contract_backend_plan_arena_report_route_returns_json`, plus focused CLI/server checks.

2026-08-25 local continuation: packed retained replay now advances culled non-clipping text through the same `RenderState` decoded-glyph, spacing, `TJ`, font-metric, and Type 3 advance paths used by normal replay. The old culled-text no-op helper is removed, culled `TJ` arrays no longer return before applying string advances and adjustments, and clipping text modes remain on normal replay because they can mutate clip state. Evidence: `packed_culled_text_array_advances_text_state_without_painting` and `packed_culled_spacing_next_line_show_advances_text_state_without_painting`.

2026-08-25 local continuation: the scan-converter convex sampled-span fast path now exposes its pixel counter through `PathRasterStats::convex_fast_pixels`, and CLI render telemetry emits `convex_fast_pixels_delta` alongside the other path-raster counters. This makes the guarded convex fill path observable in source telemetry rather than only through a private atomic. Evidence: `convex_polygon_fill_fast_path_matches_general_scanline` and `concave_polygon_fill_rejects_convex_fast_path`.

2026-08-25 local continuation: active raster shading now accepts well-formed PDF `/Function` arrays by evaluating each one-output component function through `eval_function_or_array_n`. The active validator rejects empty, oversized, malformed, or multi-output component arrays before paint, while Type 1, axial/radial, and mesh shading sampling share the same array-aware evaluator. Evidence: `function_array_evaluates_one_output_component_functions`, `function_array_rejects_multi_output_component_functions`, `axial_shading_function_array_paints_component_gradient`, and `malformed_active_shading_dictionary_returns_typed_refusal`.

2026-08-25 local continuation: active CPU render contracts now implement `image_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`. The accepted policy is part of the retained plan contract, active `RenderState`, annotation replay policy, and nested soft-mask/transparency-group states; inline images and Image XObjects validate `/Interpolate` first, then `Disabled` forces nearest-neighbour sampling while `Antialiased` and `Subpixel` preserve PDF interpolation. Evidence: `contract_image_smoothing_disabled_overrides_interpolate_true`, `contract_subpixel_text_uses_lcd_glyph_mask_policy`, and `contract_reports_unsupported_semantic_fields_by_name`.

2026-08-25 local continuation: active CPU render contracts now implement `text_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`, plus the separate `subpixel_text` policy. The accepted policies are part of the retained plan contract, active `RenderState`, annotation replay policy, and nested soft-mask/transparency-group states; device glyph-mask cache keys include binary-alpha state; disabled smoothing thresholds cached glyph masks and routes direct ordinary/color glyph fills through binary scanline coverage; and subpixel text paints cached or direct glyph masks through an RGB LCD-style per-channel compositor. Evidence: `contract_text_smoothing_disabled_thresholds_glyph_edges`, `contract_subpixel_text_uses_lcd_glyph_mask_policy`, and `contract_reports_unsupported_semantic_fields_by_name`.

2026-08-25 local continuation: active CPU render contracts now implement `path_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`. The accepted policy is part of the retained plan contract, active `RenderState`, annotation replay policy, and nested soft-mask/transparency-group states; reusable path fill/stroke mask cache keys include binary-alpha state; disabled smoothing thresholds cached path masks and uses binary scanline coverage for direct RGB and CMYK overprint-preview paths; and `Subpixel` uses the existing device-space subpixel antialias coverage path. Evidence: `contract_path_smoothing_disabled_thresholds_fill_edges`, `contract_subpixel_text_uses_lcd_glyph_mask_policy`, and `contract_reports_unsupported_semantic_fields_by_name`.

2026-08-26 local continuation: native x86/x86_64 `render-simd` builds now dispatch `unpremultiply_rgba` through a guarded SSE2 four-pixel load/store row path when available. The reciprocal conversion remains scalar per channel to preserve exact bytes, matching the existing wasm32+`simd128` load/store-group contract while adding native public dispatch coverage. The wasm32+`simd128` `premultiply_bgra8` row now also reuses the vector premultiply-RGBA lane helper and emits BGRA with a SIMD channel shuffle instead of extracting each group for per-pixel scalar channel work. Wasm RGB8-to-opaque-RGBA rows now use a SIMD expansion shuffle with an opaque alpha vector, and GrayRGB, GrayRGBA, GrayBGRA, and premultiplied GrayRGBA rows now use a shared four-pixel SIMD luma helper plus SIMD shuffles before scalar tails instead of temporary scalar expansion loops. Evidence: `unpremultiply_public_uses_sse2_when_available`, `sse` filter, wasm32+`simd128` crate check, premultiply filter, gray filter, rgb filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: wasm32+`simd128` GrayRGB, RGB, and BGR row helpers now store 12-byte four-pixel row groups through bounded 64-bit plus 32-bit lane stores instead of whole-vector temporary staging arrays. Evidence: wasm32+`simd128` crate check, gray filter, rgb filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: wasm32+`simd128` Gray8 row output now stores the low four luma bytes through a bounded 32-bit lane store instead of a full-vector temporary staging array. Evidence: wasm32+`simd128` crate check, gray filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: native SSSE3 RGB and BGR row helpers now store 12-byte four-pixel row groups through an SSE2 low-lane store plus a shifted 32-bit lane write instead of whole-vector temporary staging arrays. Evidence: `rgb` filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: native SSSE3 RGB8-to-opaque-RGBA image-row expansion now reads each 12-byte source group through a bounded SSE2 low-lane load plus a shifted 32-bit lane load instead of a temporary source staging array. Evidence: `rgb` filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: native SSSE3 grayscale row helpers now keep four-pixel luma in a SIMD register, store Gray8 through a low 32-bit lane write, expand GrayRGB and GrayRGBA with shuffles, and route premultiplied GrayRGBA through the shared SSE2 premultiply group helper instead of scalar expansion arrays. Evidence: `gray` filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: native x86/x86_64 `render-simd` builds now dispatch `blend_alpha_mask_normal` through a guarded SSE2 four-pixel load/store row path when available. The exact mixed-destination normal Compat source-over byte contract remains scalar-oracle equivalent, matching the existing wasm32+`simd128` grouped path while adding native public dispatch coverage. Evidence: `blend_alpha_mask_normal_public_uses_sse2_when_available`, `sse` filter, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: native x86/x86_64 `render-simd` builds now dispatch the full `blend_separable_opaque_destination` PDF separable set through a guarded SSE2 row path when available. Multiply, Screen, Overlay, Darken, Lighten, HardLight, Difference, and Exclusion use exact integer lane math; ColorDodge, ColorBurn, and SoftLight use SSE float lanes with grouped gather/scatter and scalar-oracle checks, matching the existing wasm32+`simd128` public helper coverage while adding native dispatch. Evidence: `blend_separable_opaque_dst_public_uses_sse2_when_available`, `separable` and `sse` filters, public-kernel scalar-equivalence test, crate check, and crate clippy.

2026-08-26 local continuation: strict SVG/PS vector-output APIs now refuse whole-page raster fallback with typed `UnsupportedFeature`: `render_page_svg_strict`, `render_page_ps_strict`, `ContentEngine::render_page_svg_strict`, `ContentEngine::render_page_ps_strict`, `render_document_ps_strict`, and `render_page_eps_strict`. CLI `render --strict-vector` now selects those strict SVG/PS/EPS paths, reports `strict_vector` in JSON summaries, and rejects raster formats. Compatibility SVG/PS/EPS export retains explicit `is_rasterized` whole-page output for legacy conversion, while runtime `renderer_fallback_policies` now reports zero material-degrading high-quality rows and zero high-quality canonical-immediate routes. Evidence: `svg_strict_refuses_alpha_group_whole_page_raster_fallback`, `ps_strict_refuses_alpha_group_whole_page_raster_fallback`, `ps_strict_refuses_normal_alpha_extgstate_whole_page_raster_fallback`, `render_strict_vector_modes_run_and_report`, `render_strict_vector_rejects_raster_formats`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

2026-08-26 local continuation: PostScript vector classification now keeps supported alpha/blend ExtGState metadata stateful until paint-time checks can distinguish exact no-paint from visible unsupported paint. Fully transparent relevant fill/stroke alpha remains a native vector no-op even when the unused alpha channel is fractional or the blend mode is non-normal; visible fractional-alpha or non-normal-blend paint still fails closed. Transparent-page-group planning continues to use the complete no-paint test before allocating a page backdrop. Evidence: `postscript_transparent_non_normal_blend_paint_is_stateful_vector_noop`, `postscript_stroke_noop_ignores_fractional_fill_alpha`, `visible_postscript_alpha_or_blend_paint_stays_whole_page`, `postscript_visible_non_normal_blend_or_alpha_remains_conservative`, `transparent_page_group_ignores_complete_no_paint_blend_state`, `transparent_page_group_requires_complete_no_paint_blend_state`, and `vector_fallback`.

## Verified implementation work completed in this pass

1. Repaired the interrupted `path.rs` parse break where `fill_flat_aa` had been accidentally commented out by a merged documentation/function line.
2. Repaired the new `render-simd` crate's `unsafe_op_in_unsafe_fn` boundary: SSE helpers now declare target features and unsafe callers are explicit.
3. Fixed SIMD scalar-equivalence guard semantics so a declined short row falls through cleanly rather than comparing untouched output to scalar output. Added `render_simd::tests::public_kernels_match_scalar_or_cleanly_decline_unaligned_rows`.
4. Restored fractional scanline stroke coverage by increasing bounded vertical color coverage samples from two to four; the analytic stroke regression test passes.
5. Removed/reintegrated source-proven dead helpers and repaired all strict Clippy findings without disabling broad warning policy.
6. Made SVG output explicitly rasterize pages containing unresolved, unsupported transparency-bearing, overprint-enabled, stroke-adjustment-enabled, alpha-source-enabled, text-knockout-disabled, visible non-normal-blend paint, or non-identity transfer-function ExtGState operators, while safe line-style/dash/font/flatness/smoothness/default-state/identity-transfer/first-supported-normal-blend-array ExtGState dictionaries, SVG normal scalar alpha, and dense ordinary text replay natively in SVG/PS. This changes known low-fidelity vector paths into explicit `SvgPage::is_rasterized` fallbacks only for unsupported state; `svg_output` passed in historical remote evidence.

   2026-08-25 continuation: SVG now accepts supported non-normal ExtGState `/BM` modes, including safe alpha-bearing blend state with `/SMask /None`, through target-aware vector fallback classification and emits native `mix-blend-mode` styles on SVG path, glyph, regional image, inline-image, shading, shading-pattern, and tiling-pattern paint output. Conservative/no-reader and PostScript classification still require first-supported Normal/Compatible blend semantics.
7. Exposed retained-list, fallback-reporting, progressive-core limitation, and CPU-SIMD/scalar-fallback state through `wellfriendpdf --mode standard capabilities`, with a dedicated runtime test.
8. Added source-based pre-audit, full method inventory, and fallback inventory documents.
9. Exposed bounded render-time font-substitution reports through Rust render APIs, CLI sidecars, C/Python/WASM/.NET/Java source render APIs, and server contract PNG/raw report routes.
10. Closed the active named-color black/default fallback path while preserving valid device aliases: bare `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` resource aliases resolve through named-color conversion; CalGray/CalRGB/Lab named paint now requires well-formed parameters and exact finite components; Separation/DeviceN named colors require exact finite tint vectors; malformed calibrated Separation/DeviceN alternates propagate invalid; unsupported alternates remain unsupported; device alternate outputs and ICCBased scalar inputs now require exact component counts; active page paint records typed renderer errors; invalid tint-space image conversion fails typed; and SMask matte/backdrop family-name-only conversion refuses unsupported/calibrated names instead of using default parameters or black.
11. Closed the active shading ColorSpace black/default fallback path: active shading dictionaries preflight `/ColorSpace` before paint, and sampled function outputs must provide valid arity for the resolved calibrated, ICCBased, Separation, or DeviceN color space plus exact finite component counts for resolved device color spaces before `ShadingRenderer` can convert them.
12. Removed the public image sampler's remaining malformed-buffer opaque-black defensive output: short decoded buffers or unsupported channel counts now produce transparent samples, while active painting continues to reject invalid decoded image shapes before paint.
13. Closed inline image metadata defaults: `paint_inline_image` now records typed refusals when `/Width` or `/Height` is missing or invalid, when non-mask `/BitsPerComponent` is missing or unsupported outside terminal JPX, and when non-mask `/ColorSpace` is missing or present as a non-name object, while keeping the valid `/ImageMask true` stencil path that may omit bpc and color space metadata.
14. Closed image XObject metadata defaults: `handle_do_image` now records typed refusals when `/Width` or `/Height` is missing or invalid, when non-mask `/BitsPerComponent` is missing or unsupported outside terminal JPX, and when non-mask `/ColorSpace` metadata is missing or malformed, before image-reference/cache-key construction or decode planning.
15. Closed explicit image Mask and image SMask metadata defaults: referenced explicit `/Mask` streams no longer inherit source dimensions or default non-stencil bpc/color-space metadata, stencil masks reject present non-1 bpc, and image SMask streams no longer inherit dimensions or default bpc/color-space metadata before decode.
16. Closed uncolored tiling-pattern color-count inference: PaintType 2 pattern paints now reject active pattern-color component counts other than 1, 3, or 4 instead of inferring `/DeviceRGB` for malformed counts.
17. Closed Type 3 path-only geometry defaults: `Type3PathCollector` now rejects malformed path construction, line state, transform, dash, color, and metric operands, and rejects path continuation operators without a current point, instead of fabricating default line state, identity transforms, solid dash state, ignored segments, or extra color operands.
18. Closed active non-text graphics-state operand defaults and restore underflow: retained state descriptors, display-list capture, immediate page rendering, and SVG/PS vector-output classification now share `graphics_state_operand_refusal` for `q/Q`, `cm`, `w`, `J`, `j`, `M`, `d`, `ri`, `i`, direct device colors, `CS/cs`, `SC/SCN/sc/scn`, and `gs`, while display-list/page/vector paths reject `Q` restore underflow, so malformed operands and empty-stack restores fail typed instead of fabricating default state or replaying malformed vector state.
19. Closed active text operand defaults and object-sequence tolerance: retained text/state descriptors, display-list text capture, immediate/packed page text rendering, and SVG/PS vector-output classification now share `text_operand_refusal` for text-state and text-showing operators plus `BT`/`ET` boundary checks, so malformed operands, bare showing/positioning, unmatched `ET`, nested `BT`, and unterminated `BT` fail typed instead of fabricating default font state, text matrices, spacing, empty strings, filtered `TJ` arrays, or malformed vector text.
20. Closed active marked-content and compatibility-section operand/sequence defaults: retained state descriptors, display-list capture, immediate page rendering, packed plan replay, and SVG/PS vector-output classification now share `marked_content_operand_refusal` plus balanced sequence checks for `BMC`, `BDC`, `EMC`, `MP`, `DP`, `BX`, and `EX`, so malformed operands, unmatched active `EMC` ends, cross-stream closures, unmatched `EX`, and unterminated `BMC`/`BDC`/`BX` begin operators fail typed instead of fabricating empty tags, empty property lists, ignored operands, default visible state, leaked end-of-stream visibility, unbalanced compatibility-section no-ops, or malformed native vector replay.
21. Closed active page path operand defaults: display-list capture, immediate page rendering, and SVG/PS vector-output classification now share `path_operand_refusal` for path construction, paint, and clip operators, so malformed operands and continuation operators without current points fail typed instead of dropping construction input, painting/clipping with extra operands, using `Path` implicit moves, or emitting malformed native vector paths.
22. Closed active resource invocation operand defaults: display-list capture, immediate page rendering, and SVG/PS vector-output classification now share `resource_invocation_operand_refusal` for `Do` and `sh`, so missing, wrong-type, or extra resource operands fail typed or force the typed raster path instead of silently no-oping, using empty names, regionalizing malformed output, or ignoring trailing operands before XObject/Form/image/shading dispatch.
23. Closed generic Type 3 glyph metric defaults: retained descriptor compilation, display-list capture, immediate page dispatch, and SVG/PS vector-output classification now share `type3_glyph_metric_operand_refusal` for `d0` and `d1`, so wrong-count, wrong-type, or non-finite glyph displacement/bbox operands fail typed or unsupported instead of synthesizing zero widths, zero bounding boxes, or malformed native vector output.
24. Closed inline image sequence and parameter-pair defaults: retained display-list capture and immediate page dispatch now reject malformed `BI`/`ID`/data/`EI` ordering, including data without `ID`, `ID` without `BI`, dangling `BI`, pending `ID`, missing data bytes, and payloads without closing `EI`, while parser normalization, direct renderer helpers, and SVG/PS classification reject non-name, dangling, duplicate, or alias-duplicate inline-image parameter pairs before they can collapse into smaller valid dictionaries.
25. Closed the SVG/PS named-paint black fallback boundary for native vector replay: ordinary path fill/stroke/fill-stroke, text paint modes, Image XObject stencil masks, and inline stencil masks now preflight named color-space resources before replay. Resource-named `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` paint stays vector-safe only with exact finite components; unresolved, unsupported, invalid, or incomplete named paint now routes to the whole-page typed raster/refusal path instead of reaching the generic named-color black conversion in SVG/PS sinks.
26. Closed device-family component defaulting in the no-dictionary converter: `ColorSpaceHandler::try_from_components` now refuses missing, overlong, and non-finite `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` component vectors before image SMask `/Matte`, luminosity SMask `/BC`, or SVG/PS named-paint preflight can use padded, truncated, or defaulted device colors.

2026-08-24 render-facing device color-state continuation: `ColorSpaceHandler::strict_to_render_color` now protects active page paint, Type 3 inherited vector color replay, retained display-list simple-color capture, and SVG/PostScript vector sinks below PDF operand validation. Malformed internal `/DeviceGray`, `/DeviceRGB`, or `/DeviceCMYK` graphics-state vectors now fail typed or decline retained metadata instead of padding missing components, truncating extras, or emitting fallback pixels.

2026-08-24 SVG radial Extend continuation: SVG vector output now accepts finite concentric radial shadings with `r1 > r0` and explicit non-default `/Extend` pairs, then emits native `<radialGradient>` output clipped to the outer circle, annulus, or page-minus-inner-circle subset as applicable. Nonconcentric, reversed-radius, non-finite, or otherwise unsupported SVG radial non-default `/Extend` geometry remains conservative; PostScript continues to preserve radial `/Extend` through native `shfill`.

2026-08-24 direct shading transform/decode continuation: `render/shading.rs` now refuses singular or non-finite direct Type 1-3 paint transforms and singular Type 1 shading matrices instead of substituting identity inverse transforms below active shading validation. Mesh vertex and patch readers now use exact validated `/Decode` indices directly, so lower stream readers no longer retain per-component decode defaults after `MeshDecode::from_dict` has proven cardinality.

2026-08-24/26 ExtGState helper-local continuation: `content/state.rs::GraphicsState::try_apply_ext_g_state` now validates ExtGState render metadata before mutation, while well-formed `/SA`, `/AIS`, and `/TK` values are retained as active graphics-state fields instead of being rejected as metadata-only unsupported flags. Active page rendering, packed descriptor replay, display-list capture, SVG/PostScript replay, vector-output classification, conservative Form subset checks, and text collector state tracking use that strict boundary before the legacy mutator can clamp malformed values, filter malformed arrays, or partially apply bad font/blend/dash state. Visible active/retained/packed paint that observes unsupported stroke-adjustment, alpha-source, or disabled-text-knockout semantics now fails typed at the paint boundary.

2026-08-24 JPEG/JPX asymmetric reduction exactness continuation: `render/image_decode_planning.rs` now requires native DCT/JPEG and JPX reduced-resolution tiers to cover both requested target axes. If no power-of-two tier can satisfy the requested width and height together, the plan keeps full-resolution identity and exact rendering refuses the missing reduced-resolution capability instead of caching or painting an undersampled native reduction.

2026-08-24 active clip-DAG identity continuation: `render/page_renderer.rs` now scopes active page, SMask `/G`, and transparency/Form group `ClipDag` instances with document revision, active render-contract fingerprint, and tile-local viewport identity. Applying an explicit contract fingerprint refreshes the live DAG scope before rendering, so future clip nodes carry non-default revision/contract/tile salts.

2026-08-25 transparency group color-space policy continuation, amended 2026-08-26: `render/page_renderer.rs` now validates Form transparency-group and SMask `/G` `/Group /CS` values before group rendering. Direct, indirect, and resource-resolved DeviceRGB/sRGB group spaces are accepted through the active RGB compositor; DeviceGray and DeviceCMYK active Form groups must now prove the opaque-normal inert Form-stream subset before rendering, and alpha/backdrop-observable non-RGB groups fail typed before offscreen allocation instead of silently using RGB group-space compositing. Opaque, normal-blend active Form groups with well-formed CalGray, CalRGB, Lab, Indexed, or ICCBased `/CS` values render through an explicit non-device-inert policy; `/S /Alpha` SMask `/G` groups with no `/BC` can also use the non-device-inert policy because only rendered alpha is consumed. Luminosity SMask `/BC` now resolves well-formed CalGray, CalRGB, Lab, Indexed, ICCBased, Separation, and DeviceN group color spaces through the named-color/CMM resolver, and the soft-mask cache key includes the resolved non-device color-space fingerprint so calibrated/profiled/indexed/tint-transform backdrops cannot alias. Malformed arrays, missing resources, unresolved references, malformed Indexed palettes, malformed tint transforms, malformed ICCBased profile references or `/N` metadata, alpha SMask non-device groups with `/BC`, alpha-bearing non-device Forms, and richer non-device group spaces fail typed before offscreen allocation or SMask group execution.

27. Closed image decode cache identity gaps: inline keys now hash the complete payload and active render context, and image decode planner keys include explicit component-selection, decoded target-format, and backend fragments for JPX/future component-subset and backend-specific decode plans.

Image decode execution-control continuation: per-image capability reports now expose cancellation and memory-budget control status, and the document report summarizes native cancellation, renderer-boundary cancellation, and renderer-boundary memory-budget counts. Current codec adapters report cancellation and memory budgeting as renderer-boundary controls rather than native codec hooks.

JPX metadata-inspection continuation: `images/jpx.rs::inspect_metadata` now exposes JPX dimensions, original bit depth, color-space family, color/stored channel counts, and alpha state without decoding pixels. `ImageDecodeCapabilityReport` serializes `metadata_inspection`, and document-level image decode capability JSON includes `native_metadata_inspection_count`; JPX metadata inspection is native while ROI/region, codestream tile, component-subset, and progressive pixel output remain explicit unavailable capabilities.

Packed replay pre-resolved resource continuation: `RenderStatePlanAdapter` now enforces the retained-plan resource invariant during execution. Packed Image XObject, Form XObject, named shading, ExtGState, font, non-device named color-space, named pattern, and `/OC` marked-content property descriptors that lack pre-resolved handles/objects/dictionaries record typed fatal replay refusals instead of falling back to runtime resource-name lookup through `handle_do`, `handle_sh`, page resource maps, or later font-resource resolution. Built-in DeviceGray/DeviceRGB/DeviceCMYK/Pattern color-space names remain valid without resource objects. `GraphicsStateDescriptor::to_content_operation` remains limited to descriptor round-trip diagnostics and is not part of active packed replay.

28. Added guarded JPX target-resolution downscale decode: safe JPX Image XObject and inline-image downscale plans now use `hayro-jpeg2000` target-resolution output after validating the original codestream dimensions against the PDF dictionary; JPX ROI/region and progressive output remain incomplete.
29. Replaced direct CPU fill and stroke path-mask cache wholesale clearing with byte-accounted LRU eviction, including hit-recency refresh and replacement byte-charge correction under entry or byte pressure.
30. Made packed Image XObject, Form XObject, and named shading compilation fail closed with typed missing-resource refusals when `compile_with_resources` has a resource table but the retained resource name is absent, preventing descriptor execution from falling back to runtime name lookup.
31. Made resource-aware packed graphics-state compilation fail closed with typed missing-resource refusals for `/ExtGState`, `/Font`, named non-device color spaces, pattern color resources, and marked-content `/Properties`, including retained-text `/Tf` descriptors.
32. Made Type 3 CharProc full replay fail closed when the nested retained CharProc plan is unavailable or contains a packed compile refusal, including compatibility mode.
33. Made packed replay itself fail closed for manually or stale-compiled Image/Form/shading/ExtGState/font/named-color-space/named-pattern/OC-property descriptors without pre-resolved resource payloads, so the replay layer no longer has a late high-level resource lookup escape hatch.
34. Made Type 3 parsed CharProc and path-geometry cache misses retain the exact source-collection failure reason for unresolved `/CharProcs`, decode failures, byte/op caps, parse failures, unsupported path extraction, or empty geometry instead of caching a generic unavailable entry.
35. Made top-level transparent-page-group decision cache keys include the active render-contract fingerprint in addition to page and document revision while preserving page-prefixed invalidation.

## Post-status matrix

`YES*` means an active retained path exists but still has the exact fallback/structural limitation named in the row. All rows retain the original stable ID from `final-universal-preimplementation-audit.md`.

| ID | Pre-status | Post-status | Files changed / active call path | Native retained replay | Immediate fallback | Tests added / verification | Bindings | Resource bounds | Remaining limitations |
|---|---|---|---|---|---|---|---|---|---|
| RV-01 | MISSING | PARTIAL_ADVANCED | `render/document_view.rs`, `engine.rs`, `sdk.rs`, `crates/cli/src/main.rs`, `crates/server/src/routes/document_views.rs`, `crates/server/src/routes/render_contract.rs`, C/Python/WASM/.NET/Java source shims; `ContentEngine::{canonical_document,render_view,edit_view,semantic_view,validation_view,document_views_report}`; `RenderDocumentView` contract/default/caller-buffer/backend-plan/document-arena wrappers | partial | n/a | focused lazy-view/report/contract-wrapper/backend-plan/document-arena tests plus CLI/server report tests and binding source compile gates where listed | Rust SDK, CLI, server, C, Python, WASM, .NET, Java source | borrowed lazy view handles; SHA-256-derived revision/object/page identities; materialization counters stay zero until a view call uses a page; per-page backend-plan arena reporting plus explicit owned CPU document-plan arena materialization through the render view | Canonical identity and lazy Render/Edit/Semantic/Validation view boundaries are source-active and observable through `document_views_report`, CLI `document-views-report`, and server `POST /api/v1/document-views/report`; render-contract and per-page backend-plan entry points can be driven through `RenderDocumentView`; explicit `RenderDocumentView::backend_document_plan_arena` owns CPU `RenderPlan` values for every page and `sdk::backend_document_plan_arena_report_json` aggregates document-level packed-plan counts; GPU/printer/debug backend arenas and external runtime validation remain future backend/platform work |
| RV-02 | MISSING | PARTIAL_ADVANCED | `render/contract.rs`, `engine.rs`, `sdk.rs`; `ContentEngine::{default_render_contract,render_page_with_contract,render_page_into_buffer}`; `RenderContractFieldEffect` telemetry | active | typed refusal for unsupported contract/policy combinations | contract validation, cache-key, CLI/server/binding builder, render-contract telemetry, Research execution-mode, ScalarReference backend, ResearchHybrid backend, and field-effect registry tests where listed | Rust/CLI/server/C/Python/WASM/.NET/Java source | schema-v1 contract fingerprint, revision identity, page box, transform, background, policy, execution mode, backend selection, per-field effect registry, and resource-budget fields seed active render/cache paths | Render-contract source and full cache identity are active; Research execution mode is identity-bearing on the deterministic CPU path, ScalarReference is an implemented active CPU scalar compositor path, and ResearchHybrid is now an active guarded CPU hybrid-dispatch path with separate identity; every serialized schema-v1 contract field is covered by the source-owned field-effect registry and render-contract telemetry; remaining unsupported semantic policies still fail typed; external runtime/backend matrix validation remains future platform work |
| RV-03 | PARTIAL | PARTIAL | `display_list.rs:RenderDevice/CpuRenderDevice`; `buffer.rs`; `render-simd` | partial | list fallback | historical check/clippy/test pass | Rust only | existing buffer/cache guards | No backend plan, caller surface API, or GPU boundary |
| RV-04 | PARTIAL | PARTIAL_ADVANCED | `ContentOperation`, `ParsedPageProgramArena`, `BackendPlanArenaReport`, `BackendDocumentPlanArenaReport`, `RenderDocumentView::{page_program,backend_plan_arena_report,backend_document_plan_arena}`, SDK/CLI/server/C/Python/WASM/.NET/Java report shims | partial | n/a | parsed-program, render-view backend-plan, document-arena, CLI report, server report, and binding source tests pass where listed | Rust SDK, CLI, server, C, Python, WASM, .NET, Java source | source operation spans, render-contract fingerprint, hot-op/descriptor/batch arena counts, compile-refusal counts, document-level owned CPU plan counts | Compact source-linked page-program arena, per-page backend-plan arena reporting, and explicit owned CPU document plan arenas are active; parser-level `Vec<ContentOperation>` remains a compatibility source boundary, and GPU/printer/debug page-program adoption remains future backend/platform work |
| RV-05 | PARTIAL | PARTIAL | `render/display_list.rs`; `PageRenderer::get_or_build_display_list_with_cache` | YES* | `!is_fully_supported()` | historical full-suite evidence | Rust/CLI partial | approximate bytes only | `Vec<DisplayOp>` remains clone-heavy/unpacked |
| RV-06 | PARTIAL | PARTIAL_ADVANCED | `DisplayOp` enum typed path/state/high-level payloads, `RenderPlan`, `BackendPlanArenaReport` | partial | n/a | typed retained state/pattern/text/inline/resource-name tests plus backend-plan arena, CLI report, server report, packed viewport/accounting tile-execution, and resource-arena report tests pass | Rust SDK, CLI, server, C/Python/WASM/.NET/Java source | approximate bytes plus packed viewport identity, cached source byte count, packed cold diagnostics, hot-op arena counts, descriptor-kind counts, resource-arena counts, batch counts, and compile-refusal counts | Retained high-level display payloads no longer store raw `ContentOperation`, packed vector tile execution and packed-plan byte accounting no longer dereference the retained source list for viewport dimensions or source-size fields, and per-page hot/cold/resource backend-plan reporting is public through SDK/CLI/server/bindings, but parser-level page programs, transparency semantic source analysis, and complete backend-specialized arenas remain partial |
| RV-07 | MISSING | PARTIAL_ADVANCED | `render/plan.rs`, `render/document_view.rs`, `engine.rs`; `RenderPlan::{compile,compile_with_resources,execute_full,execute_vector_tile_with_scratch}`; `RenderDocumentView::backend_document_plan_arena` | partial | typed descriptor dispatcher plus vector-tile path decline when native replay is required | render-plan compile/replay, descriptor/vector batch splitting, spatial culling, query scratch, packed descriptor tests, document-arena ownership tests, and packed unresolved-resource replay tests where listed | Rust internal/Rust SDK JSON | `RenderBatch` metadata splits contiguous descriptor-backed and pure-vector hot-op runs plus bounded spatial index; document-level arena owns per-page CPU plans on explicit request | RenderPlan source, descriptor/vector batches, packed display lists, spatial culling, typed dispatcher execution, and explicit owned CPU document plan arenas exist; GPU/printer/debug-device backends remain future backend/platform work |
| RV-08 | PARTIAL | PARTIAL_ADVANCED | `PageRenderer::render_packed_vector_plan`; native `RenderState` replay for supported packed descriptors | partial | pre-resolved descriptor payloads or typed refusal | retained state, pattern-path, text, marked-content, inline-image, Image XObject/Form/shading ops carry typed payloads or typed names; resource-aware Image XObject descriptors now also carry first-level named image color-space payloads; retained replay tests pass; unsupported retained display-list replay now returns typed `UnsupportedFeature` in compatibility and high-quality/exact paths instead of using canonical raw dispatch; missing resource-aware state resources (ExtGState/font/color-space/pattern/Properties), malformed non-text graphics-state operands, `Q` restore underflow, malformed text operands and text-object sequences, malformed marked-content/compatibility-section operands and sequences, malformed path operands or clip sequences, malformed resource invocation operands, malformed Type 3 `d0`/`d1` metric operands, packed compile refusals, unresolved packed Image/Form/shading/ExtGState/font/named-color-space/named-pattern/OC-property descriptor payloads, and unsupported retained state descriptors fail typed during display-list compilation and packed descriptor replay; packed-plan dispatch stops after a typed refusal before later operations can paint partial output; valid empty or move-only clipping paths install an explicit empty clip; the same operand family, stream/Form-local `Q` restore depth, clip-sequence state, and text-object sequence state are preflighted before SVG/PS vector sink replay | Rust/CLI partial | document cache | Full backend-native high-level payload coverage remains incomplete |
| RV-09 | PARTIAL | PARTIAL_ADVANCED | packed display-list builder, DrawState/path arenas, resource-aware descriptors, and resource-arena report counters | active | n/a | packed plan state/path interning, resource descriptor, Image XObject color-space payload accounting, and resource-arena report tests pass where listed | Rust internal/backend-plan report source | binary DrawState/path fingerprints with exact collision comparison; resolved resource handles for covered image/Form/shading/ExtGState/font/color-space/pattern/Properties classes, pre-resolved first-level Image XObject color-space payloads, plus `resource_arena_entries`/`total_resource_arena_entries` report counters | Packed vector paint states and paths are interned into reusable arenas and repeated identical paints share stable `state_id`/payload IDs; covered resource descriptors pre-resolve when compilation has page/form resources and fail typed when required handles are unavailable; resource-specific font/image/Form/pattern/shading/group/color-space counts, including named Image XObject color-space payloads, plus a descriptor-level appearance slot are source-visible through backend-plan reports, while complete backend-native payload coverage remains partial |
| RV-10 | PARTIAL | PARTIAL_ADVANCED | rectangle/mask/bounds specialization plus conservative packed transform/state folding | partial | general path fallback | path tests and packed-plan optimizer tests pass | Rust/backend-plan arena report source | bounded masks plus `PackedPlanOptimizationReport` counters | Identity `cm` no-op transforms, adjacent `cm` matrix concatenation composition, no-paint non-adjacent `cm` composition across independent state setters, adjacent and separated inverse `cm` collapse, adjacent duplicate absolute state setters, adjacent same-slot absolute setter overwrites, and non-adjacent same-slot overwrites inside no-paint runs for independent scalar/text/color-space state slots are reported through per-page/document backend-plan arena fields; geometry-dependent transform folding, dependent color-space/color setter folding, ExtGState side-effect folding, and backend-specific optimizer passes remain incomplete |
| RV-11 | MISSING | PARTIAL_ADVANCED | `render/invalidation.rs`, `render/transaction_invalidation.rs`, `RenderDocumentCache`, `editing_transactions.rs`, `advanced_editing.rs`, `document_subsystems.rs`, `secure_mutation.rs`, SDK/binding/server apply-with-render-invalidation surfaces | partial | page/content/resource dependency graph plus supported source-editing object/stream ID bridge, geometric/semantic TextReflow scene-mode routing, compact TextReflow option forwarding, advanced Link/Ink annotation and AcroForm widget structured render write-set reporting, explicit dirty-tile write sets, preserved indirect resource refs, automatic bounded retained resource-op tile edges, source-scoped artifact pruning, binding-safe render-invalidation plan reports with source cache markers, and Rust cache-owner plan application | transaction/cache identity, source-ID mapping, dirty-region conversion, source-tile, page-artifact exact-tile invalidation, bounded Form XObject tile-edge extraction, text font-reference tile-edge extraction, color-space-backed text, Image XObject, shading, pattern, ExtGState SMask group, and active marked-content `/Properties` tile-edge extraction including referenced image mask/SMask stream, retained inline-image, nested direct-array color-space refs, and optional-content property refs, ExtGState-backed inline-image tile-edge extraction, typed pattern-state tile-edge extraction, exact raster-tile, invalid exact-tile guard, source-scoped artifact pruning, transaction source-marker registration, render-invalidation plan source-marker registration, TextReflow mode-routing and request-forwarding adapter tests, Link/Ink annotation write-set tests, server route, binding source compile, plan application, structured nested write-set ref mapping, broad write-set metadata narrowing, and stale-tile tests pass | Rust SDK/cache owner, C, Python, WASM, .NET, Java, server source | bounded ref traversal; PDF refs, structured `[object, generation]`/`{object_number, generation}`/`{ref}` refs, target-object/generation pairs in broad transaction write-set envelopes, and `object-*`/`stream-*` source IDs dedupe through canonical object identities; ordinary metadata strings in broad `writeSet`/`affectedObjects`/`dirtyObjects` envelopes are ignored unless they parse as refs, while explicit renderer ref fields remain strict and unknown refs conservatively reset; `PageResources` preserves font/color-space/ExtGState/Properties indirect refs and Image XObject stream dictionaries; scene `geometric_block` and `semantic_document` text transactions now route to canonical TextReflow apply/preview paths, forward explicit region/flow/layout/review fields, and convert source refs, affected pages, dirty regions, typed refusals, and inverse metadata into the EditingTransactions invalidation envelope; advanced-editing Link annotation moves and Ink annotation appearance regeneration emit structured render write-set refs, changed/created object refs, affected pages, and page-space dirty regions; DocumentSubsystems AcroForm actions and secure-mutation incremental form fills expose structured field/widget/AP render invalidation reports with affected pages and dirty widget rectangles where live widget appearances change; explicit affected tiles, source-to-tile graph edges, automatic tile edges for resolved font-backed text, XObject, shading, pattern-path, pattern-painted text, color-space-backed paint/text/Image XObject/shading/pattern dictionaries/ExtGState SMask group dictionaries, referenced image `/Mask`/`/SMask` stream named color spaces, retained inline-image named color spaces, nested named resources inside direct color-space arrays, active `/OC` marked-content Properties resources, and ExtGState-backed paint/text/image resources, caller-supplied viewport/grid conversion of dirty rectangles, returned invalidation-plan metadata, `source_cache_markers` for decoded/scaled image, SMask, mesh, Form, tiling, and annotation-appearance artifacts, and `RenderInvalidationCachePlan` plan-body/envelope parsing with object-number marker derivation plus string/structured nested ref normalization let Rust cache-owning callers evict exact raster tiles without page artifact pruning; affected pages in explicit dirty-tile plans prune retained/page-scoped artifacts without expanding raster invalidation to every recorded page tile only when supplied exact tiles are valid, while page-0 or zero-sized tile entries are ignored and uncovered affected pages expand recorded page tiles | Broader arbitrary shared-resource transitive write-set matrices, safe narrowing of conservative page-level resource dependencies, external cache-handle runtime matrix validation, and remaining shared-resource classification remain incomplete |
| RV-12 | PARTIAL | PARTIAL_ADVANCED | `RenderBounds`, `RenderSpatialIndex`, `RenderPlan::execute_vector_tile_with_scratch` | YES* | unknown bounds execute | spatial order, grid-bucket culling, BVH parity, linear/grid/BVH reusable query output, and vector-tile scratch replay tests pass | internal | tile range validation | Deterministic grid buckets and a bounded BVH hierarchy are built for larger bounded plans, exact bounds filtering preserves paint order, callers can reuse the result vector through `query_into` without a separate candidate vector on linear, grid, or BVH paths, stale caller scratch is cleared before warm replay, and vector-tile replay can use caller-owned selection scratch; broader cross-thread/external runtime allocation profiling remains future validation |
| RV-13 | PARTIAL | PARTIAL_ADVANCED | `crates/render-simd/src/lib.rs`, `buffer.rs`, `engine.rs` | partial | exact scalar/wide fallback, contract-selectable ScalarReference guard, plus guarded wasm `simd128` lanes | host SIMD tests, native SSE2 alpha-mask, RGBA copy/opaque-alpha, BGRA conversion, native SSSE3 RGB/BGR/RGB-image-span conversion, native SSSE3 grayscale conversion, premultiply public-dispatch, native and wasm opaque-background flatten, and public-kernel scalar-equivalence tests pass; wasm32+`simd128` crate check, Node-executed `.work` wasm separable-helper smoke, engine alpha-mask row test, soft-mask non-opaque group-alpha row test, soft-mask mixed-destination row test, mixed-source partial-group source-over row test, mixed-destination source-over row test, separable blend test, extended separable row test, Difference/Exclusion row and byte-contract tests, RGB image-row test, Gray8/grayscale-expansion/alpha-mode/reverse-byte-order row-encoder tests, flatten-background tests, and `contract_scalar_reference_backend_uses_scalar_compositor` pass; focused refresh covered `rgba_to_premultiplied_gray_bgra8` public/scalar parity, soft-mask group-alpha public dispatcher parity, reverse 4-byte-word routing, runtime capability disclosure, engine `contract_row_encoder` routing, explicit clip/soft-mask alpha-product public-kernel plus engine fused-window coverage, and direct paint fusion row-product coverage | Rust core and WASM target source | slice bounds/runtime dispatch plus ScalarReference cache identity | ScalarReference contracts force scalar row helpers and separate image/SMask cache identity; AVX2/SSE2/NEON/wasm subset covers opaque fill, normal opaque-destination blend/source-over rows, mixed-source partial-group-alpha opaque-destination source-over rows, CPU f32x4 mixed-destination source-over rows, native x86/x86_64 SSE2 alpha/glyph-mask opaque-destination rows, alpha/glyph-mask mixed-destination rows, x86_64 AVX2 plus x86/x86_64 SSE2 clip/soft-mask alpha-product rows, direct image/glyph mask and solid/RGBA SMask/clip row-product routing through the shared alpha-product helper, native x86/x86_64 SSE2 and wasm32+`simd128` opaque-background flatten rows, native x86/x86_64 SSE2 RGBA copy/opaque-alpha, BGRA conversion, native x86/x86_64 SSSE3 RGB/BGR channel, RGB8-to-opaque-RGBA image-span rows, Gray8 conversion, grayscale RGB/RGBA/BGRA expansion, and premultiplied grayscale alpha-bearing rows, plus premultiplied RGBA caller-surface rows, wasm premultiplied BGRA rows that reuse vector premultiply lanes plus a channel shuffle instead of scalar extraction, wasm RGB8-to-opaque-RGBA rows that use SIMD expansion shuffles instead of scalar extraction, wasm shared lane-luma grayscale expansion rows that use SIMD shuffles instead of scalar extraction, soft-mask rows including non-opaque group-alpha effective-alpha rows plus CPU f32x4 mixed-destination rows, separable opaque-destination rows with true wasm lane math for Multiply, Screen, Overlay, Darken, Lighten, ColorDodge, ColorBurn, HardLight, SoftLight, Difference, and Exclusion plus native SSE2 lane coverage for the same full separable set, unpremultiply RGBA library rows, reverse 4-byte-word caller-surface rows, RGB/BGR/BGRA contract channel rows, RGB8-to-opaque-RGBA image rows, Gray8 contract conversion, and straight/opaque/premultiplied grayscale RGB/RGBA/BGRA expansion rows including explicit `rgba_to_premultiplied_gray_bgra8`; native x86/x86_64 separable rows and native/wasm flatten rows now have SIMD helpers before portable/scalar fallback on unsupported hosts, and AVX-512/full operation coverage remains incomplete |
| RV-14 | PARTIAL | PARTIAL_ADVANCED | `path.rs` edge buckets/scanline/stroke spans | yes for guarded paths | general bounded painter for unsupported geometry | cubic monotonic decomposition, finite transformed-point flattening tests, fractional coverage, active-edge invariant, active-edge bucket/subsample parity, convex geometry parity/rejection tests, axis-aligned solid stroke-span/polyline tests, hairline/polyline tests, and rectangular stroke-span tests pass | internal | edge-link cap 2,000,000; path guards; finite derivative-root and transformed-point guards; sequential-row active-edge guard; finite closed single-subpath convexity guard; integer device-boundary/monotonic-collinear stroke-span guard; hairline semantic guard | Derivative-root monotonic cubic decomposition, finite transformed-point filtering before scanline/stroke emission, row-local active-edge preparation, precomputed edge slope/direction descriptors, scanline fill/binary-clip/alpha-mask/subsampled-compositor active-edge reuse, guarded direct sampled spans for finite closed single-subpath convex fills, exact integer horizontal/vertical solid butt/projecting-square single-segment and monotonic-collinear polyline stroke spans, sampled-coverage direct spans for simple solid butt single-segment and monotonic-collinear polyline hairlines, guarded opaque rectangular miter stroke spans, and opposite-winding closed-stroke contours are active; broader complex/self-intersecting geometry/AET closure outside these guarded scanline paths and complete stroke-geometry closure remain incomplete |
| RV-15 | PARTIAL | PARTIAL_ADVANCED | `ClipMask`, `ClipRunCache`, `render/clip_dag.rs`, `render/display_list.rs`, `render/page_renderer.rs` | yes | exact dense/general paths plus persistent node variants | clip-DAG focused tests, composite window short-circuit test, saved-node restore materialization test, display-list active-node save/restore test, RenderState active-node q/Q test, transformed path replay-cache and document-cache tests, path clip-node aggregate budget test, render-contract telemetry test, clip-focused filter, CLI compile check, and engine clippy pass | internal plus Rust/CLI telemetry source | mask dimensions/caches plus node memory charges, path clip-node byte accounting, and active DAG intern/pruning counters | Full/Empty/Rectangle/SparseSpans/RleMask/DenseMask/Composite nodes exist with metadata, structural window materialization short-circuits empty/all-visible composite child windows, page/content/Form/packed-plan restore paths rebuild buffer clips from DAG state without populating node materialization caches, display-list and active RenderState replay save/restore active `Arc<ClipNode>` handles instead of reclassifying materialized masks at save time, explicit render-contract identity refresh re-interns the current clip into the new scoped DAG, repeated transformed path clip installs reuse bounded render-contract-scoped DAG-node cache entries carried through `RenderDocumentCache`, active DAG intern/pruning counters are captured on render-state return, path clip-node plus active DAG counters are exposed through `RenderContractTelemetryReport` and CLI `render-corpus`, and hot alpha-mask/cached-RGBA/solid-fill/full-buffer-composite/offset-composite row paint branches borrow active `ClipMask` state instead of cloning concrete masks; `PixelBuffer` still installs and consumes concrete masks at raster-buffer boundaries and full clip/mask fusion remains incomplete |
| RV-16 | PARTIAL | PARTIAL_ADVANCED | group surfaces and compositor | partial | scalar complex composite with bounded reusable backdrop/group/knockout surfaces | transparency and knockout focused tests pass, including reusable copy/backdrop extraction | internal | scheduler/offscreen limits plus offscreen-pool recycling | Non-isolated group backdrop snapshots, copied non-isolated group buffers, and knockout backdrop seeds now use `copy_rect_into_buffer` with pooled surfaces and recycle extracted backdrops after compositing; full non-RGB group colour-space compositing, full backdrop semantics, complete PDF knockout semantics, and print group semantics remain incomplete |
| RV-17 | PARTIAL | PARTIAL_ADVANCED | SMask cache/apply paths | partial | scalar/general mask path plus typed malformed-mask and group-space refusal | SMask identity/reuse tests pass, including malformed missing-`/G`, malformed/unsupported subtype, malformed backdrop, CalGray/CalRGB/Lab/Indexed/ICCBased alpha/no-backdrop group `/CS`, luminosity CalRGB/Indexed/Separation/DeviceN `/BC` group `/CS`, unsupported group `/CS`, and unsupported transfer-function typed refusals | internal | surface/decode limits | SMask group cache identity includes source seed, revision, render contract, tile-local viewport, device transform, structural clip-state fingerprint, active color-management policy, active overprint policy, mode/profile/policy, optional-content state, resource budget, `/TR` transfer-function identity, and resolved non-device group color-space fingerprint when `/BC` uses a calibrated/profiled/indexed/tint-transform group space; malformed soft-mask dictionaries/references, present malformed `/S`, `/BC`, and `/Group /CS` inputs, unsupported `/TR` transfer functions, malformed Indexed palettes, malformed tint transforms, malformed ICCBased profile references or `/N` metadata, and alpha SMask non-device groups with `/BC` fail typed instead of rendering unmasked content, silently defaulting subtype/backdrop/group space, or silently using identity; `/S /Luminosity` SMask `/BC` resolves well-formed CalGray, CalRGB, Lab, Indexed, ICCBased, Separation, and DeviceN group spaces through the existing named-color/CMM resolver; `/S /Alpha` SMask `/G` groups with no `/BC` can use non-device-inert group `/CS` because only alpha is consumed; full clip/mask fusion and broader non-device group-space semantics remain incomplete |
| RV-18 | PARTIAL | PARTIAL_ADVANCED | `shading.rs`, `function.rs`, `page_renderer.rs:handle_sh`, `validate_shading_dictionary_for_paint` | partial | canonical shading paths plus typed refusal for missing/malformed named shading, active required-field failures, unsupported/malformed function output, unsupported/malformed shading color spaces, unavailable mesh streams, unsupported shading types, and malformed shading patterns | shading tests pass, including render-level typed refusal for missing named shading, malformed named shading resources, malformed active shading dictionaries/functions/ColorSpaces, non-exact or non-finite device function output, and malformed shading-pattern dictionaries | CLI indirect | function/decode/color-space limits | Full exact/contract verification remains incomplete |
| RV-19 | PARTIAL | PARTIAL_ADVANCED | pattern paint handlers and retained pattern path compilation | partial | typed refusal for missing/malformed active patterns, missing active pattern names, recursive/over-cap tiling patterns, plus SVG/PS simple finite-matrix shading-pattern fill/stroke path and glyph-outline text replay and simple colored/uncolored tiling-pattern path/glyph-outline text replay, and tiling-pattern painted stencil-mask replay | pattern tests pass including retained missing-resource refusal, render-level missing-resource, missing-name, unsupported PatternType, non-stream PatternType 1, malformed tiling-geometry, recursive typed refusal, cell-limit guard coverage, simple shading-pattern regional output coverage, and SVG/PS tiling-pattern regional output/fallback plus stencil-mask coverage | Rust/CLI indirect | recursion stack, visible-cell cap, finite pattern matrix/CTM guard | Missing or malformed active patterns, `/Pattern` paint without an active pattern name, recursive tiling patterns, and over-cap tiling patterns fail typed instead of silently dropping paint or approximating; simple finite-matrix `/PatternType 2` shading-pattern path fills/strokes and glyph-outline text fills/strokes are native in SVG/PS; simple `/PatternType 1` `/PaintType` 1 and color-inheriting `/PaintType` 2 path fills/strokes, glyph-outline text fills/strokes, and inline/Image XObject stencil masks are native in SVG/PS when the tile stream is vector-safe and bounded; unsupported/advanced pattern subplans, unsupported advanced pattern-space transforms, patterned text outside the supported shading/tiling glyph-outline subset, stencil semantics outside the supported shading/tiling subsets, and complete pattern backend planning remain incomplete |
| RV-20 | PARTIAL | PARTIAL_ADVANCED | Type 3 charproc/cache paths plus generic metric descriptors | partial | typed refusal for unresolved, recursive, malformed path-only glyph programs, or malformed `d0`/`d1` metric descriptors | Type 3 tests pass including missing-name refusal, recursive CharProc refusal, exact parsed CharProc and geometry collection failure-reason caching, path-collector malformed operand/current-point refusal, malformed clipping CharProc refusal, generic metric operand validator/display-list/render/vector refusal coverage, and parsed-program cache-pressure/admission coverage | Rust indirect | parsed CharProc/geometry LRU entry caps plus mask/rendered glyph cache caps | Recursive CharProcs fail typed at the nesting-depth guard; parsed CharProc and geometry cache misses keep unresolved/decode/byte-cap/parse/op-cap/unsupported-path/empty-geometry reasons; path-only geometry extraction refuses malformed operands instead of defaulting line state, transforms, dash state, colors, or missing current points; generic metric descriptors refuse malformed operands instead of defaulting zero widths/bboxes; no immutable Type 3 sublist state machine/exact closure |
| RV-21 | PARTIAL | PARTIAL_ADVANCED | fonts/glyph caches plus `render/font_substitution_report.rs` | partial | FB-07 compatibility substitution; exact generic-bundled refusal | font tests pass; bounded substitution log serializes with requested PDF font, embedded state, encoding, Standard14/document-provided/caller-registered/deterministic-system/bundled resolution source, selected replacement, required glyph coverage, visual-risk, extraction/editing impact, font-policy identity, and risk fields; source single-flight coalesces equivalent renderer-originated report events while preserving first observed page/coverage; caller-registered font bytes resolve before bundled fallback, report `user_registered`, and salt font-policy/cache identity with a byte-sensitive provider fingerprint; C ABI/header, Python, WASM/TypeScript, .NET, Java, CLI render, server render-contract routes, and server progressive start expose registered-font input paths; descriptor Symbolic/Nonsymbolic, Italic, ForceBold, and FontWeight hints feed deterministic replacement selection so unknown non-Standard14 symbolic descriptor fonts route to the symbolic coverage face and report `CoverageOnly`; explicit `HighQualityExact` contract rendering accepts valid registered/deterministic-system replacements and refuses generic bundled substitution when no valid embedded/document/system/registered replacement exists; font cache keys include render-contract identity, resource budget, and registered-provider identity; reused device glyph masks can paint from byte-accounted atlas pages through grayscale rows or RGB LCD-style per-channel rows when subpixel text policy is active | Rust/API, CLI sidecar, and C/Python/WASM/.NET/Java/source server render API partial | selected byte caches plus bounded report events plus contract-scoped/provider-scoped font identities plus glyph atlas telemetry | External registered-font runtime validation deferred |
| RV-22 | PARTIAL | PARTIAL_ADVANCED | image decode/sampler/cache plus `ImageDecodeCapabilityReport`, `ProgressiveImageDecodeSession`, capability JSON, and lifecycle JSON | partial | canonical image sampler plus exact unsupported region/reduction and malformed decode/mask refusal | image/cache identity, capability-report, JPX metadata-inspection report, exact source-region/reduction refusal, strict inline sequence refusal, active image decode-failure refusal, invalid inline dimension refusal, inline BitsPerComponent refusal, malformed inline DecodeParms refusal, missing inline ColorSpace refusal, malformed image Decode refusal, malformed image color-space refusal, unresolved tint-space image refusal, decoded-length mismatch refusal, JPX alpha-composition length refusal, ImageMask one-channel decode regression, malformed image-mask Decode refusal, image-mask stencil length refusal, image SMask malformed refusal, image SMask alpha-channel/length refusal, pre-alpha SMask-composition refusal, explicit-mask composition length refusal, malformed image SMask Matte refusal, malformed color-key mask refusal, dimension-mismatched mask refusal, image-decode capability CLI/server tests, progressive-image lifecycle, C ABI lifecycle, server route, CCITT source-window unit tests, CCITT planner window tests, raw source-window planner/decode/ImageMask tests, raw explicit-mask source-window test, and source-window CTM test pass | Rust lifecycle/capability; binding/CLI/server lifecycle and capability reports | scheduler/image cache | Raw cache lookup/admission uses planner/render-contract identity including active rendering intent and codec/source-region/reduction state, active decode paths consume capability reports, visible plans record source-region/reduction requirements, `HighQualityExact` refuses paths when required support is unavailable, guarded DCT/JPEG downscale uses native reduced-IDCT, JPX metadata inspection exposes dimensions/bit depth/color/channel/alpha without pixel decode, guarded JPX downscale uses target-resolution decode, guarded axis-aligned CCITT source-clipped XObjects use bounded grayscale source-window decode/cache output and inline images use bounded grayscale source-window decode output when no reduction or full-image postprocessing is required, guarded axis-aligned raw XObjects/inline images use bounded 1/2/4/8/16 bpc source-window output, raw 1 bpc ImageMasks use bounded stencil source-window output, and raw Image XObjects with color-key `/Mask` arrays or same-sized referenced 1 bpc stencil `/Mask` streams crop the main image and mask through the same source-window boundary when no `/SMask`, reduction, unsupported mask shape, or non-axis transform requires full-image materialization; malformed inline image sequence boundaries (`BI`/`ID`/data/`EI`), missing or invalid inline `/Width` or `/Height`, missing or unsupported non-mask inline `/BitsPerComponent` outside terminal JPX, malformed inline `DecodeParms`, missing or non-name non-mask inline `/ColorSpace` metadata, malformed image `/Decode` arrays, unsupported or malformed image color spaces, unresolved Separation/DeviceN tint-space transforms, decoded-length mismatches, JPX color/alpha channel length mismatches, malformed image-mask `/Decode` arrays, active inline image/image XObject decode failures, image mask-load failures, image-mask stencil buffer mismatches, malformed image SMask references/decode output or `/Matte` arrays, SMask alpha-buffer/channel conflicts, pre-alpha main-image conflicts, malformed explicit color-key mask arrays, dimension-mismatched explicit mask streams, and explicit-mask composition buffer mismatches fail typed instead of dropping paint, accepting out-of-order inline markers, treating invalid inline dimensions as no-ops, defaulting/clamping bpc metadata, defaulting ambiguous inline bytes to grayscale, treating unknown color spaces as raw RGB/gray, rendering unresolved tint spaces as grayscale, ignoring decode/stencil/matte mappings, padding/truncating decoded pixels, synthesizing opaque JPX alpha, sampling/padding mask data, dropping pre-existing alpha, or rendering unmasked content; the image-decode capability report is source-visible through Rust SDK, C ABI/header, Python, WASM, .NET, Java, CLI `image-decode-capability-report`, and server `POST /api/v1/image-decode/capability-report`, and the progressive-image lifecycle reports full-decode-required without retaining pixels for non-progressive codecs through Rust SDK, C ABI/header, Python, WASM, .NET, Java, CLI, and server source surfaces; broader JPEG/JPX ROI/region/tile/component/progressive pixel output, JBIG2/lossless ROI/reduction/progressive pixel output, unsupported raw explicit-mask windows, incompatible SMask windows, plus CCITT reduction/progressive/non-axis/unsupported-postprocessing-window output remains incomplete |
| RV-23 | PARTIAL | PARTIAL_ADVANCED | CMM/color/overprint | partial | FB-10 qcms backend plus typed unavailable-native refusal | CMM rendering-intent, color-management policy, overprint-preview, focused `native_littlecms`, `preserve_separations`, deterministic fallback, proof CMM validation, active named-color fail-closed tests, active shading ColorSpace refusal tests, and SMask matte/backdrop tests pass locally | limited | profile/decode bounds; bare `/Device*` aliases; strict calibrated named-paint validation; device-only family-name conversion when no calibrated dictionary exists; active shading output arity preflight | Portable qcms remains the default backend; PDF/contract rendering intent now reaches active ICCBased paint, image XObject decode, Separation/DeviceN alternate ICC resolution, SVG/PS named-color resolution, and ICC transform cache identity; bare `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` resource aliases resolve through named-color conversion only for exact finite component vectors; active named CalGray/CalRGB/Lab paint now requires well-formed parameters and exact finite components; Separation/DeviceN named colors require exact finite tint vectors; malformed calibrated Separation/DeviceN alternates propagate invalid; device alternate output and ICCBased scalar component vectors require exact component counts; unsupported alternates remain unsupported instead of defaulting to black; active page paint records typed renderer errors, invalid tint-space image conversion fails typed, image SMask `/Matte` plus luminosity SMask `/BC` family-name-only conversion refuses unsupported/calibrated names instead of using default parameters or black, and active shading dictionaries refuse unsupported/malformed `/ColorSpace` plus non-exact or non-finite device function output before paint; explicit `ColorManagementPolicy` now selects the active ICC backend policy for contract paint/image paths and raw image cache identity, with `DeterministicFallback` avoiding ICC backend transforms and unavailable `NativeLittleCms` failing typed; `Proof` contracts now require native CMM and have a bounded OutputIntent proof-transform hook when native CMM and a `DestOutputProfile` are available; `OverprintPolicy::Preview` now gates active PDF graphics-state DeviceCMYK overprint preview while `PreserveSeparations` remains unsupported; full native CMM runtime validation and print/separation output remain incomplete |
| RV-24 | PARTIAL | PARTIAL_ADVANCED | Form/XObject program cache and active `Do` dispatch | partial | typed refusal for missing/malformed active XObjects, Form recursion/depth failures, transparency-group malformed `/CS`, transparency-group offscreen allocation denials, and unsupported retained Form lists | Form/cache identity tests pass, including retained-plan coverage for supported ordinary and transparency-group Forms, retained display-list missing/no-subtype/unsupported-subtype XObject refusal coverage, packed-plan resolved-subtype mismatch refusal coverage, active XObject missing/non-stream/missing-subtype/unsupported-subtype typed refusal tests, recursive/deep Form XObject typed refusal tests, transparency-group `/CS` refusal coverage, opaque CalGray/CalRGB/Lab/Indexed/ICCBased-inert Form group rendering coverage, alpha SMask CalGray/CalRGB/Lab/Indexed/ICCBased-inert group coverage, luminosity SMask CalRGB/Indexed/Separation/DeviceN `/BC` group coverage, transparency-group offscreen-budget refusal coverage, and the `unsupported_retained` retained-subprogram gate | Rust indirect | decode/depth | Program key now includes active page/render-contract identity plus parent-resource fingerprint; supported Form streams, including transparency-group interiors, carry retained plans; retained display-list compilation refuses missing XObject resources, XObject streams without `/Subtype`, and unsupported XObject subtypes before native Form descriptor synthesis; packed-plan compilation refuses stale/manual Image/Form descriptors whose resolved XObject subtype does not match the descriptor kind; active `Do` of missing/malformed XObjects, Form stream fetch/decode/parse failures, direct/indirect Form recursion or depth-limit hits, unsupported retained Form/group/SMask/annotation/tiling/Type 3 subprogram plans, malformed/missing-resource transparency-group `/CS`, malformed Indexed group `/CS`, malformed tint transforms, malformed ICCBased profile references or `/N` metadata, alpha-bearing non-device Form group `/CS`, alpha SMask `/G` non-device group `/CS` with `/BC`, richer non-device transparency-group `/CS`, and transparency-group offscreen allocation denials fail typed instead of silently skipping or replaying through raw immediate dispatch; opaque normal Form groups and `/S /Alpha` SMask groups without `/BC` with well-formed CalGray/CalRGB/Lab/Indexed/ICCBased `/CS` values render through a non-device-inert policy; `/S /Luminosity` SMask `/BC` resolves well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN group spaces through the named-color/CMM resolver with resolved-space cache identity; full alpha/backdrop-observable non-device group-space/backdrop/knockout closure and full operation-level invalidation remain incomplete |
| RV-25 | PARTIAL | PARTIAL_ADVANCED | annotation/widget paths | partial | generated appearances plus typed malformed-appearance refusal | annotation/cache/policy tests pass, including missing-BBox and decode-failure selected appearance refusals | CLI/server partial | bounded appearance program cache | Appearance Form programs are page/render-contract scoped, byte-charged, caller-cache retained on cached display-list annotation replay, supported appearance streams carry retained plans and replay through the packed adapter, retained replay honors print/annotation/form contract policies, page-invalidation pruned, and selected appearances with missing `/BBox` or decode failures fail typed with annotation-stage fatal errors propagated through public/cached/tile replay paths; DocumentSubsystems form actions and secure-mutation incremental form fills now expose structured widget/AP invalidation reports; cache-plan envelope application normalizes AP-state, normal/rollover/down appearance stream, and before/after appearance refs into source-scoped invalidation with unknown-ref conservative reset; dirty-region conversion accepts annotation/widget/appearance and before/after rect/bounds aliases for exact-tile conversion; external cache-handle runtime matrix validation remains incomplete |
| RV-26 | PARTIAL | PARTIAL_ADVANCED | `RenderDocumentCache` / `RenderCache` / runtime capabilities / `RenderContractTelemetryReport` | partial | cache miss | cache tests plus runtime/server cache-pressure policy tests, path clip-node aggregate budget test, glyph-outline aggregate budget test, raster-artifact aggregate budget test, Type 3 parsed-program byte-accounting tests, render-contract telemetry tests, CLI compile check, glyph-atlas tests, and C/Python/WASM/.NET/Java source checks pass | Rust/CLI/server/C/Python/WASM/.NET/Java telemetry source | per-cache LRU plus aggregate byte budget for byte-charged raw image, scaled image, SMask, mesh, Form-program, tiling-program, annotation-appearance program, transformed path clip-node, font-byte, font-resolver, glyph-outline, device glyph-mask, glyph-mask atlas page, Type 3 parsed geometry program, Type 3 parsed CharProc program, Type 3 mask, Type 3 rendered-glyph, path fill-mask, path stroke-mask, and retained display-list caches; active clip-DAG intern tables self-prune unreferenced nodes and report per-render intern/pruning counters; transparent-page-group decisions are entry-capped LRU maps; runtime capability JSON and server `/api/v1/capabilities` disclose cache classes, budget fields, pressure actions, correctness-preservation policy, and transformed path clip-node/glyph-outline/glyph-atlas/Type 3 parsed-program/Type 3 mask/path-mask accounting in the `render_document_cache_byte_accounting` reason; Rust contract telemetry APIs, server multipart contract-report routes, C ABI `wellfriendpdf_document_*_with_*_render_report_json`, Python `render_contract_*_with_render_report`, WASM `renderContract*WithRenderReport`, .NET `RenderPage*WithRenderReport`, and Java `renderPage*WithRenderReport` expose one-shot per-render cache counters including path clip-node, glyph-atlas, and active clip-DAG stats; CLI `render-corpus` reports retained display-list, annotation-appearance program, path clip-node, glyph-atlas, and active clip-DAG cache stats | External runtime telemetry parity remains incomplete |
| RV-27 | PARTIAL | PARTIAL_ADVANCED | display list/path/buffer allocations | partial | scalar/general vectors | source + test suite plus vector-tile scratch replay test | none | selected pools | Vector-tile replay can reuse caller-owned operation-selection scratch, but complete packed-arenas/no-clone warm guarantee remains absent |
| RV-28 | PARTIAL | PARTIAL_ADVANCED | tile/band APIs | YES* | compatible full-raster cache crop; typed unsupported retained-tile refusal | metamorphic, priority-band, dirty-region requeue, render-context revision, source queue-preview, source queue-execution, adjacent-prefetch-execution, and retained-tile typed-refusal tests pass | internal/server partial | tile dimensions/overdraw, visible/adjacent/background priority bands, selective dirty-region requeueing, render-context obsolete-publication reporting, adjacent-page prefetch preview planning, owned current-page queue execution reporting, bounded adjacent-page child-session prefetch execution, and typed unsupported retained-tile refusal | External viewer runtime matrices, broader queue policy validation, and full tile policy remain incomplete |
| RV-29 | PARTIAL | PARTIAL_ADVANCED | `ProgressiveRenderJob` | YES* | typed refusal for unsupported retained/progressive tiles | progressive, request-cancel, viewport-revision, priority-band, dirty-region, render-context revision, tile-publication-acceptance, viewer-queue-preview, viewer-queue-execution, adjacent-prefetch-execution, callback-dispatch, C callback-pointer dispatch, Python/WASM binding checks, .NET cancellation-token source build, and Java cancellation-supplier source compile pass | Rust/server/source bindings partial | completed tile retention, typed unsupported retained-tile refusal, structured fallback details for counted events, deterministic job/tile publication identities salted by render-contract fingerprint, serialized priority classes, selective dirty-tile obsolete reports, bounded obsolete-publication reports for viewport/dirty/render-context revisions, source-session tile-publication acceptance/rejection, adjacent-page prefetch preview, bounded viewer queue preview JSON, viewer queue execution JSON for owned current-page tiles, bounded adjacent-page child-session prefetch execution, viewer callback dispatch JSON with terminal suppression, C/Python/WASM/.NET/Java synchronous callback helpers, linked request-cancel source, Python callable/boolean step/finish cancellation predicates, WASM boolean/AbortSignal-style step/finish cancellation methods, .NET `CancellationToken` step/finish overloads, and Java `BooleanSupplier` step/finish cancellation overloads | External binding runtime parity and broader viewer queue policy incomplete |
| RV-30 | PARTIAL | PARTIAL_ADVANCED | Rust/CLI/server/C/.NET/Java/Python/WASM render APIs | partial | contract/raw surface route plus exact JSON input for raster CLI output, CLI document-view/backend-plan/image-decode reports, and server contract-builder/render/report routes | focused local CLI contract round-trip, document-view report, backend-plan report, and image-decode capability report tests pass; server route check and focused builder/PNG/raw/report/document-view/backend-plan/image-decode tests pass; C ABI check and focused handle test pass; .NET focused builder test passes; Java source compile and builder-only smoke pass; Python crate check passes; WASM target crate check passes; native binding runtime gates pending | Rust/CLI/server partial; C/.NET/Java/Python/WASM source partial | resource-budget fields and contract validation | CLI schema builders now cover matrix, page-box, background, CMM, overprint, exactness, determinism, non-pixel budgets, `document-views-report`, `backend-plan-arena-report`, and `image-decode-capability-report`; server exposes `/api/v1/render-contract`, `/api/v1/render-contract/png`, `/api/v1/render-contract/raw`, `/api/v1/render-contract/png-with-font-substitution-report`, `/api/v1/render-contract/raw-with-font-substitution-report`, `/api/v1/document-views/report`, `/api/v1/render-contract/backend-plan-arena-report`, and `/api/v1/image-decode/capability-report`; C ABI, .NET, Java, Python, and WASM now have schema-v1 contract round-trip/builder handles/objects and typed/object render overloads; non-white background, all five page boxes, device transform, rendering intent, color-management policy, overprint preview, text/image/path/subpixel smoothing, page-content/downstream decoded-byte caps, page-content decode cancellation, temporary-surface caps, and cache-byte caps are honored by active CPU paths where accepted; other non-default execution parity remains incomplete |
| RV-31 | PARTIAL | PARTIAL_ADVANCED | C ABI PNG/JPEG/contract handle functions | no | core immediate/contract encoding | current C ABI source/header check and focused handle test pass | C API partial | C pointer checks and opaque handle validation | C ABI now exposes an opaque schema-v1 render-contract handle with builders and handle render overloads; no external C consumer build/runtime parity gate in this local pass |
| RV-32 | PARTIAL | PARTIAL_ADVANCED | C/Python/WASM/.NET/Java/server | no | simple binding routes | Rust workspace/source pass; server route check and focused builder/PNG/raw/report/document-view/backend-plan/image-decode/adjacent-prefetch tests pass; C ABI, .NET, Java, Python, and WASM source surfaces updated; external binding runtime tools not run | C/Python/WASM/.NET/Java/server source partial | varies | C ABI, .NET, Java, Python, WASM, and server now expose schema-v1 contract construction/render surfaces, document-view, backend-plan arena, and image-decode capability report surfaces, progressive queue execution, adjacent-page prefetch execution, and server multipart report routes for contract PNG/raw rendering; cross-binding renderer parity incomplete and external binding gates not run |
| RV-33 | PARTIAL | PARTIAL_ADVANCED | `prepress.rs`/render paths, SDK/CLI/server plate report, binding shims, and CLI `--print-profile` | partial | display/print/proof contract behavior plus sparse Separation/DeviceN plate report exposure | focused SDK/CLI/server report tests, Rust binding checks/clippy, .NET test build, and Java source/package-smoke compile pass | Rust SDK/CLI/server/C/Python/WASM/.NET/Java source surfaces | existing limits | Proof now fails closed without native CMM and has a native OutputIntent proof-transform hook; active render-interpreter plate reports expose deterministic Separation/DeviceN plate order, contribution counts, memory accounting, and cache fingerprint data; full CMYK/DeviceN separation-preserving print execution and native runtime validation remain incomplete |
| RV-34 | PARTIAL | PARTIAL_ADVANCED | `runtime.rs`, server `/api/v1/capabilities` | source/API matrix active | n/a | `renderer_concurrency_matrix_reports_thread_and_cache_boundaries`, `renderer_capabilities_disclose_fallback_and_progressive_limits`, and server capabilities endpoint test pass | runtime/SDK/server JSON source | configured permits, cache budget fields, serial mutation policy, cancellation and stale-publication guard rows | Runtime capabilities now expose a structured renderer concurrency/cache matrix with host/effective CPU workers, max concurrent documents, per-document serial mutation, work-stealing state, thread permit rows for page-program parse/image decode/tile render/font shaping/progressive publication/document mutation, cache ownership rows for display lists, render tiles, image/mask caches, font/glyph caches, decoded streams, and transaction provenance, plus cancellation/obsolete-publication guard entries. External runtime thread/cache matrix validation remains future platform work |
| RV-35 | PARTIAL | PARTIAL_ADVANCED | page/decode/surface limits | partial | typed errors | resource-budget, max-decoded-bytes, pre-decode cancellation, temporary-budget, and contract limit tests pass | server/CLI partial | contract max pixels, page-content/downstream decoded bytes, caller cancellation before page-content decode, temporary bytes including clipped full-page intermediate budgeting, and cache-byte caps | Full tenant policy and all external binding budget-runtime parity remain incomplete |
| RV-36 | PARTIAL | PARTIAL_ADVANCED | `final-fallback-inventory.md`, `runtime.rs` | partial | FB-01-FB-20 with retained-list raw/immediate delegation removed, Type 3 typed refusal, path-geometry default refusal, generic Type 3 metric operand refusal, active graphics-state operand/restore refusal, active text operand/object-sequence refusal, active marked-content/compatibility-section operand/sequence refusal, active page-path operand and clip-sequence refusal, exact empty-clip installation, and active resource-invocation operand refusal, including SVG/PS vector-output operand, restore-depth, clip-sequence, and text-object preflight | source inventory + tests | CLI counters partial | n/a | 10 active decisions remain; 7 degraded; retained-list raw/immediate delegation, Type 3 ordinary-font fallback, path-only geometry defaulting, generic Type 3 metric defaulting, non-text graphics-state operand defaulting and restore underflow, text operand defaulting and object-boundary tolerance, marked-content/compatibility-section operand/sequence defaulting, page path operand defaulting, visible empty-clip no-ops, and resource invocation operand defaulting are removed, but residual fallbacks still block closure |
| RV-37 | PARTIAL | COMPLETE_ACTIVE_SOURCE | `tools/renderer-visual-diff/visual_diff.py`; `scripts/render_reference_compare.py` | n/a | n/a | compact visual-normalization tests pass with explicit straight/premultiplied/opaque alpha semantics, structured font-environment canonicalization/fingerprints, and RGB-only color-difference metrics; no prohibited campaign run | Python tooling | compact tool limits | Corpus-scale reference execution and human adjudication remain deferred |
| RV-38 | MISSING | COMPLETE_ACTIVE_SOURCE | `tools/pdfium-harness/render_page.c`, `CMakeLists.txt`, `smoke.sh` | n/a | n/a | compile-only MSVC stub-header source guard; SDK runtime deferred | standalone C harness | PDFium bitmap/form-fill errors with typed `failure_class` plus specific JSONL codes | Harness source exists for one/all pages, matrix, clip, page box, DPI, dimensions, annotation flags, supported media-box form-widget rendering through PDFium form-fill, typed unsupported form-policy refusal, raw BGRA/BGRx, hash, worker-count metadata, JSONL error `failure_class`, and manifest; no local PDFium SDK runtime build was run |
| RV-39 | UNVERIFIED | PARTIAL | workspace/tests/C API/SIMD/metamorphic | n/a | n/a | historical workspace evidence plus current local gates where listed | Rust/C API source verified; other tools blocked | n/a | External binding and full corpus gates unavailable in this local pass |
| RV-40 | UNVERIFIED | PARTIAL | audit/inventory/fallback docs; `runtime.rs` capability entries | n/a | n/a | current source capability entries plus focused local capability test where listed | CLI/Rust/C API report partial | n/a | Capability report now exposes core limitations but cannot claim universal completeness |

RV-22/RV-36 update: the 2026-08-18 local continuation extends image-decode/fallback evidence to inline image sequencing, inline image parameter-pair preflight, active image XObject metadata, referenced mask metadata, and uncolored tiling-pattern color counts. Malformed inline image sequences, including data without `ID`, `ID` without `BI`, dangling `BI`, pending `ID`, missing data bytes, and payloads without closing `EI`, fail typed/unsupported before retained or immediate rendering can fabricate empty params, paint unterminated bytes, or silently drop the image. Malformed inline image parameter pairs, including non-name keys, dangling keys, duplicate canonical keys, and short/long alias duplicates, fail typed or whole-page classify before parser normalization, direct renderer helpers, or SVG/PS regional classification can repair them into smaller dictionaries. Missing or invalid XObject dimensions, missing/unsupported non-mask bpc outside terminal JPX, and missing/malformed non-mask color-space metadata fail typed before image-reference/cache-key construction or decode planning. Referenced explicit `/Mask` streams and image SMask streams now fail typed on missing/invalid dimensions, bpc, or color-space metadata before mask decode. PaintType 2 tiling patterns now fail typed on unexpected active pattern-color component counts before tile replay, with focused `malformed_inline_image_parameters_return_parse_error`, `malformed_inline_image_parameters_return_typed_refusal`, `renderer_inline_image_malformed_parameter_pairs_return_typed_refusal`, `malformed_inline_image_parameters_stay_whole_page_fallback`, `malformed_inline_image_sequence_marks_display_list_unsupported`, `malformed_inline_image_sequence_returns_typed_refusal`, `inline_image_triggers_whole_page`, `inline_image`, `renderer_image_xobject_`, `image_xobject_explicit_mask_missing`, `image_xobject_smask_missing`, `smask_`, `uncolored_tiling_pattern_invalid_component_count`, `tiling_pattern`, and `image_xobject` evidence.

RV-20/RV-36 update: the 2026-08-18 local continuation also extends Type 3 evidence to path-only geometry extraction. `Type3PathCollector` now refuses malformed operands for path construction, `cm`, `w`, `J`, `j`, `M`, `d`, device colors, `sc/scn`, and `d0`/`d1`, and refuses path continuation operators without a current point. Focused evidence includes `type3_path_collector_rejects_malformed_graphics_state_operands`, `type3_path_collector_rejects_path_segments_without_current_point`, `type3_clipping_charproc_malformed_line_width_returns_typed_refusal`, and the `type3` focused test group.

RV-20/RV-36 metric update: the 2026-08-18 local continuation also extends Type 3 fail-closed coverage to generic retained/immediate/vector metric descriptors. `type3_glyph_metric_operand_refusal` validates `d0` and `d1` before `GraphicsStateDescriptor` compilation, display-list state capture, active page dispatch, or SVG/PS vector-output classification can supply zero metrics. Focused evidence includes `type3_glyph_metric_operand_validator_rejects_malformed_operands`, `malformed_type3_glyph_metric_marks_display_list_unsupported`, `malformed_type3_glyph_metric_operator_returns_typed_refusal`, and `malformed_operands_stay_whole_page_vector_fallback`.

RV-08/RV-36 update: the 2026-08-18 local continuation extends graphics-state fail-closed coverage to the active page state interpreter and SVG/PS vector-output classifier. `graphics_state_operand_refusal` validates non-text state operands before `GraphicsStateDescriptor` compilation, display-list capture, immediate `GraphicsState::process` mutation, or vector sink replay can supply default state, and display-list/page/vector paths now reject `Q` restore underflow before a tolerant empty-stack restore can occur. Focused evidence includes `graphics_state_descriptor_rejects_malformed_state_operands`, `malformed_graphics_state_marks_display_list_unsupported`, `malformed_graphics_state_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, `graphics_state_descriptor`, and `graphics_state`.

RV-08/RV-36 text update: the 2026-08-18 local continuation also extends operand and object-sequence fail-closed coverage to text-state and text-showing operators, including SVG/PS vector-output classification. `text_operand_refusal` validates text operands before retained text capture, descriptor compilation for text state, immediate text rendering, or vector text replay can supply empty/default state, while display-list capture, immediate/packed rendering, and vector-output classification reject bare showing/positioning operators outside `BT`/`ET`, unmatched `ET`, nested `BT`, and unterminated `BT`. Focused evidence includes `text_operand_validator_rejects_malformed_text_operands`, `malformed_text_marks_display_list_unsupported`, `malformed_text_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, `text_is_replayable_as_native_operation`, and the `text` focused test group.

RV-08/RV-36 marked-content update: the 2026-08-18 local continuation also extends operand and sequence fail-closed coverage to marked-content and compatibility-section operators, including SVG/PS vector-output classification. `marked_content_operand_refusal` validates `BMC`, `BDC`, `EMC`, `MP`, `DP`, `BX`, and `EX`, while display-list finish and relative `RenderState::dispatch_all` boundaries reject unmatched `EMC`, cross-stream `EMC`, unmatched `EX`, and unterminated `BMC`/`BDC`/`BX` begins before descriptor compilation, display-list capture, immediate optional-content visibility mutation, packed replay, or vector replay can supply empty/default marked-content state, leaked visibility, or unbalanced compatibility-section no-ops. Focused evidence includes `marked_content_operand_validator_rejects_malformed_operands`, `malformed_marked_content_marks_display_list_unsupported`, `unterminated_marked_content_marks_display_list_unsupported`, `unbalanced_compatibility_section_marks_display_list_unsupported`, `malformed_marked_content_operator_returns_typed_refusal`, `unterminated_marked_content_operator_returns_typed_refusal`, `unbalanced_compatibility_section_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, and the `marked_content` focused test group.

RV-08/RV-36 path update: the 2026-08-18 local continuation also extends operand and clip-sequence fail-closed coverage to page path construction, paint, and clip operators, including SVG/PS vector-output classification. `path_operand_refusal` validates `m`, `l`, `c`, `v`, `y`, `h`, `re`, `S`, `s`, `f/F`, `f*`, `B`, `B*`, `b`, `b*`, `n`, `W`, and `W*` before display-list capture, immediate path mutation, or vector path replay can drop malformed input, paint/clip with operand-bearing operators, or synthesize missing current points. Valid empty or move-only clipping paths now install an explicit empty clip, while dangling or repeated `W/W*` fails typed before retained or immediate replay can leave later paint visible. Focused evidence includes `path_operand_validator_rejects_malformed_operands`, `malformed_path_marks_display_list_unsupported`, `malformed_path_operator_returns_typed_refusal`, `empty_path_clip_installs_empty_clip`, `malformed_operands_stay_whole_page_vector_fallback`, and the `path` focused test group.

RV-14/RV-36 named-paint update: the 2026-08-19 local continuation extends SVG/PS vector-output fail-closed coverage to named ordinary path paint, text paint modes, Image XObject stencil masks, and inline stencil masks. `render/vector_fallback.rs` allows resource-named device paint only for exact finite component vectors and routes unresolved, unsupported, invalid, or incomplete named paint to the whole-page typed raster/refusal path before SVG/PS sinks can reach the generic black fallback. Focused evidence includes `resource_named_device_rgb_fill_paint_remains_vector_safe`, `unresolved_named_fill_paint_stays_whole_page`, `unsupported_named_stroke_paint_stays_whole_page`, `named_device_rgb_paint_without_components_stays_whole_page`, and `vector_fallback`.

RV-17/RV-23/RV-36 component-exactness update: the no-dictionary device-family conversion helper now requires exact finite `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` component counts instead of delegating to the defaulting converter. Image SMask `/Matte`, luminosity SMask `/BC`, and SVG/PS named-paint preflight therefore fail typed or route to the typed raster/refusal boundary when component vectors are missing, overlong, or non-finite. Focused evidence includes `try_from_components_requires_exact_finite_device_components`, `smask_matte_rgb_rejects_incomplete_device_components`, `smask_backdrop_color_rejects_incomplete_device_components`, `smask_matte_rgb`, `smask_backdrop_color`, and `vector_fallback`.

RV-23/RV-36 named-color component exactness update: `render/colorspace.rs` now rejects overlong vectors in the named-color resolver itself, not only in the no-dictionary device-family helper. Bare device named colors, CalGray/CalRGB/Lab named paint, Separation tint input, DeviceN tint input, and device alternate outputs all require exact finite component counts, and `render/cmm.rs` requires ICCBased scalar inputs to match profile `/N` exactly. Focused evidence includes `bare_device_rgb_overlong_components_is_invalid_not_truncated`, `calrgb_overlong_components_is_invalid_named_color`, `separation_overlong_tint_component_is_invalid_not_truncated`, `separation_overlong_alternate_components_are_invalid_not_truncated`, `device_n_overlong_components_are_invalid_not_truncated`, `colorspace`, `cmm`, and `tint`.

RV-23/RV-36 render-facing device color-state update: `render/color.rs::ColorSpaceHandler::strict_to_render_color` is now used by active page paint conversion, retained display-list simple-color capture, Type 3 inherited vector color replay, and SVG/PostScript vector color resolution. Focused evidence includes `strict_to_render_color_requires_exact_finite_device_state`, `display_list_simple_color_requires_exact_finite_device_state`, `svg_device_color_resolution_rejects_malformed_state_locally`, `postscript_device_color_resolution_rejects_malformed_state_locally`, and the existing `malformed_device_sc_arity_returns_typed_refusal` active PDF-operator gate.

RV-23/RV-26 colour-cache update: Separation and DeviceN tint-transform outputs now use a bounded thread-local LRU cache in `render/colorspace.rs`, keyed by source document hash/length, resolved tint-function fingerprint, exact input count, and tint input bits. The cache admits only successful bounded outputs, evicts least-recently-used entries by entry and byte caps, rejects oversized transform outputs without changing paint semantics, and exposes metrics plus configured caps through the structured `ColorReport` surface. Focused evidence includes `tint_transform_cache_reuses_device_n_function_output`, `tint_transform_cache_evicts_lru_entries`, `tint_transform_cache_does_not_admit_invalid_functions`, and `tint_transform_cache_key_includes_document_boundary`.

RV-21/RV-36 SVG-in-OpenType paint-metadata update: the static SVG color-glyph parser now rejects out-of-range `rgb()` numeric components, percentage components, and opacity attributes instead of clamping them into visible paint. Focused evidence includes `svg_static_subset_rejects_out_of_range_rgb_component`, `svg_static_subset_rejects_out_of_range_rgb_percentage`, `svg_static_subset_rejects_out_of_range_opacity`, `svg_static_subset`, and `color_glyph`.

RV-21 color-glyph JPEG invariant update: decoded JPEG color-glyph output now validates pixel cap, channel count, and exact byte length before returning `RawImage` output or converting CMYK bytes to RGB. Focused evidence includes `color_glyph_jpeg_invariants_reject_short_cmyk_output`, `color_glyph_jpeg_invariants_reject_unsupported_channels`, `color_glyph_jpeg_invariants_apply_pixel_cap`, and `color_glyph`.

RV-21 font substitution descriptor-routing update: deterministic replacement requests now propagate FontDescriptor Symbolic/Nonsymbolic, Italic, ForceBold, and FontWeight hints. Unknown non-Standard14 symbolic descriptor fonts route through the symbolic coverage lookup and report `CoverageOnly` bundled fallback identity plus provider selection reason `symbolic_flag` in compatibility mode, while high-quality/exact rendering still refuses generic bundled replacement.

RV-21 exact raster text metric-source update: high-quality/exact raster text now advances from PDF widths, native Type 3 metrics, or strict variation-aware sfnt/bare-CFF font advances only. Missing exact raster metrics fail typed instead of accepting the compatibility `500` movement fallback, and vertical writing requires a real vertical advance instead of a synthesized `-1000`. Focused evidence includes `text_advance`.

RV-22/RV-26 cache-identity update: the image pipeline now prevents inline decoded-image reuse when two inline payloads share the same length and first 128 bytes but differ later, and inline keys now include active render-contract/CMM/tile/policy/budget/optional-content context. The planner cache contract also carries an explicit `ImageComponentSelection` value, currently `All` for active decoders, so future JPX component-subset decode plans cannot collide with all-component output. Focused evidence includes `inline_image_decode_cache_key_hashes_full_payload_and_render_context` and `cache_key_includes_component_selection_identity`. Native JPX ROI/region/progressive output, JBIG2/lossless region/reduction/progressive output, and broader component-subset pixel output remain incomplete.

RV-22 JPX dimension-consistency update: JPX terminal decode now routes legacy inline images, resolved-color-space inline images, and Image XObjects through a checked finalizer that rejects decoded JPX dimensions differing from the PDF dictionary `/Width` and `/Height` before `/SMaskInData` handling or downstream paint/cache behavior. Focused evidence includes `jpx_finish_rejects_pdf_dimension_mismatch`, `jpx_finish`, `jpx`, `inline_image`, and `image_xobject`. JPX ROI/region, tile/component, progressive, cancellation-inside-codec, and corpus verification remain incomplete.

RV-22 JPX SMaskInData metadata update: JPX finalization now validates `/SMaskInData` as integer `0`, `1`, or `2`, and `/SMaskInData 1` or `2` requires decoded alpha samples. Focused evidence includes `jpx_smask_in_data_rejects_malformed_metadata`, `jpx_smask_in_data_requires_decoded_alpha_channel`, `jpx`, `inline_image`, and `image_xobject`. JPX ROI/region, tile/component, progressive, cancellation-inside-codec, and corpus verification remain incomplete.

RV-22 JPX finalizer invariant update: JPX finalization now enforces the normalized `RawImage` boundary by rejecting non-8-bit samples, zero-channel output, decoded byte-length mismatches, and decoded dimensions over the image decode budget. Focused evidence includes `jpx_finish_rejects_non_8_bit_raw_output`, `jpx_finish_rejects_decoded_length_mismatch`, `jpx_finish_applies_decode_budget`, `jpx`, `inline_image`, and `image_xobject`. JPX ROI/region, tile/component, progressive, cancellation-inside-codec, and corpus verification remain incomplete.

RV-22 DCT inline ColorSpace update: the legacy inline `DCTDecode` path now uses the checked DCT finalizer, so JPEG component counts must match the declared PDF inline image color space before `RawImage` output is returned. Focused evidence includes `dct_inline_rejects_declared_color_space_component_mismatch`, `dct_inline`, `dct_finish`, `inline_image`, and `image_xobject`.

RV-22 image normalized-length update: `build_raw_image` now checks exact normalized sample-buffer length immediately after bit-depth normalization and before color conversion, and the DCT CMYK finalizer shortcut applies decode-budget and exact decoded-length checks before conversion. Focused evidence includes `dct_finish_rejects_short_cmyk_before_conversion`, `dct_finish_cmyk_applies_decode_budget`, `build_raw_image_rejects_mismatched_buffers`, `inline_image`, `image_xobject`, and `dct_inline`.

RV-22 CMYK converter length update: DeviceCMYK and ICCBased-CMYK image conversion entry points now validate exact CMYK input byte length before calling the chunked CMYK-to-RGB helper. Focused evidence includes `color_space_converter_rejects_short_device_cmyk_input`, `cmyk`, `inline_image`, and `image_xobject`.

RV-21 color-glyph PNG invariant update: decoded PNG color-glyph output now validates pixel cap, channel count, and exact byte length before returning `RawImage` output. Grayscale-alpha samples are checked as two-channel decoded data before RGBA expansion. Focused evidence includes `color_glyph_png_invariants_reject_short_gray_alpha_output`, `color_glyph_png_invariants_reject_unsupported_channels`, `color_glyph_png_invariants_apply_pixel_cap`, and `color_glyph`.

RV-21 color-glyph bitmap exactness update: BGRA, gray8, gray subbyte, and mono bitmap color-glyph decoders now require exact payload byte lengths and reject overlong buffers instead of slicing to the computed image span. Focused evidence includes `color_glyph_bgra_bitmap_rejects_trailing_payload_bytes`, `color_glyph_gray_bitmap_rejects_trailing_payload_bytes`, `color_glyph_mono_bitmap_rejects_trailing_payload_bytes`, and `color_glyph`.

RV-22 sub-byte row exactness update: decoded 1/2/4 bpc image data now must match the computed packed row-byte length exactly before sample unpacking. Focused evidence includes `overlong_subbyte_rows_return_malformed_pdf`, `build_raw_image_rejects_overlong_subbyte_rows`, `subbyte_rows`, `build_raw_image`, `inline_image`, and `image_xobject`.

RV-22 raw window source-length update: unfiltered 1/2/4/8/16-bit raw source-window decode now validates that the full source image stream length exactly matches dimensions and channels before cropping the requested window. Focused evidence includes `raw_window_decode_rejects_overlong_source_stream`, `inline_raw_window_decode_rejects_overlong_source_stream`, `raw_window`, `inline_image`, and `image_xobject`.

RV-22 raw window bit-depth update: guarded axis-aligned unfiltered raw Image XObjects and inline images now use bounded source-window decode for 1/2/4/8/16 bpc samples when no unsupported explicit mask, SMask, reduction, full-image postprocessing, or non-axis transform requires full-image materialization. Focused evidence includes `inline_raw_window_subbyte_decode_uses_requested_source_region`, `inline_raw_window_subbyte_rgb_decode_preserves_component_order`, `inline_raw_window_16bit_decode_uses_requested_source_region`, `raw_unfiltered_subbyte_xobject_plan_uses_windowed_source_region_when_clipped`, `raw_window`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

RV-22 raw ImageMask source-window update: guarded axis-aligned unfiltered 1 bpc `/ImageMask true` Image XObjects and inline images now use bounded raw source-window decode before stencil expansion when no reduction, full-image postprocessing, or non-axis transform requires full-image materialization. Focused evidence includes `raw_window_xobject_decode_supports_image_mask_source_region`, `raw_window_unfiltered_image_mask_plan_uses_windowed_source_region_when_clipped`, `raw_window`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

RV-22 raw explicit Mask source-window update: guarded axis-aligned unfiltered raw Image XObjects with color-key `/Mask` arrays or same-sized referenced 1 bpc stencil `/Mask` streams now crop the main image and mask through the same source-window boundary when no `/SMask`, reduction, unsupported mask shape, or non-axis transform requires full-image materialization. Focused evidence includes `raw_unfiltered_explicit_mask_plan_uses_windowed_source_region_when_crop_aligned`, `image_xobject_explicit_stencil_mask_uses_shared_raw_source_window_when_clipped`, and `image_xobject_color_key_mask_keeps_raw_source_window_when_clipped`.

RV-23/RV-36 Indexed lookup exactness update: image decoding and active/named color resolution now require `/Indexed` lookup tables to match `(hival + 1) * base_channels` exactly. Overlong lookup data fails typed instead of being prefix-truncated before palette conversion. Focused evidence includes `indexed_image_rejects_overlong_lookup_table`, `indexed_overlong_lookup_is_invalid_not_prefix_truncated`, `indexed`, `inline_image`, and `image_xobject`.

RV-23/RV-36 Indexed arity exactness update: image decoding and active/named color resolution now require `/Indexed` color-space arrays to contain exactly four entries. Overlong arrays fail typed instead of carrying ignored trailing metadata. Focused evidence includes `indexed_image_rejects_overlong_color_space_array`, `indexed_overlong_color_space_array_is_invalid`, `indexed`, `inline_image`, and `image_xobject`.

RV-23/RV-36 calibrated arity exactness update: image decoding, active/named color resolution, and SVG/PS vector-shading validation now require CalGray, CalRGB, and Lab color-space arrays to contain exactly two entries. Overlong arrays fail typed or stay on the conservative vector fallback boundary instead of carrying ignored trailing metadata. Focused evidence includes `cal_rgb_image_rejects_overlong_color_space_array`, `calrgb_overlong_color_space_array_is_invalid_named_color`, `vector_calibrated_shading_rejects_overlong_color_space_array`, `calrgb`, `calibrated`, `vector_fallback`, `inline_image`, and `image_xobject`.

RV-23/RV-36 color-glyph sbix parser fail-closed update: sbix strike metadata now validates strike headers, strike-offset tables, glyph-offset tables, record bounds, origin fields, duplicate records, and pixels-per-em values before payload selection. Malformed records fail typed instead of defaulting origin/ppem metadata, converting to an unsupported dummy payload, or disappearing as absent glyph data. Focused evidence includes `sbix_parser` and `color_glyph`.

RV-18/RV-23/RV-36 PDF function input-arity update: `eval_function_n` now validates Type 0/2/3/4 required function shapes before evaluation and rejects missing or non-finite caller inputs. Sampled Type 0 functions no longer fill omitted dimensions with `0.0`, Type 2/3 functions no longer evaluate with absent input or missing required domain/encode metadata, and Type 4 calculator functions execute only the strict `/Domain` input count. Focused evidence includes `function` and `shading`.

RV-16/RV-17 SMask transfer backdrop update: alpha soft-mask `/BC` backdrop probing now validates `/TR` shape before testing `TR(0)` and rejects malformed or empty-output transfer functions instead of interpreting an empty function result as zero. Focused evidence includes `smask_transfer`, `alpha_smask_backdrop_transfer`, and `smask`.

RV-16/RV-17 SMask group cache-identity update: SMask `/G` Form resources, transparency group `/CS` policy, and derived `/BC`/default-alpha backdrop identity are resolved before cache lookup, so cached masks cannot bypass malformed, missing-resource, non-device group color-space, malformed backdrop, or malformed transfer-function refusals. The SMask group cache key now includes the resolved group color policy, deterministic `/G` resource fingerprint, and initial-backdrop/outside-alpha identity in addition to revision, source, transform, clip, tile, subtype, CMM/overprint policy, render contract, backend, quality, and display/print profile identity. Focused evidence includes `smask_group_cache_key_includes_contract_output_identity` and `smask`.

RV-22 utility image sample-length update: utility image ingestion now validates exact decoded sample lengths before JPEG CMYK conversion, PNG grayscale/grayscale-alpha/RGB/RGBA expansion, and rendered-page RGBA/gray-to-RGB conversion. Focused evidence includes `utility_rgb_to_rgba_rejects_short_input`, `utility_gray_alpha_to_rgba_rejects_trailing_sample`, `utility_cmyk_length_guard_rejects_short_input`, `utility_raw_image_to_rgb_rejects_short_rgba_buffer`, `utility_raw_image_to_rgb_rejects_trailing_gray_buffer`, and `utilities::tests`.

RV-18/RV-23/RV-36 active-shading device-component exactness update: active shading device color validation now requires sampled function outputs for `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` to have exactly the device component count and all finite values. Too-short, overlong, or non-finite device output vectors fail typed through `malformed_active_shading_dictionary_returns_typed_refusal` instead of being truncated or routed through generic black/default conversion.

RV-08/RV-36 resource invocation update: the 2026-08-18 local continuation also extends operand fail-closed coverage to XObject and shading resource invocation operators. `resource_invocation_operand_refusal` validates `Do` and `sh` before display-list capture, immediate rendering, or SVG/PS vector-output classification can treat missing names as no-ops, look up empty names, regionalize malformed output, or ignore extra operands. Focused evidence includes `resource_invocation_operand_validator_rejects_malformed_operands`, `malformed_resource_invocation_marks_display_list_unsupported`, `malformed_resource_invocation_operator_returns_typed_refusal`, `malformed_resource_invocation_stays_whole_page_vector_fallback`, `xobject_resource`, and `named_shading`.

RV-14 convex-geometry update: finite, closed, single-subpath convex fills now use a guarded direct sampled-span compositor before the generic edge-bucket scanline path. Non-finite, multi-subpath, degenerate, and concave geometry still falls back to the bounded general painter. Focused evidence includes `convex_polygon_fill_fast_path_matches_general_scanline` and `concave_polygon_fill_rejects_convex_fast_path`.

RV-15/RV-26 clip-DAG cache-bound update: `render/clip_dag.rs` now gives each persistent clip DAG a default intern-table cap and automatically prunes unreferenced nodes on insertion when the table exceeds that cap. Full/Empty flyweights and externally referenced save/restore/composite nodes are preserved, and stats now expose max nodes, pruning passes, pruned nodes, and over-capacity-live-node cases. `RenderDocumentCache` stores the active render state's final `ClipDagStats`, and `RenderContractTelemetryReport` serializes those counters through the existing Rust/server/C/Python/WASM/.NET/Java render-report JSON paths. Focused evidence includes `intern_prunes_unused_nodes_when_over_capacity`, `intern_keeps_live_nodes_when_over_capacity`, `clip_dag`, `content_path_clip_replay_reuses_transformed_clip_node_cache`, and `render_contract_telemetry_report_exposes_cache_counters`.

## Current validation evidence

### Earlier local execution before historical remote validation

| Command | Outcome |
|---|---|
| `cargo fmt --all --check` | Initially exposed the `path.rs` parse error; later formatting was applied. Superseded by the local-only continuation gates below. |
| `cargo check --workspace --all-targets --jobs 1` | Passed after syntax/SIMD safety repairs. |
| `cargo test -p wellfriendpdf-engine --lib --jobs 1` | Initially 1,422/1,425 due to two SIMD guard failures and one AA regression; after repairs 1,425/1,425 passed. |
| `cargo clippy --workspace --all-targets --jobs 1 -- -D warnings` | Passed after clippy repairs. |
| `cargo test --workspace --all-targets --jobs 1` | Initial local invocation was cancelled by the user before execution. |

### Historical VPS code-validation commit

At `dd9e9d7b64bd13598b143d54d24a5ecd331a9ec8`, earlier VPS `HEAD == origin/main`, and `git status --short` was empty. The following commands passed on the VPS during that earlier pass, not during the local-only continuation:

| Command | Exit code | Evidence log |
|---|---:|---|
| `cargo fmt --all --check` | 0 | `cargo-fmt-check-round4.log` |
| `cargo check --workspace --all-targets --jobs 1` | 0 | `cargo-check-workspace-round4.log` |
| `cargo clippy --workspace --all-targets --jobs 1 -- -D warnings` | 0 | `cargo-clippy-workspace-round4.log` |
| `cargo test -p wellfriendpdf-engine --lib renderer_capabilities_disclose_fallback_and_progressive_limits --jobs 1` | 0 | `cargo-test-runtime-capabilities-round4.log` |
| `cargo test --workspace --all-targets --jobs 1` | 0 | `cargo-test-workspace-round4.log` |
| `cargo build -p wellfriendpdf-cli --release` | 0 | `cargo-build-cli-release-round4.log` |
| `cargo build -p wellfriendpdf-capi --release` | 0 | `cargo-build-capi-release-round3.log` |
| `cargo test -p wellfriendpdf-capi --lib --jobs 1` | 0 | `cargo-test-capi-lib-round3.log` |
| `cargo test -p wellfriendpdf-render-simd --lib --jobs 1` | 0 | `cargo-test-render-simd-round3.log` |
| `cargo test -p wellfriendpdf-engine --test binding_surface1_renderer_metamorphic --jobs 1` | 0 | `cargo-test-renderer-metamorphic-round3.log` |
| `cargo test -p wellfriendpdf-engine --test svg_output --jobs 1` | 0 | `cargo-test-svg-output-round3.log` |
| `wellfriendpdf --mode standard capabilities` | 0 | `cli-standard-capabilities-round4.log` |
| `wellfriendpdf --mode standard render --help` | 0 | `cli-standard-render-help-round4.log` |
| `wellfriendpdf --mode research capabilities` | 0 | `cli-research-capabilities-round4.log` |

The first historical VPS attempt returned `127` for Cargo only because noninteractive SSH omitted `$HOME/.cargo/bin`; this was diagnosed and rerun after sourcing `$HOME/.cargo/env`. The first full historical suite found the SVG fidelity defect; the subsequent SVG fallback commits and historical final suite passed.

## Binding and feature blockers from historical VPS pass

- `cargo check/test --workspace --all-features --all-targets`: **BLOCKED**; `pkg-config` cannot locate `lcms2`, required by `native-cmm-lcms2`.
- Python package build: **BLOCKED**; `maturin` absent.
- WASM build: **BLOCKED**; `wasm-pack` and `wasm32-unknown-unknown` target absent.
- .NET build: **BLOCKED**; `dotnet` absent.
- Java Maven/Gradle build: **BLOCKED**; only Java runtime 21 exists; `javac`, Maven, Gradle, and repository-required JDK 25 toolchain are absent.
- Direct PDFium C/C++ harness: **SOURCE_ACTIVE / RUNTIME DEFERRED**; harness source is present under `tools/pdfium-harness`, including PDFium form-fill widget rendering for the supported viewport path, effective matrix/clip and raw-byte JSONL metadata, requested matrix/clip/background manifest metadata, explicit serial worker execution disclosure, and typed refusal for unsupported form transform or raw-output allocation/size combinations. No local PDFium SDK/header/import library was provisioned or built in this task; a compile-only MSVC `/W4 /WX` guard used stub public PDFium headers.

## No-benchmark statement

**No performance, corpus, latency, throughput, or competitor benchmark was executed during this task.**

No production service was deployed, no release tag was created, and no package was published.

## Final implementation status

The repaired renderer continuation is locally source-validated for the available Rust/CLI/core surfaces listed below, and its capability output is more honest about retained replay, fallback reporting, progressive limitations, publication identity, and SIMD fallback. It is **not** a universal renderer implementation under the requested definition because the missing/partial IDs above remain active architectural and public-surface blockers.

## 2026-08-11 local source-only continuation

**Local start commit:** `2c893fbfe5ca3799f7ba9e437fe080f63735e0ca`
**Branch:** `main`
**Working root:** `.work/final-universal-renderer-implementation`
**Execution boundary:** local device only; no VPS, no SSH, no corpus, no benchmarks, no competitor run, no deployment, no release publication.

### Implemented in this local pass

1. Caller-owned contract surfaces now honor `reverse_byte_order` and RGBA/BGRA `alpha_mode` in the CPU row encoder instead of treating those layout choices as refused or metadata-only contract fields.
2. `HalftonePolicy::Screen` now has a deterministic ordered-screen raster pass for RGB caller surfaces and normal render-contract output. It remains a typed refusal only when combined with separation-preserving overprint.
3. Progressive rendering is exposed through server HTTP sessions with owner scoping, bounded session count, idle reaping, pause/resume/cancel/close, finish-and-release PNG output, strict multipart numeric parsing, and page-pixel/file-size validation.
4. Progressive tile sizing now supports deterministic adaptive selection from 128/192/256/384/512 by passing zero tile dimensions. The server also accepts `tile_size=adaptive`, and .NET/Java expose adaptive convenience methods.
5. Progressive tokens and step reports now expose deterministic publication identity salted by render-contract fingerprint, and completed tile publications include per-tile stale-publication discriminators plus render-context revision rejection.
6. Image metadata-first culling now accounts for non-zero tile origins instead of failing open for tile viewports, and inline images now skip decode before scheduler reservation when metadata proves they are outside the active viewport.
7. Image soft-mask classification is collected during the primary XObject walk, removing the previous second object-lookup pass.
8. C header declarations, WASM TypeScript declarations, and managed binding guards were updated so the existing contract/progressive/caller-surface source is visible to downstream callers.
9. The compact visual-normalization harness now records canonical RGBA policy, raw stride, packed raw byte-order normalization, explicit straight/premultiplied/opaque alpha semantics, render-context sidecars including structured font-environment canonicalization/fingerprints, RGB-only color-difference metrics, context-difference taxonomy, and per-region likely-cause classifications.
10. Cached render dependency recording now includes page object, content streams, bounded indirect resource references, selected page dictionary references, preserved indirect font/color-space/ExtGState refs, and automatic bounded retained resource-op tile edges for resolved font-backed text, color-space-backed paint/text, ExtGState-backed paint/text/inline-image, XObject/Form and shading object graphs, pattern-path, and pattern-painted text resources, including bounded transitive refs for active retained font, color-space, ExtGState, pattern, XObject/Form, and shading resource objects; supported source-editing text transactions refresh their post-apply write set from the concrete mutation report; scene `geometric_block` and `semantic_document` text transactions now route to canonical TextReflow preview/apply paths, forward explicit region/flow/layout/review fields, and return source refs/pages/dirty regions plus typed refusal metadata through the EditingTransactions invalidation report; advanced-editing Link annotation moves and Ink annotation appearance regeneration now emit structured render write-set refs, changed/created object refs, affected pages, and page-space dirty regions; transaction invalidation maps PDF refs, structured `[object, generation]`/`{object_number, generation}`/`{ref}` refs, and `object-*`/`stream-*` source IDs, deduplicates them, unions mapped source refs with explicit affected pages, accepts explicit pixel-space dirty tiles, exposes caller-supplied viewport/grid conversion from dirty rectangles to render tiles, and routes source-only mapped transactions through page-artifacts-plus-exact-tiles invalidation so exact source-tile coverage can avoid page-raster expansion while incomplete coverage still expands safely; source-to-tile graph edges and valid explicit affected tiles can evict exact raster tiles without page artifact pruning, and explicit dirty-tile plans with affected pages now prune page-scoped retained artifacts without expanding raster invalidation to every recorded tile on that page only when the supplied exact tiles are valid; page-0 or zero-sized exact-tile entries do not count as coverage and affected pages fall back to recorded page-tile invalidation. Mapped source IDs also prune source-bound decoded-image, scaled-image, soft-mask, mesh-shading, Form-program, tiling-program, and annotation-appearance program cache entries; page-scoped decoded-image, scaled-image, soft-mask, mesh-shading, Form-program, tiling-program, and annotation-appearance program cache entries are pruned for full page invalidations; Form, mesh-shading, tiling-program, and annotation-appearance program keys include active page/render-contract identity where applicable.
11. SVG and PostScript/EPS output now replay vector-safe Form XObjects, including no-op and opaque-inert isolated/knockout transparency-group Forms, simple linear axial shadings, finite/invertible affine-transformed radial shadings, simple finite-matrix shading-pattern path and glyph-outline text paints, safe line-style/dash/font/flatness/smoothness/default-state/identity-transfer/first-supported-normal-blend-array ExtGState, and SVG normal scalar alpha natively instead of forcing whole-page raster embedding, using sink-specific vector fallback classification, scoped resource merge, Form matrix application, BBox clipping, depth/cycle guards, SVG `<linearGradient>`/`<radialGradient>`, PostScript `shfill`, SVG opacity attributes, and native vector line/dash-state application.
12. SVG and PostScript/EPS output now embeds complete finite-affine DeviceGray/DeviceRGB/DeviceCMYK inline images, resource names that resolve to those device color spaces, direct/resource-resolved CalGray/CalRGB/Lab inline images, direct/resource-resolved Indexed inline images, direct/resource-resolved ICCBased inline images, preflighted opaque direct/resource-resolved Separation/DeviceN inline images, DCT-filtered opaque resource-resolved Separation and one-colorant DeviceN inline images, DCT-filtered calibrated/Indexed/ICCBased inline images, simple inline image masks, and Image XObject stencil masks as bounded transformed regional raster elements instead of rasterizing the whole page. PostScript stencil regions use `imagemask`; color regions use `colorimage`.

### Updated status rows

| ID | 2026-08-11 status | Source status | Remaining source limitation |
|---|---|---|---|
| RV-05/RV-09 packed backend plans | PARTIAL_ADVANCED | Packed vector plans pre-resolve covered image/Form/shading/ExtGState/font/color-space/pattern/Properties resources when compiled with page/form resources; resource-aware Image XObject descriptors also carry first-level named image color-space payloads; resource-aware descriptors whose referenced resources are absent now compile to typed refusals instead of retaining names for runtime lookup; BMC/BDC/EMC boundaries force packed adapter replay when vector visibility state is needed, including inline `/OC` dictionaries; supported Form XObject streams, including transparency-group interiors, cache parent-resource-scoped retained plans and replay through the packed adapter; supported tiling-pattern streams cache resource-scoped retained plans and replay through a tile-CTM-aware packed adapter; supported annotation appearance streams cache retained plans and replay through the packed adapter; Type 3 CharProcs with clips, typed resources, `d0`/`d1` metrics, and vector paths with per-paint inherited/explicit color handling can replay through guarded retained plans. | Unsupported/advanced pattern subplans, alternate optional-content configuration/public-selection plans, complete transparency/group semantics, complete colour transforms, complete text shaping/font-program state, and complete backend-native payloads remain incomplete. |
| RV-08 retained immediate delegation | COMPLETE_ACTIVE_SOURCE | Default, cached, contract full-page, retained tile, direct tile, progressive supported-list rendering, and CLI `render-corpus --pipeline display-list` route through packed plans with typed descriptor replay and caller cancellation propagation; unsupported retained display-list replay now returns typed `UnsupportedFeature` in compatibility and high-quality/exact full-page, display-list, tile, band, progressive, and render-corpus page paths instead of canonical raw/immediate dispatch; missing ExtGState `gs` resources and malformed `Do`/`sh` resource invocation operands fail typed instead of retaining default graphics state or no-oping resource dispatch. | Full backend-native high-level payload coverage remains incomplete, but canonical raw-dispatch fallback for unsupported retained replay is removed. |
| RV-11 transaction invalidation | PARTIAL_ADVANCED | Cached render setup records bounded page/content/resource dependencies; supported source-editing text transactions now carry the source mutation's changed object/page set into the report, scene `geometric_block` and `semantic_document` text transactions route to TextReflow with forwarded compact request region/flow/layout/review fields and carry source refs/pages/dirty regions plus typed refusal metadata into the EditingTransactions report, advanced-editing Link/Ink annotation updates emit structured render write-set refs plus page-space dirty regions, DocumentSubsystems AcroForm actions and secure-mutation incremental form fills emit structured field/widget/AP invalidation reports, and invalidation honors mapped PDF refs, structured `[object, generation]`/`{object_number, generation}`/`{ref}` refs, target-object/generation pairs in broad write-set envelopes, `object-*`/`stream-*` source IDs, explicit affected pages, valid explicit dirty tiles, source-to-tile graph edges, preserved indirect font/color-space/ExtGState/Properties refs, automatic bounded retained resource-op tile edges for resolved font/pattern/color-space/ExtGState/Properties resources, bounded transitive retained font/color-space/ExtGState/pattern/XObject/Form/shading tile-source refs, active `/OC` marked-content property tile-source refs, image filter/decode/decode-params/interpolation/matte/color-key-mask and JPEG/DCT/JPX/CCITT/JBIG2 parameter write-set aliases, source-only mapped transaction exact-tile mode with safe expansion when source coverage is incomplete, source-marker pruning for source-bound decoded/scaled image, SMask, mesh, Form, tiling, and annotation-appearance artifacts, caller-supplied viewport/grid conversion from dirty rectangles to render tiles, and page-artifact-plus-exact-tile invalidation when a dirty-tile plan also carries affected pages and valid exact-tile coverage. Broad `writeSet`/`affectedObjects`/`dirtyObjects` metadata strings such as operation names, reasons, and semantic labels are ignored unless they parse as object/source refs; explicit renderer ref fields remain strict and still drive conservative reset for unknown refs. Page-0 or zero-sized exact tiles are ignored and therefore cannot mask page-wide raster invalidation. Rust SDK, C, Python, WASM/TypeScript, .NET, Java, and server entry points return a binding-safe render-invalidation plan with mapped source IDs, `source_cache_markers`, unmapped reset indicators, next revision, cache entry-point guidance, and optional dirty render tiles. Rust cache owners can parse either the plan body or SDK envelope, derive omitted marker strings from object number/generation, normalize string and structured nested write-set refs, and apply exact or conservative cache mutation through `RenderInvalidationCachePlan`. | Broader transaction write-set classification across arbitrary shared resources, remaining conservative page-level narrowing, external runtime matrices, and remaining shared-resource classification are incomplete. |
| RV-15 persistent clip DAG | PARTIAL_ADVANCED | The active renderer now keeps persistent clip DAG representation variants, structural intersections, group-window materialization, display-list and active RenderState active-node save/restore, contract-scope active-clip rebinding, saved-node restore without cache materialization, bounded render-contract-scoped transformed path clip-node reuse through `RenderDocumentCache` for repeated content/display-list/packed-plan/Form/pattern clip installs, active DAG intern-table pruning, path clip-node telemetry, active ClipDag intern/pruning telemetry through `RenderContractTelemetryReport` plus CLI `render-corpus`, and clone-free active-clip borrowing in the main `PixelBuffer` row paint paths. | `PixelBuffer` still installs and consumes concrete masks at raster-buffer boundaries; full dense clip/mask fusion, non-normal/high-quality fusion policy, and complete active-buffer clip adoption remain incomplete. |
| RV-18/RV-19 shading and pattern caches | PARTIAL_ADVANCED | Mesh shading and tiling program cache keys include active page/render-contract identity, and page-scoped entries are pruned on narrow page invalidation. | Backend-plan payload closure and progressive exact batching for all valid high-level resources remain incomplete. |
| RV-21 font substitution reporting | PARTIAL_ADVANCED | Bounded substitution events are recorded during render, serializable, returned from Rust render APIs, exposed through CLI JSON sidecars, available through C/Python/WASM/.NET/Java source render APIs, and returned by server contract PNG/raw report routes as multipart metadata; events include requested PDF font, embedded state, encoding, high-level substitution reason, provider selection reason, metric posture, Standard14/document-provided/caller-registered/deterministic-system/bundled resolution source, selected replacement, bounded required glyph coverage, missing-glyph samples/counts, visual-risk category, extraction/editing impact, font-policy identity, and risk flags; source single-flight coalesces equivalent renderer-originated report events while preserving first observed page/coverage; caller-registered font bytes resolve before bundled fallback, report `user_registered`, and salt font-policy/cache identity with a byte-sensitive provider fingerprint; C ABI/header, Python, WASM/TypeScript, .NET, Java, CLI render, server render-contract routes, and server progressive start expose registered-font input paths; descriptor Symbolic/Nonsymbolic, Italic, ForceBold, and FontWeight hints feed deterministic provider selection so unknown non-Standard14 symbolic descriptor fonts route to the symbolic coverage face and report `CoverageOnly` with selection reason `symbolic_flag`; explicit `HighQualityExact` contract rendering accepts valid registered/deterministic-system replacements and refuses generic bundled substitution when no valid embedded/document/system/registered replacement exists; font byte/resolver cache keys and derived glyph hashes are scoped by render-contract identity, resource budget, and registered-provider identity; device glyph/path-mask cache pressure now evicts least-recently-used masks without clearing hot masks; reused device glyph masks can paint from bounded atlas pages through grayscale rows or RGB LCD-style per-channel rows when subpixel text policy is active. | External registered-font runtime validation is deferred. |
| RV-22 image pipeline | PARTIAL_ADVANCED | Metadata-first image culling includes tile-origin handling for XObjects and pre-decode scheduler-skip behavior for offscreen inline images; raw decoded-image cache lookup/admission uses planner keys salted with revision, render-contract identity, tile viewport, device transform, target size, quality, codec, source-region, reduction dimensions, policy, optional-content state, and resource budget; the planner exposes typed Rust/source capability reports for full-decode-only JBIG2/lossless paths, guarded DCT/JPEG native reduced-IDCT downscale, guarded JPX native target-resolution downscale, and guarded native CCITT source windows, active decode-required paths consume/log them, visible plans record source-region/reduction requirements, `HighQualityExact` refuses unsupported paths when required support is unavailable, runtime capabilities disclose full-decode codec limitations plus guarded DCT/JPX/CCITT exceptions, per-image capability JSON is source-visible through Rust SDK/C/Python/WASM/.NET/Java without decoding pixels, and `ProgressiveImageDecodeSession` exposes bounded start/continue/pause/resume/cancel/close lifecycle reports for image decode work through Rust SDK, C ABI/header, Python, WASM, .NET, Java, CLI, and server source surfaces; SMask discovery is primary-walk based. Guarded axis-aligned CCITT source-clipped XObjects now decode/cache a bounded grayscale window, and matching inline images decode a bounded grayscale window; both paint through an adjusted source-region CTM when no reduction or full-image postprocessing is required. | JPEG/JPX ROI/region/progressive pixel continuation, JBIG2/lossless ROI/reduction-tier decode/progressive pixel continuation, and CCITT reduction/progressive/non-axis/unsupported-postprocessing-window closure remain incomplete. |
| RV-24 Form/XObject program cache | PARTIAL_ADVANCED | Reusable Form programs are keyed by active page/render-contract identity plus the active parent-resource fingerprint, pruned when narrow page invalidation evicts page-scoped artifacts, supported Form streams including transparency-group interiors carry retained plans that replay through the packed adapter, retained display-list compilation refuses missing/no-subtype/unsupported-subtype XObject resources before native Form descriptor synthesis, packed-plan compilation refuses stale/manual Image/Form descriptors whose resolved subtype does not match the descriptor kind, active `Do` of missing/malformed XObjects plus recursive, over-depth, malformed `/Group /CS`, or offscreen-budget-denied transparency-group Form XObjects fails typed instead of silently skipping, opaque normal Form groups with well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN `/CS` values render through a non-device-inert active policy, and luminosity SMask `/BC` resolves the same bounded non-device/tint-transform group color-space subset through the named-color/CMM path. | Unsupported Form display lists, alpha/backdrop-observable non-device group-space/backdrop/knockout closure beyond the non-device-inert/luminosity-backdrop subsets, and full operation-level invalidation remain incomplete. |
| RV-26 render document cache | PARTIAL_ADVANCED | Revision-bound caches have explicit page-scoped artifact pruning for decoded images, scaled images, SMask groups, mesh data, Form programs, tiling programs, annotation appearance programs, display lists, and transparent-page-group decisions; byte-charged raw image, scaled image, SMask group, mesh-shading, Form-program, tiling-program, annotation-appearance program, transformed path clip-node, font-byte, font-resolver, glyph-outline, device glyph-mask, glyph-mask atlas page, Type 3 parsed geometry program, Type 3 parsed CharProc program, Type 3 mask, Type 3 rendered-glyph, path fill-mask, path stroke-mask, and retained display-list caches enforce deterministic caps where applicable, with Type 3 parsed-program caches tracking explicit `Unvisited`/`Compiling`/`Compiled`/`Failed` lifecycle states and retained failure reasons; transparent-page-group decisions enforce entry-capped LRU admission, Form/tiling/annotation-appearance/display-list/path-clip/font map/glyph-atlas stats report retained bytes, active clip-DAG intern tables self-prune and report final per-render stats, runtime capability JSON and server `/api/v1/capabilities` disclose structured renderer cache-pressure policy and transformed path clip-node/glyph-outline/glyph-atlas/Type 3 parsed-program/Type 3 mask/path-mask cache accounting, Rust/server/C/Python/WASM/.NET/Java contract report routes or source APIs expose one-shot per-render cache counters including path clip-node, glyph-atlas, and active clip-DAG stats, and CLI `render-corpus` reports retained display-list, annotation-appearance program, path clip-node, glyph-atlas, and active clip-DAG cache stats. | External runtime telemetry parity remains incomplete. |
| RV-28 tile scheduler | PARTIAL_ADVANCED | Fixed tile sizes 128/192/256/384/512 are supported, adaptive size selection is deterministic, visible/adjacent/background ordering is active through viewport hints, dirty-region revision requeues only intersecting tiles while retaining clean completed tiles, render-context revision obsoletes prior caller-visible contract/visibility publications, source queue reports preview adjacent-page prefetch work before background tiles, source queue execution reports render owned current-page tiles, bounded adjacent-page prefetch execution creates child jobs for live neighboring-page work, and source callback-dispatch reports derive ordered ready/obsolete/scheduled events without rendering with C/Python/WASM/.NET/Java synchronous callback helpers. | External viewer runtime matrices and broader queue policy are not complete. |
| RV-29 progressive lifecycle | PARTIAL_ADVANCED | Rust, server, C ABI source, Python, WASM, .NET, and Java expose progressive session lifecycles in source; request-cancel links the session-owned cancellation source with each render step; server sessions are bounded and owner scoped; step-report JSON carries structured fallback policy details for counted events, deterministic job/tile publication identities with render-contract fingerprints, serialized visible/adjacent/background priority classes, dirty-region scheduler generation, bounded obsolete-publication IDs after viewport, dirty-region, or render-context revision, source-session tile-publication acceptance reports, adjacent-page prefetch previews, bounded viewer queue preview ranks, viewer queue execution JSON for owned current-page tiles, bounded adjacent-page child-session prefetch execution, viewer callback dispatch JSON with terminal suppression, C/Python/WASM/.NET/Java synchronous callback helpers, Python callable/boolean step/finish cancellation predicates, WASM boolean/AbortSignal-style step/finish cancellation methods, .NET `CancellationToken` step/finish overloads, and Java `BooleanSupplier` step/finish cancellation overloads. | Binding runtime matrix, cross-language behavioral parity, and broader viewer queue policy remain incomplete; unsupported retained tile and retained full-page compatibility replay now fail typed. |
| RV-30 Rust/API controls | PARTIAL_ADVANCED | Contract JSON, caller-owned buffer render, grayscale/raw layout, reverse byte order with SIMD-assisted row routing where available, RGBA/BGRA alpha-mode row encoding, ScalarReference caller-surface scalar row preservation, adaptive progressive entry, bounded CLI raw contract rendering, exact CLI JSON input, CLI `document-views-report` and `backend-plan-arena-report`, contract background, all five page boxes, device transform, page-content/downstream decoded-byte caps, page-content decode cancellation, temporary-surface caps including canonical/clipped render-surface budget refusal, cache-byte caps, runtime caller-surface capability disclosure including alpha-mode and reverse-byte-order routing, CLI schema builders for alpha mode, CMM, overprint, exactness, determinism, and resource budgets, server `/api/v1/render-contract`, `/api/v1/render-contract/png`, `/api/v1/render-contract/raw`, `/api/v1/render-contract/png-with-font-substitution-report`, `/api/v1/render-contract/raw-with-font-substitution-report`, `/api/v1/document-views/report`, and `/api/v1/render-contract/backend-plan-arena-report`, and C/.NET/Java/Python/WASM typed contract construction/render/backend-plan report overloads are active in source. Rendering intent, color-management policy, overprint preview, text/image/path/subpixel smoothing, exactness, and determinism are accepted as active CPU-renderer policies for the currently implemented boundaries: rendering intent feeds active ICC/named-color CMM paths, `ColorManagementPolicy` selects the active ICC backend policy/cache identity for contract renders, `OverprintPolicy::Preview` gates PDF graphics-state DeviceCMYK overprint preview, subpixel text uses RGB LCD-style glyph-mask painting, ScalarReference forces render-time and caller-surface row scalar paths, exactness is accepted independently from compositing for exact-refusal/fallback, and `BestEffortResearch` determinism is accepted as satisfied by deterministic CPU output. | Full native CMM output, proof/separation output, separation-preserving overprint, universal exact rendering, and external binding runtime matrix remain incomplete. |
| RV-31 C ABI | PARTIAL_ADVANCED | Contract JSON render, opaque typed render-contract handle, caller buffer, progressive create/step/token/pause/resume/cancel/finish/free exports exist and are declared in the public header. | Header/source checks and focused handle test are local; no external C consumer build was run in this pass. |
| RV-32 language bindings | PARTIAL_ADVANCED | C, Python, WASM, .NET, Java, and server source expose contract/progressive surfaces, including request-cancel, viewport revision JSON, dirty-region revision JSON, render-context revision JSON, tile-publication evaluation JSON, viewer queue JSON, viewer queue execution JSON, adjacent-page prefetch execution, viewer callback dispatch JSON, and synchronous callback helpers where applicable; C ABI, Python, WASM, .NET, and Java expose `document_views_report`/`documentViewsReportJson` and backend-plan arena report source surfaces for the canonical lazy-view/render-view reports; C ABI, .NET, Java, Python, and WASM also expose typed/object schema-v1 render-contract helpers in source, while server exposes contract build/PNG/raw HTTP routes, document-view and backend-plan arena report HTTP routes, multipart PNG/raw font-substitution report variants, and the progressive render-context, queue, queue execution, adjacent-prefetch execution, and callbacks routes. Python/WASM checks pass with cancellation predicates, adjacent-prefetch source, and document-view/backend-plan report source; .NET builds with token overloads, adjacent-prefetch source, and document-view/backend-plan report source; Java source compiles with supplier cancellation overloads, adjacent-prefetch source, and document-view/backend-plan report source. | Full generated-schema ergonomics and runtime validation remain incomplete. |
| RV-33 print profile | PARTIAL_ADVANCED | Screen halftone execution is active for RGB raster surfaces; incompatible separation-preserving output fails typed; CLI exposes display/print/proof contract selection; proof contracts now require native CMM and route through a bounded OutputIntent proof-transform hook when native CMM/profile data are available. | Full CMYK/DeviceN/separation-preserving print execution and native runtime validation remain incomplete. |
| RV-37 visual normalization | COMPLETE_ACTIVE_SOURCE | Visual-diff schema v3 covers canonical comparison surface, raw stride/channel/byte-order/straight-premultiplied-opaque alpha/rotation normalization, explicit background compositing, grayscale luma conversion, exact post-rotation dimension validation, render-context sidecars including deterministic structured font-environment identity/fingerprints, expected-difference masks, metrics including RGB-only `color_mae`/`color_rmse`/`max_color_delta`, connected regions, severity, and likely-cause taxonomy. | Corpus-scale reference execution and human adjudication remain deferred. |
| RB-14 SVG/PS regional fallback | PARTIAL_ADVANCED | Finite affine image XObjects including stencil masks, complete finite-affine DeviceGray/DeviceRGB/DeviceCMYK inline images except CCITT/JBIG2 terminal RGB/CMYK declarations, resource-named device color spaces, direct/resource-resolved CalGray/CalRGB/Lab inline images, direct/resource-resolved Indexed inline images, direct/resource-resolved ICCBased inline images, preflighted opaque direct/resource-resolved Separation/DeviceN inline images, DCT-filtered opaque resource-resolved Separation and one-colorant DeviceN inline images, DCT-filtered calibrated/Indexed/ICCBased inline images, simple inline image masks, exact regional image/stencil buffer validation, inert SVG/PS regional kind/bounds metadata for Image XObject and inline-image segments, vector-safe Form XObjects including no-op, opaque-inert isolated/knockout transparency-group Forms, opaque normal DeviceGray/DeviceRGB/DeviceCMYK group-colour transparency Forms, and opaque normal well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN group-colour transparency Forms, composed SVG stacked clips, dense ordinary text, simple linear DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved well-formed CalGray/CalRGB/Lab/Separation axial shadings with endpoint-sampled contained non-unit shading `/Domain` support and transformed shading `/BBox` clipping, finite/invertible affine-transformed DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved well-formed CalGray/CalRGB/Lab/Separation radial shadings through the same simple Type 2 endpoint-sampling and BBox path, simple finite-matrix shading-pattern fill/stroke path paints and glyph-outline text paints with shading BBox clip intersection, simple colored and color-inheriting uncolored tiling-pattern path paints, glyph-outline text paints, vector-safe resource-bearing image/Form/shading/inline-image tile cells, plus tiling-pattern painted inline/Image XObject stencil masks, SVG normal-blend scalar ExtGState opacity, stateful exact PostScript zero-alpha ExtGState no-op paint, fully transparent regional RGBA no-op regions, binary 0/255 regional RGBA alpha clips, safe line-style/dash/font/flatness/smoothness/default-state/identity-transfer/first-supported-normal-blend-array ExtGState, styled stroked/fill-stroke text outlines, font-resolved text-clipping glyph clips, PostScript `imagemask` stencil emission, PostScript binary regional RGBA alpha clip plus fractional RGBA refusal, and overwritten dead pattern color state are preserved as native/vector-bounded SVG/PS output without silent regional sample synthesis, dropped valid shading BBoxes, or default-parameter calibrated gradient conversion. | Non-linear/mesh/malformed radial shadings, malformed calibrated shading dictionaries, malformed or degenerate shading BBoxes, Type 2 domains/functions requiring clipped flat sections outside the function domain, unsupported named color spaces, `/None` or unresolved Separation/DeviceN/tint-space inline-image resources, multi-component DeviceN terminal-codec tint-space inline images, non-DCT terminal-codec tint-space inline images, CCITT/JBIG2 terminal inline images declared as non-gray device color spaces, non-DCT terminal-codec calibrated/Indexed/ICCBased inline images, advanced pattern paint and resource-bearing tiling cells outside the vector-safe image/Form/shading/inline-image subset, unsupported advanced pattern-space transforms, active patterned text outside the supported shading/tiling glyph-outline subset, alpha/backdrop-observable semantic transparency groups, alpha/backdrop-observable non-device group-colour transparency Forms, visible PostScript fractional-alpha/non-normal-blend paint, fractional regional RGBA, SVG soft masks or semantic transparency groups outside native blend/opacity, visible PostScript non-normal-blend paint, enabled overprint, invalid overprint mode, enabled stroke adjustment, enabled alpha-source, disabled text knockout, unresolved/preflight-unavailable text clipping, soft masks, non-identity transfer functions, invalid ExtGState font arrays, negative/non-finite flatness or smoothness values, unsafe nested Forms, degenerate image transforms, and other unsupported local constructs still require whole-page raster embedding. |

SVG/PS group-colour Form continuation note: the vector Form classifier now accepts explicit DeviceGray, DeviceRGB/RGB, DeviceCMYK/CMYK, and well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN transparency-group `/CS` values only after the existing Form-stream scan proves every paint is opaque, normal-blend, and vector-safe. Separation and one-/multi-component DeviceN group spaces validate exact component metadata plus zero/full tint samples through the named-color resolver before native SVG/PostScript replay. Alpha-bearing, backdrop-observable, `/None`, malformed, unresolved, or unsupported group-colour Forms still force the whole-page vector fallback/refusal boundary. Evidence: `device_group_color_space`, `calibrated_group_color_space`, `indexed_group_color_space`, `iccbased_group_color_space`, `group_color_space_opaque_transparency_group_form_xobject_replays_natively`, `transparency_group_form_xobject`, engine check, and regional-vector clippy.

Proof OutputIntent selection continuation note: `engine.rs` now scans catalog `/OutputIntents` until the first `DestOutputProfile` entry is found, while keeping malformed or image-filtered profile streams as typed fail-closed outcomes. Evidence: `proof_output_intent_profile_scan_skips_unprofiled_entries`.

PostScript exact Type 2 domain/range continuation note: finite function `/Domain` and finite unit `/Range` metadata on direct DeviceRGB, DeviceGray, and DeviceCMYK Type 2 shading functions plus one-component Type 2 function arrays is now preserved in the exact PostScript sidecar and serialized into native `/FunctionType 2` dictionaries, including Type 3 stitching segments. Same-exponent component arrays collapse only when the component domains match and ranges are all absent or exactly representable as a multi-output `/Range`; mixed-domain, mixed-exponent, or partially ranged arrays serialize as exact per-component functions. This closes the source-level whole-page fallback trigger for the supported function metadata subset only; SVG, non-unit shading dictionaries, malformed domains/ranges, out-of-unit ranges, mesh shadings, transparency interactions, corpus validation, and external viewer proof remain incomplete. Evidence: `nonlinear_type2_function_with_range_is_exact_postscript_regional`, `nonlinear_type2_function_with_non_unit_domain_is_exact_postscript_regional`, `nonlinear_device_rgb_type2_function_array_with_ranges_is_exact_postscript_regional`, `nonlinear_device_cmyk_type2_function_array_with_ranges_is_exact_postscript_regional`, `ps_rgb_shading_function_preserves_exact_type2_exponent`, `ps_rgb_shading_function_preserves_exact_type2_component_array`, `ps_cmyk_shading_function_preserves_exact_type2_component_array`, and `ps_cmyk_shading_function_preserves_exact_type3_stitching`.

### Focused local checks run in this continuation

| Command | Exit code |
|---|---:|
| `cargo fmt --all` | 0 |
| `cargo test -p wellfriendpdf-engine transformed_path_clip --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine content_path_clip_replay_reuses_transformed_clip_node_cache --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine clip --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings` | 0 |
| `cargo test -p wellfriendpdf-engine adaptive_tile --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine zero_tile_dimension_selects_adaptive_size --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine tile_origin_participates_in_metadata_culling --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine mark_soft_masks_uses_collected_primary_walk_refs --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine halftone --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_row_encoder_honors_reverse_byte_order --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_honors_non_white_background --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_media_page_box_uses_media_dimensions --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_prepress_page_boxes_use_retained_dimensions --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_honors_device_translation_transform --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine viewport_device_transform_applies_after_page_transform --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine pixel_window_subtracts_tile_origin_after_device_transform --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine "resource_budget" --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_max_decoded_bytes_limits_page_content_stream_decode --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine contract_page_content_stream_decode_observes_cancel_token --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine "transaction" --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine program_cache_keys_include_page_and_contract_identity --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine cached_render_dependencies_include_resource_xobjects --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine narrow_page_invalidation_prunes_page_scoped_artifact_caches --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine render_document_cache_retains_form_xobject_programs --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine fallback_report_keeps_codes_and_structured_policy_details --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine publication_identity --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine reset_refreshes_existing_clones --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine revise_viewport_hint_obsoletes_prior_publications_and_reorders_work --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-capi capi_progressive_revise_viewport_hint_reports_obsolete_publication --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-server progressive_revise_viewport_reports_obsolete_publication --test progressive_integration --jobs 1 -- --test-threads=1` | 0 |
| `cargo check -p wellfriendpdf-server --lib --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-server render_contract --test server_integration --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_capabilities_disclose_fallback_and_progressive_limits --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-server capabilities_endpoint_exposes_renderer_cache_pressure_policy --test server_integration --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine render_contract_telemetry_report_exposes_cache_counters --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-server render_contract_png_report_route_returns_png_and_report_part --test server_integration --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-server render_contract_raw_report_route_returns_surface_and_report_part --test server_integration --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine document_view --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-capi capi_read_only_report_envelopes --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-capi capi_render_report_outputs_owned_json --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo clippy -p wellfriendpdf-engine --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo clippy -p wellfriendpdf-capi --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo check -p wellfriendpdf-py --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-wasm --target wasm32-unknown-unknown --jobs 1` | 0 |
| `cargo clippy -p wellfriendpdf-py --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo clippy -p wellfriendpdf-wasm --all-targets --target wasm32-unknown-unknown --jobs 1 -- -D warnings` | 0 |
| `dotnet build bindings/dotnet/WellfriendPdf.Tests/WellfriendPdf.Tests.csproj --no-restore --nologo /m:1 -v:minimal` | 0 |
| `javac --enable-preview --release 25 -d .work/final-universal-renderer-implementation/java-classes bindings/java/src/main/java/io/wellfriendpdf/WellfriendPdf.java bindings/java/src/test/java/io/wellfriendpdf/WellfriendPdfSmokeTest.java` | 0 |
| `python -m pytest tools/renderer-visual-diff/test_visual_normalization.py -q` | 0 |
| `cargo test -p wellfriendpdf-engine --test regional_vector_fallback -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine ext_gstate_dash_pattern_updates_graphics_state --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine --test regional_vector_fallback svg_output_safe_ext_gstate_stays_vector_and_applies_line_width --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine --test regional_vector_fallback ps_output_safe_ext_gstate_stays_vector_and_applies_line_width --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine render_page_returns_font_substitution_report --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine exact_contract_refuses_generic_bundled_font_substitution --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine exact_contract_refuses_unsupported_display_list_fallback --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine packed_vector_plan_replays_without_raw_content_cold_table --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine text_is_replayable_as_native_operation --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine text_showing_native_op_records_tile_culling_bounds --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine text_showing_bounds_use_pre_advance_text_matrix --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine text_descriptor_compiles_tj_without_raw_content_operation --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine execute_plan_dispatches_text_through_typed_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine inline_image_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine execute_plan_dispatches_inline_image_through_typed_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine image_xobject_native_op_records_tile_culling_bounds --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine form_xobject_native_op_stores_typed_resource_name --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine named_shading_is_replayable_as_native_operation --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine resource_aware_plan_pre_resolves_high_level_handles --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine pattern --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine color_space --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine optional_content --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine annotation_appearance --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine display_list --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine contract --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine rendering_intent --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine color_management_policy --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine deterministic_fallback_policy_disables_icc_backend --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine image_decode_cache_key_includes_render_contract_identity --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine overprint_preview --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine contract_validate_accepts_valid_print_combinations --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine progressive --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine typed_state_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine risk_flags_report_missing_glyph_and_metric_risks --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-capi capi_render_font_substitution_report_outputs_owned_json --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-render-simd --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine separable_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_capabilities_disclose_fallback_and_progressive_limits --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `$env:RUSTFLAGS='-C target-feature=+simd128'; cargo check -p wellfriendpdf-render-simd --target wasm32-unknown-unknown --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-engine --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-engine --lib --jobs 1` | 0 |
| `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings` | 0 |
| `cargo clippy -p wellfriendpdf-engine --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo test -p wellfriendpdf-engine scanline --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine path::tests --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine axis_aligned_polyline_hairline_fast_path_matches_outline_fill --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine integer_axis_aligned_polyline_stroke_fast_path_matches_outline_fill --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine spatial_index_bvh_query_matches_linear_scan_for_large_plan --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine spatial_index_query_into_reuses_output_buffer_for_bvh --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine spatial_index_query_into_reuses_output_buffer_for_grid --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine spatial_index --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine vector_plan_tile_replay_reuses_selection_scratch --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine typed_state_descriptor_has_no_content_operation_in_active_plan --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine unsupported_state_operator_produces_compile_refusal --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine pattern_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine execute_plan_dispatches_pattern_through_typed_descriptor --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine optional_content_marked_content_replays_as_state_ops_without_page_fallback --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine ext_gstate_source_dependency_records_bounded_inline_image_tile --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine color_space_source_dependency_records_text_tile --lib --jobs 1 -- --test-threads=1` | 0 |
| `dotnet build bindings/dotnet/WellfriendPdf.Tests/WellfriendPdf.Tests.csproj --no-restore --nologo /m:1 /p:BaseIntermediateOutputPath=... /p:BaseOutputPath=...` | 1 |
| `dotnet restore bindings/dotnet/WellfriendPdf.Tests/WellfriendPdf.Tests.csproj --nologo` | 0 |
| `dotnet build bindings/dotnet/WellfriendPdf.Tests/WellfriendPdf.Tests.csproj --no-restore --nologo /m:1` | 0 |
| `javac --release 25 -d .work/final-universal-renderer-implementation/java-classes bindings/java/src/main/java/io/wellfriendpdf/WellfriendPdf.java bindings/java/src/test/java/io/wellfriendpdf/WellfriendPdfSmokeTest.java` | 0 |
| `cargo check -p wellfriendpdf-py --all-targets --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-wasm --all-targets --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-wasm --target wasm32-unknown-unknown --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-py --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-capi capi_render_report_outputs_owned_json --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo clippy -p wellfriendpdf-capi --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo clippy -p wellfriendpdf-py --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo clippy -p wellfriendpdf-wasm --all-targets --target wasm32-unknown-unknown --jobs 1 -- -D warnings` | 0 |
| `dotnet build bindings/dotnet/WellfriendPdf.Tests/WellfriendPdf.Tests.csproj --no-restore --nologo /m:1 -v:minimal` | 0 |
| `javac --enable-preview --release 25 -d .work/final-universal-renderer-implementation/java-classes bindings/java/src/main/java/io/wellfriendpdf/WellfriendPdf.java bindings/java/src/test/java/io/wellfriendpdf/WellfriendPdfSmokeTest.java` | 0 |
| `cargo test -p wellfriendpdf-engine materialize_window_composite_short_circuits_empty_child_window --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine restore_clip_from_dag_node_preserves_unmaterialized_saved_state --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine dense_intersection_records_composite_metadata --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine q_restore_restores_previous_clip_mask --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine blend_alpha_mask_fuses_partial_clip_and_smask_into_row_path --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_row_encoder --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine contract_row_encoder_honors_alpha_modes_for_rgba_bgra --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine render_document_cache_enforces_aggregate_resource_budget --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine cache_evicts_least_recently_used_entry_by_byte_budget --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_outside_viewport_skips_decode_scheduler --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine tile_origin_participates_in_metadata_culling --lib --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-server --test progressive_integration --jobs 1` | 0 |
| `cargo test -p wellfriendpdf-cli render_contract_media_page_box_uses_media_dimensions --test tool_surface --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-cli render_contract_trim_page_box_uses_trim_dimensions --test tool_surface --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-cli render_contract_device_transform_shifts_raw_surface --test tool_surface --jobs 1 -- --test-threads=1` | 0 |
| `cargo test -p wellfriendpdf-engine pattern_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine xobject_resource_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine --test patterns --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine shading_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine malformed_active_shading_dictionary_returns_typed_refusal --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine shading --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine sample_short_buffer_is_transparent_not_black --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine sample_unsupported_channels_is_transparent_not_black --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine image_painter --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_missing_width --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_zero_height --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_missing_bits_per_component --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_unsupported_bits_per_component --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_missing_color_space --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_inline_image_mask_without_color_space --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine inline_image --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_image_xobject_missing_width --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine renderer_image_xobject_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine image_xobject --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine image_xobject_explicit_mask_missing --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine image_xobject_smask_missing --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine smask_ --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine uncolored_tiling_pattern_invalid_component_count --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine tiling_pattern --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine type3_path_collector_rejects --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine type3_clipping_charproc_malformed_line_width_returns_typed_refusal --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine type3 --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine separable_difference_exclusion_fill_rect_uses_wide_row_path --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-engine exclusion --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-render-simd separable_blend --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `cargo test -p wellfriendpdf-render-simd public_kernels_match_scalar_or_cleanly_decline_unaligned_rows --lib --jobs 1 -- --test-threads=1 --nocapture` | 0 |
| `$env:RUSTFLAGS='-C target-feature=+simd128'; cargo check -p wellfriendpdf-render-simd --target wasm32-unknown-unknown --jobs 1` | 0 |
| `cargo check -p wellfriendpdf-render-simd --all-targets --jobs 1` | 0 |
| `cargo clippy -p wellfriendpdf-render-simd --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo fmt --all --check` | 0 |
| `cargo check -p wellfriendpdf-engine --all-targets --jobs 1` | 0 |
| `cargo clippy -p wellfriendpdf-engine --all-targets --jobs 1 -- -D warnings` | 0 |
| `cargo check --workspace --all-targets --jobs 1` | 0 |
| `cargo clippy --workspace --all-targets --jobs 1 -- -D warnings` | 0 |
| `[historical] cargo check --workspace --all-features --all-targets --jobs 1` | 0 |
| `[historical] cargo clippy --workspace --all-features --all-targets --jobs 1 -- -D warnings` | 0 |
| `rg` source guard for PDFium harness required switches/manifest fields | 0 |
| `cl /std:c11 /W4 /WX /I .work\final-universal-renderer-implementation\pdfium-stub\public /c tools\pdfium-harness\render_page.c` | 0 |

SVG/PS regional order/bounds continuation note: regional Image XObject and inline-image SVG/PostScript output now carries inert kind and bounds metadata, and focused Image XObject plus inline-image tests assert the expected bounds plus vector-before/raster/vector-after order. Simple linear Type 2 vector shadings now also accept contained non-unit shading `/Domain` intervals by sampling native SVG/PS gradient endpoints at the domain endpoints; the focused synthetic gates cover axial SVG/PS output and the fallback boundary for domains outside the function domain. Accepted simple vector shadings now also preserve non-degenerate shading `/BBox` as a native SVG clipPath or PostScript clip around direct `shfill` and shading-pattern replay. This does not close remaining RB-14 unsupported constructs or final visual parity.

SVG shading BBox clip continuation note: SVG native shading, shading-pattern fill/stroke, and pattern-painted stencil replay now returns a typed unsupported error if the accepted shading `/BBox` cannot be materialized as a finite device-space clip. The previous replay path converted that failure into an empty clip and could silently omit paint. This does not add new native mesh, nonlinear shading, transparency, pattern, corpus, or benchmark coverage.

Overprint CMYK state exactness continuation note: active overprint-preview dispatch now requires an internal `DeviceCMYK` color state to carry exactly four finite components before using the CMYK preview painter. Malformed helper inputs decline the preview path instead of synthesizing missing inks as zero. Valid PDF color operators remain covered by the existing operand validators and valid CMYK preview behavior is unchanged.

Synthesized/Type3 DeviceCMYK continuation note: synthesized annotation `/C`, widget `/MK /BG`/`/MK /BC`, widget `/DA k`, and Type 3 device-color pixel paths now use `render/cmm.rs::device_cmyk_to_srgb` after finite unit normalization. The previous page-renderer-local algebraic CMYK preview conversion is gone from those paths. Evidence: `synthesized_annotation_cmyk_uses_shared_device_cmyk_fallback`.

Progressive image-decode terminal-state continuation note: `ProgressiveImageDecodeSession::continue_decode` now preserves terminal `Completed`, `Cancelled`, `Failed`, and `Closed` states before evaluating native-progressive capability, and terminal reports suppress `full_decode_required`. `ProgressiveImageDecodeReport` carries a typed release reason; `ProgressiveImageDecodeSession::fail` releases retained state for render failures, and `ProgressiveImageDecodeSession::close_for_document_close` releases retained state with a distinct document-close reason. The SDK JSON lifecycle envelope accepts `fail` and `document_close` actions so render/decode failure cleanup and owning-document cleanup are represented through the same binding-facing source surface. This prevents future native progressive image decoders from scheduling work or reporting completion after cancellation, failure, session close, or document close. Evidence: `progressive_image_decode_continue_preserves_terminal_states`, `progressive_image_decode_document_close_releases_state`, `progressive_image_decode_pause_resume_cancel_close_release_state`, `progressive_image_decode_lifecycle_report_envelope`, and `capi_progressive_image_decode_lifecycle_report_json`.

Retained-list refusal continuation note: full-page direct, cached, contract, display-list helper, direct display-list replay, cached display-list replay, retained tile, public tile helper, band, progressive, and CLI `render-corpus --pipeline display-list` paths now treat unsupported retained display-list replay as a typed failure in compatibility mode instead of publishing canonical raw/immediate-rendered output. Progressive jobs transition to `Failed` and record no fallback publication; render-corpus records a failed file/page and keeps `display_list_fallbacks` at zero. Evidence: `high_quality_display_list_page_helpers_refuse_unsupported_list`, `high_quality_display_list_tile_refuses_unsupported_list`, `compat_display_list_bands_refuse_unsupported_list_without_immediate_fallback`, `compat_progressive_refuses_unsupported_display_list_tile_without_immediate_fallback`, `high_quality_progressive_refuses_unsupported_display_list_tile`, and `render_corpus_display_list_refuses_unsupported_retained_replay`.

Annotation display-text exactness continuation note: synthesized annotation/widget display text now rejects non-finite internal real values before generated appearance text serialization, and the shared non-strict display helper no longer partially filters malformed array members while keeping earlier renderable text. Explicit `/Off` remains the accepted empty state. This is a source-level helper-boundary closure only; it does not add corpus validation or external runtime parity.

Type 4 calculator output exactness continuation note: PDF Function Type 4 numeric operators now require finite numeric operands, `if`/`ifelse` require boolean conditions, and final outputs must be the top numeric stack values. Final booleans are no longer coerced to `0`/`1`, and final procedures are no longer filtered while earlier numeric values are kept. This closes malformed calculator function coercion for shading, tint-transform, and transfer-function consumers without adding new function operators.

Type 2 component-function array continuation note: simple SVG/PS vector shadings now accept `/Function` arrays when every element resolves to a linear Type 2 component function and the concatenated endpoint samples form a supported color vector. The focused synthetic evidence covers axial SVG/PostScript output only; malformed arrays, non-linear functions, clipped-outside-function-domain cases, mesh shadings, transparency, advanced patterns, and final visual parity remain incomplete.

PostScript exact shading-function continuation note: direct DeviceRGB/DeviceGray axial/radial shadings with a single finite Type 2 function remain native for PostScript/EPS by carrying the exact `/Domain`, `/N` exponent, and finite unit `/Range` metadata into a LanguageLevel 3 `/FunctionType 2` `shfill`; direct DeviceCMYK axial/radial shadings with a single finite Type 2 function now remain native as `/ColorSpace /DeviceCMYK` `shfill` with exact `/Domain`, `/C0`, `/C1`, `/N`, and finite unit `/Range`; direct DeviceRGB arrays of one-component Type 2 functions collapse to one exact RGB Type 2 sidecar when every component shares the same finite domain/exponent and the component ranges are all absent or exactly representable as a multi-output `/Range`, and otherwise serialize as exact per-component function arrays preserving finite component domains and ranges; direct DeviceCMYK arrays of one-component Type 2 functions do the same for `/ColorSpace /DeviceCMYK`; direct DeviceRGB/DeviceGray, Type 3 stitching functions whose Type 2 segments can be represented exactly as RGB now carry a native `/FunctionType 3` sidecar, preserving finite outer domains, segment `/N` exponents, and finite Type 2 domain/range metadata as well. SVG and calibrated/named, non-unit shading dictionaries, out-of-unit or malformed ranges, malformed domains, mesh, malformed, or otherwise unsupported nonlinear shadings remain fail-closed instead of being approximated.

PostScript exact DeviceCMYK stitching continuation note: direct `/DeviceCMYK` Type 3 stitching functions whose Type 2 segments are finite, unit-domain CMYK functions now carry native `/ColorSpace /DeviceCMYK` `/FunctionType 3` sidecars for PostScript/EPS, preserving non-linear segment exponents exactly. SVG and inexact/non-unit-domain stitching segments remain conservative.

SVG/PS shading-pattern stencil-mask continuation note: SVG regional fallback now accepts simple shading-pattern painted Image XObject and inline-image stencil masks by emitting a decoded SVG mask plus the existing native gradient fill, preserving the active clip and accepted shading BBox clip. PostScript now accepts bounded versions of the same shape by vectorizing painted stencil cells into a native clip path before replaying shading-pattern `shfill`; larger masks stay conservative under the vectorized-cell cap.

SVG/PS colored/uncolored tiling-pattern continuation note: simple colored and uncolored `/PatternType 1` path fills/strokes and pattern-painted inline/Image XObject stencil masks now have a guarded regional-vector subset for SVG/PostScript when the stream decodes completely, geometry metadata is exact and finite, `/PaintType` is 1 or 2, tile steps are positive, and the tile program is pure vector or contains scoped vector-safe image/Form/shading/inline-image resources without nested pattern paint. `/PaintType 2` additionally requires finite caller DeviceGray, DeviceRGB, or DeviceCMYK components and rejects tile streams that set paint color themselves. The sinks replay bounded visible tiles as native vector paths under `MAX_VECTOR_TILING_PATTERN_CELLS`, using a decoded SVG mask or vectorized PostScript stencil-cell clip for stencil masks. Nested pattern-paint, color-setting uncolored, negative-step, over-cap, malformed, transparency-bearing, resource programs outside the vector-safe image/Form/shading/inline-image subset, and advanced tiling-pattern cases remain incomplete.

Image filter-chain capability exactness continuation note: image decode capability planning now treats an image filter chain containing any unknown filter as `UnknownCodec`, even when a later terminal filter is recognized. Unknown wrappers therefore cannot inherit CCITT/raw/JPEG source-window or reduction capability claims. Valid fully known CCITT chains still advertise the guarded source-window path; no new decoder-native ROI/reduction/progressive API support is claimed.

Active shading Domain sample exactness continuation note: active shading function sample-input generation now parses present `/Domain` arrays through `strict_shading_float_array` and returns typed malformed-Domain errors instead of accepting numeric prefixes or defaulting sample inputs from a filtered array. This hardens the active shading validation path only; mesh/nonlinear shading coverage, corpus validation, and benchmark evidence remain outside this increment.

Visual font metric exactness continuation note: `text_decode.rs` now calls the render-facing font metric validator before visual glyph placement. Simple font `/Widths` and CID `/DW`/`/W`/`/DW2`/`/W2` metadata must be exact and finite where present, so malformed width arrays and vertical metric triples cannot be filtered or defaulted into text advance output. This preserves Standard 14 and absent spec default behavior while closing malformed active metric paths.

SVG/PS regional Image XObject color-space preflight continuation note: `vector_fallback.rs` now resolves non-mask Image XObject `/ColorSpace` metadata through page resources and checks the resolved space with the same support predicate used for regional inline images before classifying a bounded SVG/PostScript image region. Unsupported direct names, unsupported resource aliases, and malformed color-space arrays now stay on the whole-page typed raster/refusal boundary. Focused SVG and PostScript resource-named `/DeviceRGB` tests still pass.

SVG/PS tiling-pattern text continuation note: simple colored and uncolored `/PatternType 1` glyph-outline text fills/strokes now have the same bounded regional-vector subset for SVG/PostScript. The sinks replay bounded visible tiles through glyph fill/stroke clips before falling back to supported shading-pattern text replay. Patterned text outside the supported shading/tiling glyph-outline subset remains incomplete.

SVG/PS vector text metric-source continuation note: SVG/PostScript vector text now advances only from PDF widths, strict sfnt/bare-CFF font-program metrics, or valid vertical CMap metrics. Ordinary text without a reader-backed metric preflight is classified to the typed whole-page raster/refusal path, and the sinks return typed output errors rather than native output with synthesized `500` horizontal or `-1000` vertical advances. Visible and clipping SVG/PS glyph-outline emission now also requires a real mapped glyph id from the font program instead of drawing a code-point-derived compatibility glyph when PDF widths exist. This is a source-level fail-closed correction, not corpus fidelity or broader font-shaping proof.

Exact raster text metric-source continuation note: high-quality/exact raster text now requires PDF widths, native Type 3 metrics, or strict variation-aware sfnt/bare-CFF font advances before ordinary glyph movement. Metricless exact glyph movement records a typed `HighQualityExact` refusal instead of using the compatibility `500` horizontal default, and vertical writing requires an explicit vertical advance instead of `-1000`.

Deterministic schedule worker-local cache continuation note: progressive rendering now has a scoped two-worker parity test that runs different quantum schedules concurrently and checks byte-identical output, publication identity, and tile order. Display-list band replay also has synthetic parity tests that alternate bands across two separate worker-local `RenderDocumentCache` instances and run a scoped two-worker concurrent band render with separate worker-local caches, then stitch the bands back to the full display-list render. These cover focused source worker cases only; broader server/external runtime matrices remain incomplete.

Packed state-resource descriptor continuation note: resource-aware packed state compilation now emits typed refusals for missing `/ExtGState`, `/Font`, named non-device color-space, pattern color, and marked-content `/Properties` resources, including retained-text `/Tf` descriptors. Resource-free debug compilation remains permissive, but active resource-aware plans no longer retain those names for runtime lookup or allow vector replay to skip the refusal descriptor.

Type 3 retained exactness continuation note: `render_type3_charproc_full_with_ctm` now uses the retained CharProc compile refusal reason as the Type 3 boundary in both compatibility and high-quality/exact rendering. Unsupported nested CharProc content records a typed retained replay error instead of dispatching through immediate rendering, and full CharProc replay returns failure when retained compile or execute records that fatal boundary. Evidence: `compatibility_type3_charproc_refuses_unsupported_retained_replay`, `high_quality_type3_charproc_refuses_unsupported_retained_replay`, and `type3` focused gates.

Type 3 parsed-program lifecycle continuation note: parsed Type 3 CharProc and geometry caches now store explicit `Unvisited`, `Compiling`, `Compiled`, and `Failed` states instead of representing lifecycle solely through entry absence or `Option<Arc<_>>`. Cache misses mark the program as compiling before collection, then commit either a compiled program or a retained failure reason. Evidence: `type3_program_cache_tracks_explicit_lifecycle_states`, `type3_program_cache`, and `type3` focused gates.

Type 3 source-identity invalidation continuation note: active parsed CharProc, retained CharProc subplan, Type 3 geometry, Type 3 device-mask, and coloured rendered-glyph cache keys now include canonical document revision plus indirect `type3-program` source markers where available. Source-scoped transaction invalidation prunes each matching Type 3 cache family, so a changed indirect CharProc cannot leave stale parsed programs, masks, rendered coloured glyphs, geometry, or retained subplans behind. Evidence: `type3_charproc_render_cache_key_includes_revision_and_source_marker` and `type3_source_pruning_removes_program_mask_rendered_and_retained_entries`.

Retained subprogram exactness continuation note: cached Form XObject, transparency Form group, annotation appearance, tiling-pattern programs, and soft-mask `/G` Form replay now keep or compute retained-plan refusal reasons and apply them at replay. High-quality/exact and compatibility rendering refuse unsupported retained subprogram replay before raw immediate dispatch. Evidence: `unsupported_retained` focused gates.

SMask cache identity continuation note: `smask_group_cache_key` now includes structural clip-shape fingerprints and explicit color-management/overprint policies. Same-bounds different-shape clips, CMM policy changes, and overprint policy changes now produce distinct soft-mask cache identities. Evidence: `smask_group_cache_key_includes_contract_output_identity`.

AcroForm widget invalidation continuation note: DocumentSubsystems FormData actions now return `document-subsystems-render-invalidation.v1` with structured field/widget/AP write-set refs, created/changed/removed refs, affected pages, dirty widget rectangles for live appearance changes, and value/default deltas. Secure-mutation incremental form value updates and signature-preserving form fills now return `secure-mutation-incremental-form-render-invalidation.v1` with field/widget/AP write-set refs, affected pages, dirty rectangles, and before/after values. Progressive C/Python/WASM/.NET/Java handles apply render-invalidation plans to their owned caches, and non-progressive C/Python/WASM/.NET/Java source surfaces now expose caller-owned render cache handles for contract PNG/render-report paths. Evidence: `canonical_form_action_reopens_and_exposes_inverse_proof`, `document_subsystems`, and `secure_mutation_closeout_advanced_secure_mutation`.

Progressive binding/server cache-host invalidation continuation note: server-owned progressive sessions now accept render-invalidation plan JSON through `/api/v1/progressive/:id/apply-render-invalidation`, and C/Python/WASM/.NET/Java progressive handles expose the same cache mutation; both paths apply the plan to the retained session `RenderDocumentCache`, clear affected current-page retained tile buffers, mint a new publication identity, and report obsolete tile-publication identities. This closes direct shared-cache mutation for progressive server and binding hosts; shared-resource transitive write sets, conservative resource-dependency narrowing, and external cache-handle runtime matrix validation remain incomplete. Evidence: `progressive_apply_render_invalidation_obsoletes_retained_tile_publication`, `capi_progressive_apply_render_invalidation_plan_obsoletes_retained_tile`, .NET build, and Java source compile.

Transaction write-set binding alias continuation note: `RenderInvalidationCachePlan` envelope parsing now accepts binding/server camelCase and separator variants for nested render write-set fields, structured PDF object-reference keys, direct cache-plan fields, source-cache marker fields, affected-tile fields, and `report.renderInvalidation` envelopes. This maps `changedFontRefs`, `changedOutputIntentRefs`, `changedPageTreeRefs`, `changedCatalogRefs`, `changedStructTreeRefs`, `changedTransparencyGroupRefs`, `changedImageSmaskRefs`, `renderWriteSetRefs`, `fontObject`, `fontGeneration`, `objectRef`, `sourceRef`, `schemaVersion`, `nextRevision`, `mappedSourceIds`, `sourceCacheMarkers`, `sourceId`, `objectNumber`, `generationNumber`, `affectedPages`, `affectedTiles`, `pageNumber`, and `conservativeResetRequired` to canonical plan state before cache invalidation, reducing avoidable decode failures or conservative resets for non-Rust clients while preserving the reset boundary for unmapped refs. The public `TransactionWriteSet` serde path also accepts binding-style affected object/page/tile/revision fields and both tuple/object affected-tile shapes, and dirty-region tile conversion accepts binding-style page/bounds aliases such as `pageNumber` and `dirtyRegion`. Evidence: `nested_write_set_collector_accepts_camel_case_binding_refs`, `nested_write_set_collector_accepts_document_structure_and_group_aliases`, `render_invalidation_plan_json_accepts_camel_case_plan_fields`, `render_invalidation_plan_json_accepts_camel_case_envelope_and_tile_fields`, `transaction_write_set_json_accepts_camel_case_fields_and_tile_objects`, `dirty_regions_accept_camel_case_page_and_bounds_aliases`, `transaction_invalidation`, `nested_write_set_collector`, `render_invalidation_plan_json_`, engine check, and engine clippy.

Non-progressive render cache-handle continuation note: ordinary contract rendering now has caller-owned `RenderDocumentCache` handles outside progressive sessions. Rust engine contract PNG/render-report APIs accept a mutable cache, the C ABI/header expose `WellfriendRenderCache` creation, clearing, render-invalidation-plan application, and cached contract PNG/report entry points, and Python, WASM/TypeScript, .NET, and Java expose matching cache-owner surfaces. Render-report telemetry uses `scope: caller_owned_render_cache_report` on these paths. This closes the source-level non-progressive binding cache-handle adoption item; broader shared-resource transitive write-set matrices, safe narrowing of conservative page-level dependencies, and external runtime matrices remain incomplete. Evidence: `capi_render_cache_handle_renders_and_applies_invalidation_plan`, Python/WASM cargo checks, .NET build, Java source compile, and workspace check/clippy gates.

Nested render write-set cache-plan continuation note: cache-owning callers can now pass direct plans or full SDK/server/report envelopes to `apply_render_invalidation_plan_json_to_cache`; the applier scans nested render write-set fields, maps PDF refs, structured `[object, generation]` arrays, `{object_number, generation}` objects, `{ref}`/`{object_ref}`/`{source_ref}` records, and `object-*`/`stream-*` refs through `RenderDocumentCache`'s remembered object identities, merges the resulting source IDs and source-cache marker entries, and conservatively resets on unmapped nested refs. This extends the existing DocumentSubsystems and secure-mutation field/widget/AP invalidation reports to exact raster/source-artifact eviction when a cache owner receives the full report envelope. Broader arbitrary shared-resource write-set matrices and external runtime matrices remain incomplete. Evidence: `cargo test -p wellfriendpdf-engine transaction_invalidation --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo check -p wellfriendpdf-engine --lib --jobs 1`, and `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`.

Image SMask source-window continuation note: guarded axis-aligned unfiltered raw Image XObjects and monochrome CCITT Image XObjects with same-sized unfiltered grayscale `/SMask` streams now keep `SubRect` cache identity and crop the soft mask through the same source window before alpha combine. The previous broad SMask full-image boundary now applies to filtered, mismatched, non-grayscale, non-CCITT/non-raw, reduced, non-axis, or otherwise postprocessed SMask cases. Evidence: `cargo test -p wellfriendpdf-engine ccitt_smask_plan_uses_windowed_source_region_when_crop_aligned --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine image_smask_shared_source_window_allows_guarded_ccitt_main_image --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine smask_loader_crops_source_window_for_ccitt_main_image --lib --jobs 1 -- --test-threads=1 --nocapture`, plus the earlier raw SMask `image_decode_planning` and `smask` focused gates.

Transitive retained resource-op tile dependency continuation note: retained tile dependency recording now walks bounded transitive PDF source refs for each intersecting XObject/Form, named-shading, and active state-resource object before source-id mapping. The focused fixtures prove exact tile invalidation for nested Form image resources, font descriptor refs, color-space functions, ExtGState SMask Form refs, and pattern cell resources. Source-only mapped transactions and render-invalidation plans with no reported affected pages now enter the page-artifacts-plus-exact-tiles path, preserving exact source tiles when recorded coverage is complete and expanding source-derived pages when coverage is incomplete. Broader transaction write-set classification across arbitrary shared resources and remaining conservative page-level narrowing remain incomplete. Evidence: `transitive_`, `source_dependency`, `bounded_form_xobject`, `transaction_invalidation`, and `exact_tile_mode` focused gates.

JPX alias decode-planning/vector-fallback continuation note: `/JPX` is now treated as the same terminal codec as `/JPXDecode` in image decode planning, lossless stream image-filter stopping, terminal decoder dispatch, inline target-resolution dispatch, DecodeParms filter matching, codec-isolation backend selection/reporting, and SVG/PostScript vector-fallback image filter classification. This keeps guarded target-resolution reduction, exact capability/backend reporting, and regional vector image handling active for abbreviation-bearing JPX images instead of falling into false `UnknownCodec`, unsupported-codec registry, or whole-page vector fallback paths. JPX ROI/region/progressive output remains incomplete. Evidence: `jpx_abbreviation`, `image_decode_planning`, and `jpx` focused gates.

Image decode component-selection capability continuation note: component-subset image decode requests are now part of the source capability and exact-refusal model instead of only changing cache identity. `ImageDecodePlan` records `requires_component_decode`, `ImageDecodeCapabilityReport` exposes `component_decode`, exact-mode limitation summaries include `component-selection`, and the document-level capability JSON reports `native_component_decode_count`. The unfiltered raw adapter now implements native component-selected source-window output for XObjects and inline images across 1/2/4/8/16-bit samples, including full-window raw component requests and `/Decode` application by original source component index. Known source shapes normalize explicit all-component selections so they do not require native subset support, invalid known raw component indexes fail exact planning instead of being advertised as native, and per-image capability reporting marks shape-supported raw plus monochrome CCITT source-window and trivial single-component support as native instead of staying filter-only conservative. JPEG/JPX/JBIG2 and filtered-lossless adapters still report unavailable native component-subset decode; CCITT reports the trivial single-component selection as native for 1 bpc DeviceGray/ImageMask shapes; JPX ROI/region/tile/progressive pixel output remains incomplete. Evidence: `component_selection`, `raw_component`, `image_decode_planning`, `component_subset_plan_fails_exact_when_native_component_decode_is_unavailable`, and `image_decode_capability_report_lists_images_without_decoding`.

Image decode codestream-tile capability continuation note: codec-native tile decode is now a first-class image capability rather than an implicit JPX limitation. `ImageDecodeCapabilityReport` exposes `tile_decode`, the document-level capability JSON reports `native_tile_decode_count`, and JPEG/JPX plans report `CodestreamTileDecodeUnavailable` when the selected decoder API cannot return codestream tiles natively. This does not claim JPX codestream tile pixel output; it makes the unsupported capability explicit beside region, reduction, progressive, and component decode. Evidence: `jpx_plan_reports_full_decode_only_capability`, `jpx_downscale_plan_reports_native_reduction_capability`, `image_decode_capability_report_lists_images_without_decoding`, and `image_decode_planning`.

Image decode CCITT terminal-shape exactness continuation note: CCITT source-window capability reporting now checks the same terminal monochrome shape that the decoder requires, and runtime capability text names that boundary. `render/image_decode_planning.rs` advertises native CCITT region decode only for 1 bpc DeviceGray/G or ImageMask shapes; CCITT streams declared as RGB/CMYK report `MonochromeTerminalShapeUnsupported`, keep full-region cache identity, and trigger exact-mode source-region refusal instead of planning a native partial decode that cannot execute. Evidence: `ccitt_plan_does_not_advertise_window_decode_for_non_monochrome_shape`, `ccitt_image_reference_capability_reports_non_monochrome_shape`, `image_decode_planning`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

Image decode render-contract identity continuation note: decoded-image planner cache keys now include active render-contract identity as an explicit `:contract:` fragment, and active inline-image/Image XObject planning passes the `RenderState` render-contract fingerprint through `ImageDecodePlanIdentity`. This closes a revision/output-policy cache collision boundary without changing codec capability claims. JPEG/JPX/JBIG2 region and progressive pixel output remain reported unavailable where the selected decoder APIs cannot provide them. Evidence: `cache_key_includes_render_contract_identity`, engine check, engine clippy, and workspace check.

Visual normalization pixel-policy continuation note: the compact renderer visual-diff harness now applies explicit render-context RGBA background compositing, grayscale luma conversion, exact post-rotation dimension validation, and packed raw byte-order normalization to the canonical comparison surface. It also canonicalizes structured font-environment sidecars into stable strings plus SHA-256 fingerprints so equivalent manifests do not create false font-fallback classifications while genuine fallback/provider changes remain classified. The manifest documents these active policy steps, and per-side normalization/context reports carry the expected size, applied byte order, applied background/grayscale flags, and `font_environment_fingerprint`. This closes the harness-side source gap where those fields were only policy metadata; real corpus comparison, profile adjudication, and human visual review remain deferred. Evidence: `python -m pytest tools/renderer-visual-diff/test_visual_normalization.py -q`, `python -m py_compile tools/renderer-visual-diff/visual_diff.py`, and `python -m json.tool tools/renderer-visual-diff/normalization-manifest.json`.

Render-contract color-scheme continuation note: the active CPU contract path now
implements `ColorScheme::Dark` as deterministic RGB inversion and
`ColorScheme::ForcedMonochrome` as deterministic contract-luma conversion over
the canonical RGBA surface, both preserving alpha before canonical return or
caller-owned row encoding. Unsupported semantic policies are still refused, but
the refusal now names the exact unsupported fields instead of using a generic
semantic-policy bucket. Subpixel smoothing execution is active for accepted CPU
contracts; `ResearchHybrid` backend selection is now active through the guarded
CPU hybrid dispatcher, while separation-preserving output remains unsupported
source work.

Retained display-list raw-operation storage continuation note: retained
display-list compilation no longer keeps cloned raw `ContentOperation` values
for path assembly or pending inline-image state. It tracks path assembly with a
typed count, stores only inline-image parameter operands while waiting for the
data payload, and accounts typed retained payloads directly instead of building
temporary raw operation vectors. Retained replay payloads remain typed
`DisplayOp` variants; parser-level and SVG/PostScript sink `ContentOperation`
boundaries remain separate source/sink concerns. Evidence: the `native` focused
gate, engine library check, engine library/test clippy, format check, and the
display-list source guard for removed raw-operation storage patterns.

Image capability full-decode-only continuation note: static image capability
reports now use an explicit full-decode-only classifier instead of treating
every full-source/full-resolution capability report as incapable. Raw and CCITT
image references with native region or component capability are therefore not
counted as full-decode-only in document reports, while progressive image decode
sessions still report `full_decode_required` for nonterminal full-source work
without native progressive continuation. Evidence:
`image_decode_capability_report`,
`progressive_full_raw_plan_reports_full_decode_required_even_with_window_capability`,
`progressive_image_decode_session_reports_full_decode_required`, and the
`native` focused gate.

Image DecodeParms cache-identity continuation note: decoded-image cache identity
now includes stable filter-parameter semantics. `ImageMetadata`,
`ImageContractState`, and `ImageDecodeCacheKey::to_cache_string` carry a
`decode_params_fingerprint`; active inline-image planning fingerprints
normalized inline `/DecodeParms` operands, and Image XObject planning
fingerprints `/DecodeParms` or `/DP` from the stream dictionary. Otherwise
identical image payload/filter/base identities with different predictor, CCITT,
JBIG2, or other filter-parameter semantics therefore cannot share decoded
pixels. Evidence: `cache_key_includes_decode_params`,
`cache_key_differs_for_decode_params`, the `image_decode_planning` focused gate,
engine check, and engine clippy.

SIMD clip/soft-mask alpha-fusion continuation note: `render-simd/src/lib.rs`
now exposes `multiply_alpha_rows` with guarded native x86_64 AVX2,
x86/x86_64 SSE2, and
wasm32 `simd128` lanes for `round(alpha * mask / 255)` row products.
`render/buffer.rs::AlphaMask::fused_clip_window` now uses that helper for wide
bounded clip/soft-mask windows after materializing the exact local clip row, and
`runtime.rs` reports `clip_mask_fusion_rows` in `cpu_simd_compositor`. Evidence:
`public_kernels_match_scalar_or_cleanly_decline_unaligned_rows`,
`alpha_mask_fused_clip_window_wide_row_matches_scalar_product`, runtime
capability, render-simd check/clippy, and engine check/clippy.

SIMD direct paint fusion continuation note: direct image/glyph alpha-mask,
solid-fill, and RGBA fragment paint rows now reuse the same public
alpha-product helper for SMask and partial-clip opacity products before
dispatching to the existing row compositors. `blend_alpha_mask_rows_normal_fused`,
`blend_solid_rows_normal_fused`, and `blend_rgba_rows_normal_fused` reuse
bounded row scratch, materialize only the needed destination-local SMask/clip
factor rows, and fall back to scalar multiplication when SIMD is declined.
Runtime capabilities disclose `image_glyph_mask_clip_smask_fusion_rows` and
`solid_rgba_clip_smask_fusion_rows`. Evidence:
`paint_fusion_wide_row_matches_scalar_product`,
`fuses_partial_clip_and_smask_into_row_path`,
`renderer_capabilities_disclose_fallback_and_progressive_limits`, engine check,
and engine clippy.

Transaction display/proof/prepress alias continuation note:
`render/transaction_invalidation.rs` now recognizes additional nested write-set
aliases for display/print/proof OutputIntent refs, display/proof/proofing
profiles, color-management profile and policy refs, CMM profiles, Separation
profiles, DeviceN profile refs, rendering intent, transfer functions,
halftone-screen resources, overprint state/mode, prepress policy, prepress
plates, ink/spot color, Separation/DeviceN plates, black-point compensation,
black generation, undercolor removal, trapping, and trap networks. The collector
accepts both explicit `changed_*_refs` fields and matching structured
object/generation key pairs in binding/server/report envelopes. Known refs map
through the same source-cache marker path as other render write-set refs, while
unknown refs still force the conservative reset boundary. Evidence:
`nested_write_set_collector_accepts_display_proof_profile_aliases`,
`nested_write_set_collector_accepts_prepress_overprint_aliases`,
`transaction_invalidation`, engine check, and engine clippy.

### Superseded local continuation verdict

`IMPLEMENTATION_INCOMPLETE`

That checkpoint preceded the later packed-plan, dependency, clip-DAG,
print/prepress, progressive queue/revision, image-planning, SIMD, regional
vector, and contract-field source slices recorded in the implementation journal.
Those later slices supersede its blocker list.

### 2026-09-07 final source verdict

All currently known technically actionable renderer implementation gaps are
closed in source. Remaining work is the separate corpus, benchmark,
cross-platform runtime, comparator, and human visual-verification campaign.

`IMPLEMENTATION_COMPLETE_AWAITING_FINAL_VERIFICATION`
