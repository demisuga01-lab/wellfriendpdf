# Final remaining renderer blocker preaudit

**Repository:** `E:\wellpdfsdk`
**Starting commit:** `bfc28b7a894f77623e75daf39e73f5a721cf01d5`
**Scope:** remaining source-backed renderer closure work after the universal revalidation. This is a preimplementation inventory, not a readiness claim and not benchmark evidence.

## Method

Inspected the committed implementation/fallback inventories, current renderer source, active entry points, binding code, and current fallback markers. The goal of this document is to bind each remaining blocker to an implementation, test, and public-surface requirement before source work begins.

| ID | Subsystem | Exact source locations / active path | Current behavior / output degradation | Missing implementation | Bindings affected | Tests missing | Completion evidence required |
|---|---|---|---|---|---|---|---|
| RB-01 | Universal packed high-level plans | `render/display_list.rs:DisplayOp`, `render/plan.rs:PackedDisplayList::compile_with_resources`, `RenderPlan::execute_full`, `RenderPlan::execute_vector_tile`; `page_renderer.rs:render_packed_vector_plan` | Default, cached, contract full-page, retained tile, direct tile, and progressive supported-list rendering now execute through packed plans; typed descriptors pre-resolve covered image/Form/shading/ExtGState/font/color-space/pattern/Properties resources and propagate caller cancellation into descriptor replay; retained state display ops now store typed `GraphicsStateDescriptor` payloads; retained stateful pattern paths store typed `PatternPathDescriptor` payloads; retained text/text-state display ops store typed `RetainedTextOp` payloads; retained inline images store typed parameter/data payloads; retained image XObject, Form XObject, and shading display ops store typed resource names instead of cloned raw `Do`/`sh` operations; packed cold tables are diagnostics-only and no longer expose raw `ContentOperation` payload variants; BMC/BDC/EMC marked-content boundaries force packed adapter replay so optional-content visibility suppresses vector clip/fill/stroke, including inline `/OC` dictionaries; supported Form XObject streams, including transparency-group interiors, cache parent-resource-scoped retained plans and replay through the packed adapter; supported tiling-pattern streams cache resource-scoped retained plans and replay through a tile-CTM-aware packed adapter; supported annotation appearance streams cache retained plans and replay through the packed adapter; Type 3 CharProcs with clips, typed resources, `d0`/`d1` metrics, and vector paths with per-paint inherited/explicit color handling can replay through guarded retained plans. Parser-level page programs still use raw `ContentOperation`, and unsupported lists, unsupported/advanced pattern subplans, alternate optional-content configuration/public-selection plans, complete colour transforms, complete transparency/group semantics, unsupported Form display lists, and advanced annotation/widget update/write-set plans remain partial. | complete backend-native payload arenas and native planned execution for every supported high-level op without raw-dispatch fallback | all | high-level packed plan equivalence/no dictionary lookup/no immediate delegation | retained text/image/Form/shading/pattern/group fixtures execute plan path |
| RB-02 | Narrow edit invalidation | `render/invalidation.rs`; `render/transaction_invalidation.rs`; `ContentEngine::{invalidate_for_transaction,invalidate_for_transaction_with_tiles}`; `editing_transactions.rs:apply_scene_text_transaction`; SDK/binding/server apply-with-render-invalidation entry points | Source-active for supported source-editing text transactions: post-apply reports copy the concrete source-editing changed object/page set, the invalidation bridge maps PDF refs plus `object-*`/`stream-*` source IDs to canonical render dependencies, deduplicates refs, unions mapped sources with explicit affected pages, accepts explicit pixel-space dirty tiles, and exposes `dirty_regions_to_render_tiles` for caller-supplied viewport/grid conversion from transaction-report dirty rectangles. `PageResources` preserves indirect font, color-space, and ExtGState references. The dependency graph records source-to-page and source-to-tile edges; cached display-list raster insertion now scans bounded retained resource ops and records concrete tile edges for resolved font-backed text, color-space-backed paint/text, ExtGState-backed paint/text/inline-image, XObject, shading, pattern-path, and pattern-painted text resources where canonical source IDs exist. The document cache separates full page-artifact pruning from exact raster-tile eviction. Rust SDK, C ABI/header, Python, WASM/TypeScript, .NET, Java, and server multipart entry points return a binding-safe render-invalidation plan with mapped source IDs, unmapped reset indicators, next revision, cache entry-point guidance, and optional dirty render tiles for cache-owning callers. `RenderInvalidationCachePlan` parses direct plan JSON or SDK envelopes and applies exact tile/page/source invalidation or conservative resets to `RenderDocumentCache`. Unknown refs still reset conservatively, and conservative page-level resource dependencies remain active for structural and transitive uncertainty. | shared-resource transitive write sets beyond the current bounded source edit, safe narrowing of conservative page-level resource dependencies once write-set classification distinguishes pixel-only from structural edits, external binding/server cache-handle adoption, and actual shared-cache mutation in binding/server hosts | Rust engine plus Rust SDK/cache-owner helper, C/Python/WASM/.NET/Java/server source surfaces active; external binding/server host cache handles still incomplete | focused source-ID mapping, page-only union, dirty-region conversion, explicit affected-tile, source-tile, bounded Form XObject tile-edge extraction, text font-reference tile-edge extraction, color-space-backed text tile-edge extraction, ExtGState-backed inline-image tile-edge extraction, typed pattern-state tile-edge extraction, exact raster-tile, plan application, server route, binding source compile/build, and local stale-tile tests pass; shared resource and full undo/redo invalidation matrices still missing | local supported text edit preserves unrelated page tiles through mapped source IDs; explicit tile write sets can evict raster tiles without page artifact pruning; bounded retained resource ops now create source-to-tile edges automatically for resolved font/XObject/shading/pattern/color-space/ExtGState resources; binding/server callers can receive the exact plan and Rust cache owners can apply it directly, while full transitive dependents and host cache mutation still require completion |
| RB-03 | Persistent clip DAG | `render/buffer.rs:ClipMask`; `render/clip_dag.rs`; `page_renderer.rs:clip_stack`; `page_renderer.rs:PathClipNodeCache`; `plan.rs:OP_CLIP` | save/restore stacks hold interned `Arc<ClipNode>` values with stable IDs, Full/Empty/Rectangle/SparseSpans/RleMask/DenseMask/Composite representations, structural rectangle/span/RLE intersections, DAG-native window materialization for transparency group clip carry, saved-node restore that avoids populating node materialization caches, bounded transformed path clip-node caching for repeated content-stream, display-list, packed-plan, Form BBox, group, tiling-pattern, and shading-pattern clip installs, document-cache carried clip-node reuse with render-contract identity in the key, path clip-node cache stats exposed through render-contract telemetry and CLI render-corpus JSON, compact group clip/soft-mask row fusion, direct alpha-mask clip/SMask row fusion, RGBA fragment clip/SMask row fusion, solid fill clip/SMask row fusion, and guarded separable solid/RGBA f32x4 row fusion with scalar tails including translucent and mixed-destination solid fills plus mixed-alpha/mixed-destination RGBA source rows with no clip, all-visible clips, binary clips, and partial clips; `PixelBuffer` still consumes concrete masks at paint boundaries and full direct/general dense fusion is incomplete | complete use of DAG nodes as the active buffer clip contract beyond the install boundary, non-normal/high-quality fusion policy, and universal tile/cache adoption across every planned path | all | `cargo test -p wellfriendpdf-engine transformed_path_clip --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine content_path_clip_replay_reuses_transformed_clip_node_cache --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine render_contract_telemetry_report_exposes_cache_counters --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine clip --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo clippy -p wellfriendpdf-engine --lib --jobs 1 -- -D warnings`, plus prior focused clip-DAG/fusion checks and `cargo test -p wellfriendpdf-engine separable_ --lib --jobs 1 -- --test-threads=1 --nocapture` | repeated transformed path clips hit a bounded DAG-node cache carried through `RenderDocumentCache`, keyed by render contract, and clip installs intersect through DAG nodes before materializing for `PixelBuffer`; universal concrete-mask elimination remains incomplete |
| RB-04 | Transparency, masks, print | `page_renderer.rs:apply_smask`, group handlers; `buffer.rs:composite_from`; `contract.rs:PrintProfile` | common semantics exist; compact `composite_from_at` windows, direct alpha-mask paints, RGBA fragments, and solid fills fuse partial clip opacity with soft-mask coverage into row kernels; compact non-normal and high-quality group fallback compositing now delegates to the same `blend_pixel` scalar oracle as full-page group flattening; normal Compat source-over rows with mixed source alpha and partial group alpha over opaque destinations stay on the wide uniform-alpha row path before scalar fallback; normal Compat source-over rows with mixed destination alpha now stay on a portable f32x4 general row path before scalar tails; normal Compat soft-mask rows with mixed destination alpha now stay on a portable f32x4 general row path before scalar tails; separable solid fills, including translucent source-alpha and mixed-destination rows, now use f32x4 rows for all PDF separable modes before scalar tails, cached RGBA fragments use f32x4 separable rows for mixed source-alpha/mixed-destination rows with no clip, all-visible clips, binary clips, and partial clips, and high-quality knockout solid/RGBA/alpha-mask/full-group/compact-group rows use row oracles that read destination RGB/alpha from the stored knockout backdrop; active Form transparency groups and SMask groups validate `/Group /CS`, accepting direct or resource-resolved DeviceGray/DeviceRGB/DeviceCMYK and failing typed for malformed, missing, or non-device group spaces; SMask group cache lookup happens only after `/G` resource merge and `/Group /CS` validation, and its key includes resolved group color policy plus deterministic `/G` resource fingerprint; rendering intent and color-management policy are active for ICC/named-color CMM paths and raw image cache identity, proof contracts fail closed without native CMM and use the native OutputIntent proof hook when available, and overprint preview is active for DeviceCMYK graphics-state overprint, but proof runtime validation/separation policy routing is not complete | packed group descriptors, backdrop-dependent mask identity beyond the current device-policy/resource scope, full non-device group colour-space compositing/backdrop validation, full group-space/knockout closure, native proof runtime matrix, and separation-preserving overprint output | all | `cargo test -p wellfriendpdf-engine high_quality_knockout --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine transparency_group --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine smask --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine high_quality --lib --jobs 1 -- --test-threads=1 --nocapture`, `cargo test -p wellfriendpdf-engine separable_ --lib --jobs 1 -- --test-threads=1 --nocapture`, focused `uniform_alpha`, `rgba_pixels_at`, `alpha_mask`, `group_soft_mask`, and `composite_from_` row tests, plus group/knockout/print cache separation tests | exact supported groups; typed unsupported exact print paths |
| RB-05 | Adaptive tile scheduling | `render/progressive.rs:ProgressiveRenderJob` | deterministic adaptive tile sizing, visible/adjacent/background viewport priority bands, dirty-region tile rescheduling with selective obsolete tile-publication identities, publication identities with serialized tile `priority_class` plus render-contract fingerprint, source-level progressive request cancellation, bounded viewport/dirty/render-context obsolete-publication reporting, source-session tile-publication acceptance/rejection, adjacent-page prefetch preview planning, bounded source viewer queue preview JSON, source viewer queue execution JSON for owned current-page tiles, bounded adjacent-page child-session prefetch execution, source viewer callback dispatch JSON with terminal suppression, and C/Python/WASM/.NET/Java synchronous callback helpers are active | external viewer runtime queue matrices and broader cross-language queue policy validation | Rust/server/bindings | visible/adjacent/background priority-band, dirty-region selective requeue, render-context revision, budget/order/request-cancel/viewport-revision/tile-publication-acceptance/viewer-queue-execution/adjacent-prefetch-execution/callback-dispatch/callback-helper determinism tests pass; source viewer-prefetch preview and child-session execution tests pass; full external runtime matrices still missing | priority schedule active in progressive execution with source-session stale publication rejection, render-context obsolete-publication reporting, queue preview reporting, owned-current-page queue execution reporting, adjacent-page child-session prefetch execution, callback-dispatch JSON, and synchronous callback helpers |
| RB-06 | Region/progressive image decode | `page_renderer.rs:scheduled_decode_image_with_color_space`, `paint_inline_image`; `render/image_decode_planning.rs`; `images/ccitt.rs`; `images/jpx.rs`; `image_painter.rs`; `runtime.rs`; `engine.rs`; `sdk.rs`; CLI/server/C/Python/WASM/.NET/Java binding sources | metadata-first viewport culling now skips offscreen image XObjects and inline images before decode/scheduler reservation; raw XObject cache keys carry planner/render-contract identity including codec/source-region/reduction state; active decode-required paths consume and log typed capability reports that mark JBIG2/lossless paths as full-decode-only, mark guarded DCT/JPEG downscale as native reduced-IDCT, mark guarded JPX downscale as native target-resolution reduction, and keep JPEG/JPX region/progressive unavailable; guarded axis-aligned CCITT source-clipped XObjects use bounded grayscale source-window decode/cache output and inline images use bounded grayscale source-window decode output when no reduction or full-image postprocessing is required; visible plans record source-region/reduction requirements and `HighQualityExact` refuses paths when required support is unavailable; runtime capabilities expose the guarded DCT/JPX/CCITT paths and remaining codec limitations; per-image capability JSON is source-visible through Rust SDK/C/Python/WASM/.NET/Java without decoding pixels; `ProgressiveImageDecodeSession` exposes a bounded start/continue/pause/resume/cancel/close lifecycle, returns `full_decode_required` only for nonterminal non-progressive work, preserves terminal states through continue-after-cancel/close, and retains no decoded pixels; lifecycle JSON is exposed through Rust SDK, C ABI/header, Python, WASM, .NET, Java, CLI, and server | JPEG/JPX ROI/region/progressive interfaces, JBIG2/lossless ROI/reduction/progressive interfaces, and CCITT reduction/progressive/non-axis-aligned/postprocessing-window closure | all | XObject tile-origin, inline-image scheduler-skip, JPX guarded-reduction capability, renderer consumption of capability reports, exact source-region/reduction refusal, runtime capability disclosure, per-image capability report, progressive-image lifecycle, C ABI lifecycle, server lifecycle route, CCITT source-window unit tests, CCITT planner source-window tests, source-window CTM test, JPX downscale planner tests, and reduced-JPX finalizer tests pass; codec region/progressive/cancel/cache output tests still missing | no invisible XObject/inline decode for metadata-provable offscreen images; exact contracts refuse visible unsupported region/reduction-required output; guarded DCT/JPEG and JPX reduction plus CCITT source-window output are active, while broader codec-supported ROI/region/progressive decode remains required |
| RB-07 | Actual WASM SIMD | `crates/render-simd/src/lib.rs` wasm branches; `buffer.rs` alpha-mask/separable/image-row paths; `engine.rs:encode_contract_row` | wasm32+`simd128` now has guarded kernels for opaque fill, normal opaque-destination blend/source-over rows, CPU f32x4 mixed-destination source-over rows, alpha/glyph-mask opaque- and mixed-destination normal Compat rows, soft-mask rows including non-opaque group-alpha effective-alpha rows plus CPU f32x4 mixed-destination rows, separable opaque-destination rows with true lane math for Multiply, Screen, Overlay, Darken, Lighten, ColorDodge, ColorBurn, HardLight, SoftLight, Difference, and Exclusion, CPU f32x4 separable solid-fill mixed-destination rows and cached-RGBA mixed-destination rows, copy RGBA and opaque RGBA contract rows, premultiply RGBA/BGRA contract rows, unpremultiply RGBA library rows, RGBA-to-RGB8/BGR8/BGRA8 rows, RGB8-to-opaque-RGBA image rows, Gray8 contract channel conversion, simple grayscale RGB/RGBA/BGRA expansion rows, and premultiplied grayscale RGBA/BGRA expansion rows; runtime capabilities disclose the bounded lane set; remaining requested categories are not complete | broader non-normal/high-quality SIMD row coverage remains incomplete | WASM | `cargo test -p wellfriendpdf-render-simd --lib --jobs 1 -- --test-threads=1`, focused `soft_mask` and public dispatcher group-alpha tests, `$env:RUSTFLAGS='-C target-feature=+simd128'; cargo check -p wellfriendpdf-render-simd --target wasm32-unknown-unknown --jobs 1`, focused `blend_alpha_mask` buffer tests, focused `separable` buffer tests, focused `opaque_rgb_run_writer` buffer tests, focused contract row-encoder alpha-mode tests, focused engine non-opaque group-alpha soft-mask row-path test, focused runtime capability disclosure test, and a Node-executed `.work` wasm cdylib smoke for the separable helper; general wasm-bindgen-test harness execution still required | actual wasm SIMD build selects implemented kernels where supported |
| RB-08 | Progressive sessions | `render/progressive.rs`; server and binding wrappers | Rust/server/C/Python/WASM/.NET/Java source expose start/step/token/pause/resume/cancel/request-cancel/viewport revision/dirty-region revision/render-context revision/tile-publication evaluation/viewer queue reporting/viewer queue execution/adjacent-page prefetch execution/viewer callback dispatch reporting/synchronous callback execution/close where applicable; C header and WASM `.d.ts` declare request-cancel, viewport revision, dirty-region revision, render-context revision, tile-publication evaluation JSON, viewer queue JSON, viewer queue execution JSON, adjacent-page prefetch execution, viewer callback dispatch JSON, and callback execution; Python exposes predicate step/finish cancellation, WASM exposes boolean/AbortSignal-style step/finish cancellation, .NET exposes `CancellationToken` step/finish overloads, and Java exposes `BooleanSupplier` step/finish cancellation overloads | external binding runtime parity and full viewer queue policy | C/Python/WASM/.NET/Java/server | lifecycle/cancel/request-cancel/viewport-revision/dirty-region-revision/render-context-revision/tile-publication-evaluation/viewer-queue/viewer-queue-execution/adjacent-prefetch-execution/callback-dispatch/callback-helper/close parity manifest plus Python/WASM checks, .NET token build, and Java supplier source compile | start/continue/pause/resume/cancel/request-cancel/viewport-revise/dirty-region-revise/render-context-revise/evaluate-publication/viewer-queue/execute-viewer-queue/execute-adjacent-prefetch/viewer-callback-dispatch/callback-helper/close source surface exposed everywhere, with binding-native pre-cancel hooks in Python, WASM, .NET, and Java |
| RB-09 | Caller-owned surfaces | `engine.rs:render_page_into_buffer`; `PixelBuffer`; binding contract-buffer wrappers; `runtime.rs` | source-active caller-owned render buffers validate explicit pixel format, stride, alpha, grayscale, reverse byte order, and contract dimensions; RGBA/BGRA row encoding now honors `AlphaMode::Premultiplied`, `Straight`, and `Opaque`; runtime capabilities expose the global caller-surface support; external binding runtime matrix remains incomplete | full external binding runtime parity and consumer-build coverage | Rust/C/Python/WASM/.NET/Java | focused stride/format/short-buffer/core/CLI tests and contract row-encoder alpha-mode tests pass; external consumer runtime gates still missing | caller buffer exercised in each applicable binding runtime |
| RB-10 | Contract builders | `render/contract.rs`; binding default JSON adapters; `crates/server/src/routes/render_contract.rs`; `crates/server/src/app.rs`; `crates/wellfriendpdf-capi/src/lib.rs`; `crates/wellfriendpdf-capi/include/wellfriendpdf.h`; `bindings/dotnet/WellfriendPdf/RenderContract.cs`; `bindings/java/src/main/java/io/wellfriendpdf/WellfriendPdf.java`; `crates/wellfriendpdf-py/src/lib.rs`; `crates/wellfriendpdf-wasm/src/lib.rs`; `crates/wellfriendpdf-wasm/wellfriendpdf.d.ts` | CLI builds full schema-v1 JSON, server exposes `/api/v1/render-contract`, `/api/v1/render-contract/png`, `/api/v1/render-contract/raw`, `/api/v1/render-contract/png-with-font-substitution-report`, and `/api/v1/render-contract/raw-with-font-substitution-report`, C ABI exposes an opaque `WellfriendRenderContract` handle, .NET and Java have typed `RenderContract` round-trip/builder objects, Python exposes a canonical Rust-backed `RenderContract` object, and WASM exposes a canonical Rust-backed `WellfriendRenderContract` object with surface/background/clip/transform/resource-budget helpers plus typed/object PNG/caller-buffer overloads; core rejects unimplemented semantic deviations, applies public `max_decoded_bytes` to page content stream decode plus downstream decode paths, observes caller cancellation during contract page-content decode, and enforces public max-temporary-byte budgets for canonical working surfaces plus clipped full-page intermediates before allocation | complete field-effect parity across all public fields and external runtime gates | Rust/CLI/C/.NET/Java/Python/WASM/server partial | serialization/default/field effect parity tests; decoded-byte, cancellation, and temporary-budget tests; server route check and focused builder/PNG/raw/report tests pass; C ABI check and focused handle test pass; .NET managed build/focused builder test pass; Java source compile/builder-only smoke pass; Python crate check passes; WASM target crate check passes; native binding runtime gates still pending | all public fields round-trip and alter active semantics or typed refusal |
| RB-11 | Font substitution diagnostics | `font_rasterizer.rs:get_fallback_font`; `page_renderer.rs:get_font_bytes`; `render/font_substitution_report.rs`; binding report-returning render APIs; `crates/server/src/routes/render_contract.rs` | bounded render-time substitution events/report are visible through Rust/CLI, C/Python/WASM/.NET/Java source APIs, and server contract PNG/raw report routes; events include requested PDF font, embedded state, encoding, resolution source, selected replacement, bounded first-run required glyph coverage, missing-glyph samples/counts, visual-risk category, extraction/editing impact, font-policy identity, and risk flags; font byte/resolver cache keys and derived glyph hashes now include render-contract identity and resource budget; reused device glyph masks can paint from bounded atlas pages with render telemetry; renderer-originated substitution events coalesce equivalent selections across state merges while preserving first observed page/coverage; deterministic bundled substitution remains active | external binding runtime parity | all | font contract-key tests pass; focused font-substitution policy-field and single-flight tests pass; server focused builder/PNG/raw/report tests pass; glyph-atlas tests pass; external binding runtime gates still missing | report fields, contract-scoped font identities, glyph-atlas cache telemetry, and source single-flight coalescing are active; runtime gates still need to prove each external binding returns the enriched JSON |
| RB-12 | Type 3 sublist closure | `page_renderer.rs:render_text_string`, `render_type3_glyph` | native CharProc rendering is source-active; CharProcs with clips, typed text/image/Form/shading/inline/pattern resources, Type 3 `d0`/`d1` metrics descriptors, and vector paths with per-paint inherited/explicit fill-stroke color handling can replay through guarded retained plans; parsed CharProc/geometry caches now track explicit `Unvisited`/`Compiling`/`Compiled`/`Failed` lifecycle states, preserve failure states with reasons, use bounded LRU admission, and absorb child render-state cache results; Type 3 does not resolve ordinary fallback font bytes; missing PDF Encoding CharProc names and unsupported/unresolved glyph programs return typed `UnsupportedFeature` instead of Unicode-derived aliases | broader unsupported CharProc state/resource coverage beyond the explicit program-cache lifecycle | all | recursion/unsupported/color-resource tests plus `type3_missing_encoding_name_returns_typed_refusal`, `type3_charproc_renders_resource_xobject_image`, `type3_mixed_path_resource_charproc_preserves_inherited_text_color`, `type3_retained_mixed_inherited_and_explicit_path_colors`, `type3_program_cache_tracks_explicit_lifecycle_states`, and Type 3 parsed-program cache-pressure/admission tests | supported Type 3 never substitutes ordinary font; parsed program caches expose an explicit lifecycle state machine |
| RB-13 | JPX semantics | `images/jpx.rs`; `images/decoder.rs`; `image_painter.rs`; `render/image_decode_planning.rs`; `page_renderer.rs`; `runtime.rs`; `engine.rs`; `sdk.rs`; CLI/server/C/Python/WASM/.NET/Java binding sources | the JPX-only sampler branch is removed; guarded downscale plans use `hayro-jpeg2000` target-resolution decode when no image mask or full-image postprocessing step requires the original sample grid; reduced decode validates original codestream dimensions against the PDF dictionary before accepting smaller output; active decode-required paths consume capability reports, `HighQualityExact` refuses visible JPX paths when source-region support or unsafe reduction support is required, cache identity includes codec/source-region/reduction state, runtime capabilities disclose guarded reduction plus remaining ROI/region/progressive limits, per-image capability JSON is source-visible through Rust SDK/C/Python/WASM/.NET/Java, and `ProgressiveImageDecodeSession` reports full-decode-required for JPX continuation attempts through Rust SDK, C ABI/header, Python, WASM, .NET, Java, CLI, and server lifecycle JSON | codec-native ROI/region/progressive integration plus broader tile/component output | all | JPX target-resolution planner tests, reduced-JPX finalizer tests, renderer capability-report consumption, exact source-region/reduction refusal, runtime capability disclosure, per-image capability report, progressive-image lifecycle, C ABI lifecycle, and server lifecycle route tests pass; JPX ROI/region/progressive/cancel output tests still missing | guarded JPX reduction, exact decoder capability report, cache identity, exact source-region/reduction refusal, canonical sampler routing, and bounded cross-binding lifecycle reporting are source-active; real ROI/region/progressive JPX output still required |
| RB-14 | SVG/PS regional fallback | `render/vector_fallback.rs`; `render/svg.rs`; `render/postscript.rs` | finite affine image XObjects, complete finite-affine DeviceGray/DeviceRGB/DeviceCMYK inline images except CCITT/JBIG2 terminal RGB/CMYK declarations, resource-named device color spaces, direct/resource-resolved CalGray/CalRGB/Lab inline images, direct/resource-resolved Indexed inline images, direct/resource-resolved ICCBased inline images, preflighted opaque direct/resource-resolved Separation/DeviceN inline images, DCT-filtered opaque resource-resolved Separation and one-colorant DeviceN inline images, DCT-filtered calibrated/Indexed/ICCBased inline images, simple inline image masks, inert SVG data attributes and PostScript comments now identify regional image/inline-image kind plus device-space bounds for focused order/bounds assertions, vector-safe non-transparency Forms, simple linear DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved CalGray/CalRGB/Lab/Separation axial shadings with endpoint-sampled contained non-unit shading `/Domain` support and transformed shading `/BBox` clipping, finite/invertible affine-transformed DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved CalGray/CalRGB/Lab/Separation radial shadings through the same simple Type 2 endpoint-sampling and BBox path, simple finite-matrix shading-pattern path/text paints, simple colored and color-inheriting uncolored tiling-pattern path paints, glyph-outline text paints, plus bounded tiling-pattern painted inline/Image XObject stencil masks, and safe opaque line-style/dash/font/flatness/smoothness/default-state/identity-transfer ExtGState dictionaries stay native/bounded; non-linear/mesh/malformed radial shadings, malformed or degenerate shading BBoxes, Type 2 domains/functions requiring clipped flat sections outside the function domain, unsupported named color spaces, `/None` or unresolved Separation/DeviceN/tint-space inline-image resources, multi-component DeviceN terminal-codec tint-space inline images, non-DCT terminal-codec tint-space inline images, CCITT/JBIG2 terminal inline images declared as non-gray device color spaces, non-DCT terminal-codec calibrated/Indexed/ICCBased inline images, advanced pattern paint and resource-bearing tiling cells outside the vector-safe image/Form/shading/inline-image subset, transparency groups, transparency-bearing, non-identity transfer-function, invalid font arrays, negative/non-finite flatness or smoothness values, invalid overprint-mode, overprint-enabled, stroke-adjustment-enabled, alpha-source-enabled, or text-knockout-disabled ExtGState, unsafe nested Forms, degenerate/unresolvable image transforms, and unsupported local constructs still rasterize whole page | native remaining advanced gradient/pattern/transparency regionalization plus bounded unsupported-region raster fallback | CLI/Rust | `cargo test -p wellfriendpdf-engine --test regional_vector_fallback --jobs 1 -- --test-threads=1`, `cargo test -p wellfriendpdf-engine shading_is_regional --lib --jobs 1 -- --test-threads=1`, `cargo test -p wellfriendpdf-engine radial_shading_with_nonuniform_ctm_is_regional_vector_output --lib --jobs 1 -- --test-threads=1`, `cargo test -p wellfriendpdf-engine ext_gstate --lib --jobs 1 -- --test-threads=1 --nocapture`, focused inline-image order/bounds tests `svg_output_inline_image_uses_regional_embed` and `ps_output_inline_image_uses_regional_colorimage`, focused Image XObject order/bounds tests `svg_output_image_xobject_resource_color_space_uses_regional_embed` and `ps_output_image_xobject_resource_color_space_uses_regional_colorimage`, focused non-unit shading-domain tests `cargo test -p wellfriendpdf-engine non_unit --jobs 1` and `cargo test -p wellfriendpdf-engine shading_domain_outside_function_domain_stays_whole_page_fallback --lib --jobs 1`, focused BBox shading tests `cargo test -p wellfriendpdf-engine bounded_axial_shading --jobs 1`, focused tiling-pattern tests `cargo test -p wellfriendpdf-engine pattern --test regional_vector_fallback --jobs 1 -- --test-threads=1`, plus remaining mixed vector/raster seam tests outside these focused cases | unsupported local region only embeds raster; surrounding vectors remain native |
| RB-15 | Visual reference normalization | `tools/renderer-visual-diff/visual_diff.py` | CLOSED_SOURCE in local continuation: schema-v3 canonical comparison surface, raw stride/channel/straight-premultiplied-opaque alpha/rotation normalization, render-context sidecars, masks, metrics including RGB-only `color_mae`/`color_rmse`/`max_color_delta`, connected regions, severity, and cause taxonomy | corpus/reference execution and human adjudication remain deferred | tooling | `python -m pytest tools/renderer-visual-diff/test_visual_normalization.py -q` | compact manifest normalization passes without corpus work |
| RB-16 | Active fallback categories | `docs/renderer/final-fallback-closure-report.md`; `runtime.rs`; `render/image_decode_planning.rs`; `page_renderer.rs` | active categories remain, but exact policy now also converts visible full-decode-only image paths that require source-region/reduction support into typed refusal | eliminate or convert remaining silent material fallbacks to typed exact policy | all | high-quality exact image guard and one test per remaining category | zero silent material high-quality fallback categories |
| RB-17 | Cache resource accounting | `page_renderer.rs:enforce_bounded_maps`; `engine.rs:RenderContractTelemetryReport`; `runtime.rs:runtime_capabilities_for`; `cli/src/main.rs:render_corpus_file_record`; `server/src/routes/health.rs:runtime_capabilities`; `server/src/routes/render_contract.rs`; `crates/wellfriendpdf-capi/src/lib.rs`; `crates/wellfriendpdf-capi/include/wellfriendpdf.h`; `crates/wellfriendpdf-py/src/lib.rs`; `crates/wellfriendpdf-wasm/src/lib.rs`; `bindings/dotnet/WellfriendPdf/WellfriendPdfDocument.cs`; `bindings/java/src/main/java/io/wellfriendpdf/WellfriendPdf.java` | byte-charged raw image, scaled image, SMask group, mesh-shading, Form XObject program, tiling-pattern program, transformed path clip-node, font-byte, font-resolver, glyph-outline, device glyph-mask, glyph-mask atlas page, Type 3 parsed geometry program, Type 3 parsed CharProc program, Type 3 mask, Type 3 rendered-glyph, path fill-mask, path stroke-mask, and retained display-list caches now enforce active caps/aggregate cleanup where applicable when render state returns to `RenderDocumentCache`; transparent-page-group decisions use deterministic entry-capped LRU admission; runtime capability JSON and server `/api/v1/capabilities` disclose structured renderer cache-pressure policy with cache classes, budget fields, pressure actions, and correctness-preservation state; Rust `RenderContractTelemetryReport`, server multipart render-contract report routes, C ABI `wellfriendpdf_document_*_with_*_render_report_json` functions, Python `render_contract_*_with_render_report` methods, WASM `renderContract*WithRenderReport` methods, .NET `RenderPage*WithRenderReport` methods, and Java `renderPage*WithRenderReport` methods expose one-shot per-render cache counters for glyph, glyph-atlas, mask, Type 3, path masks, path clip nodes, font, display-list, raster-tile, image, SMask, mesh, Form, pattern, annotation, transparent-page-group, and offscreen-pool classes; `render-corpus` JSON exposes retained display-list, path clip-node, and glyph-atlas cache stats alongside the other byte-accounted caches | external runtime telemetry parity | Rust/CLI/server/C/Python/WASM/.NET/Java source active; external runtime parity incomplete | aggregate resource-budget, per-cache LRU, Form/tiling-program byte-charge, font-map byte-charge, display-list byte-charge, path clip-node aggregate budget test, glyph-outline aggregate budget test, raster-artifact aggregate budget test, Type 3 parsed-program byte-accounting tests, glyph-atlas tests, path clip-node telemetry, display-list invalidation accounting, transparent-page-group LRU/invalidation, runtime capability disclosure, server capabilities route policy disclosure, CLI cache-stat JSON shape tests, engine render-contract telemetry test, server multipart metadata tests, C API render-report test, Python/WASM checks and clippy, .NET build, and Java source compile pass | contract budget constrains byte-charged resource caches deterministically; Rust/server/C/Python/WASM/.NET/Java render-report telemetry is public at source level; external runtime telemetry remains partial |
| RB-18 | Deterministic rendering matrix | `progressive.rs`, `page_renderer.rs` | deterministic fixtures now include a synthetic progressive schedule matrix that compares full-page rendering against single-tile, batched, and viewport-hinted progressive completion, plus a scoped two-worker progressive job matrix with different quantum schedules and byte-identical output/publication identity/tile order; display-list band schedules now compare multiple cold band heights, warm-cache band crops from a cached full-page raster, alternating band ownership across two separate worker-local `RenderDocumentCache` instances, and a scoped two-worker concurrent band render with separate worker-local caches against the full display-list render; full server/external concurrent worker/cache/tile matrices remain incomplete | broader deterministic execution policy and tests for server/external worker/cache matrices | Rust/server | `progressive_quantum_schedules_finish_to_identical_pixels`, `progressive_concurrent_worker_jobs_finish_to_identical_pixels`, `display_list_band_schedules_match_full_page_across_cache_states`, `display_list_band_schedules_match_full_page_across_worker_local_caches`, and `display_list_band_schedules_match_full_page_across_concurrent_worker_local_caches` pass for focused tile/band/cache scheduling; broader server/external runtime matrices remain missing | same pixels across supported execution schedules, including progressive concurrent workers, cache/worker-local variants, and the focused two-worker display-list band case |

RB-14 continuation note: simple SVG/PS vector shadings now accept `/Function` arrays when every element resolves to a linear Type 2 component function and the concatenated endpoint samples form a supported color vector. The focused evidence covers axial SVG/PostScript output only; malformed arrays, non-linear functions, clipped-outside-function-domain cases, mesh shadings, transparency, advanced patterns, and final visual parity remain incomplete.

RB-14 SVG/PS stencil-mask continuation note: SVG regional fallback now accepts simple shading-pattern painted Image XObject and inline-image stencil masks by emitting a decoded SVG mask plus the existing native gradient fill. PostScript now accepts bounded bitmap stencil masks painted with native `shfill` by vectorizing painted cells into a clip path; larger masks and broader pattern/transparency regionalization remain incomplete.

RB-14 SVG/PS colored/uncolored tiling-pattern continuation note: SVG/PostScript regional fallback now accepts simple colored and uncolored `/PatternType 1` tiling-pattern path paints and pattern-painted inline/Image XObject stencil masks when the pattern stream decodes completely, carries exact finite geometry metadata, uses `/PaintType` 1 or 2, has positive tile steps, and its tile program is pure vector or contains scoped vector-safe image/Form/shading/inline-image resources without nested pattern paint. `/PaintType 2` additionally requires finite caller DeviceGray, DeviceRGB, or DeviceCMYK components and rejects tile streams that set paint color themselves. The sinks clip the painted path or stencil mask, apply per-tile BBox clips, and replay bounded tiles as native vector paths under `MAX_VECTOR_TILING_PATTERN_CELLS`. Nested pattern-paint, color-setting uncolored, negative-step, over-cap, malformed, transparency-bearing, resource programs outside the vector-safe image/Form/shading/inline-image subset, and advanced tiling-pattern cases remain incomplete.

RB-14 SVG/PS tiling-pattern text continuation note: SVG/PostScript regional fallback now accepts supported colored/uncolored tiling-pattern glyph-outline text fill/stroke, using the same bounded native tile replay as path paints before falling back to the existing shading-pattern text replay. Patterned text outside the supported shading/tiling glyph-outline subset remains incomplete.

RB-14 SVG/PS resource-bearing tiling-cell continuation note: SVG/PostScript regional fallback now threads viewport scale and active Form recursion state into tiling-pattern classification, then replays each tile with the pattern's merged resources and nested regional allow-lists. Focused synthetic evidence covers colored tiling cells that invoke a vector-safe Image XObject and a named axial shading through pattern-local resources; vector-safe Form and complete inline-image tile resources use the same scoped classifier path, while nested `/Pattern` paint, unsupported resource programs, and color-setting uncolored cells remain incomplete.

RB-14 SVG blend-mode continuation note: SVG regional/vector output now accepts supported ExtGState `/BM` modes, including safe alpha-bearing blend state with `/SMask /None`, through target-aware fallback classification and emits native `mix-blend-mode` styles on path, glyph, regional image, inline-image, shading, shading-pattern, and tiling-pattern paint output. Conservative and PostScript classification still require first-supported Normal/Compatible blend semantics; PostScript fractional alpha/non-normal blending, soft masks, semantic transparency groups, and broader unsupported local constructs remain incomplete.

RB-06/RB-13/RB-14 JPX alias consistency continuation note: image decode planning, stream lossless stopping, decoder terminal dispatch, inline scaled JPX dispatch, DecodeParms filter matching, codec-isolation backend registry/canonicalization, and SVG/PostScript vector-fallback image filter classification now treat `/JPX` as the same JPXDecode terminal codec alias. This removes a false `UnknownCodec`/full-decode-only capability path for abbreviation-bearing JPX images, keeps guarded target-resolution reduction eligibility and backend reporting consistent, and prevents vector output classification from whole-page rasterizing solely because the short filter name was used. It does not add JPX ROI/region/progressive output.

RB-04 high-quality row continuation note: high-quality Normal group compositing now routes unmasked full-page and compact no/all-visible/binary-clip windows plus partial clips through high-quality normal row oracles; high-quality Normal cached RGBA glyph/image fragments use high-quality normal row oracles for no/all-visible/binary-clip rows and partial clips; high-quality Normal alpha-mask glyph/image/stroke paints use `blend_alpha_mask_run_high_quality_normal` for no/all-visible/binary-clip rows plus `blend_alpha_mask_run_high_quality_normal_partial_clip` for partial clips; high-quality Normal rectangle fills use `blend_solid_run_high_quality_normal` for no/all-visible/binary-clip rows plus `blend_solid_run_high_quality_normal_partial_clip` for partial clips; high-quality non-normal solid rectangle fills use `blend_solid_run_high_quality_blend` plus `blend_solid_run_high_quality_blend_partial_clip`; high-quality non-normal cached RGBA fragments use `composite_high_quality_blend_row` plus `composite_high_quality_blend_row_partial_clip`; high-quality non-normal alpha-mask paints use `blend_alpha_mask_run_high_quality_blend` plus `blend_alpha_mask_run_high_quality_blend_partial_clip`; high-quality non-normal group compositing uses `composite_high_quality_blend_row` plus `composite_high_quality_blend_row_partial_clip`; high-quality installed-SMask solid fills use `blend_solid_run_high_quality_masked`; high-quality installed-SMask cached RGBA paints, installed-SMask alpha-mask paints, and full/compact group soft-mask composites use `HighQualityMaskSources`, `composite_high_quality_masked_row`, and `blend_alpha_mask_run_high_quality_masked`; high-quality knockout solid fills use `blend_solid_run_high_quality_knockout`; and high-quality knockout cached RGBA paints, knockout alpha-mask paints, and full/compact knockout group soft-mask composites use backdrop-reading row oracles for the same clip classes. These preserve the existing `blend_pixel` linear-light source-over/blend oracle without per-pixel dispatcher fallback. Active Form and SMask group `/CS` policy is fail-closed for direct/resource-resolved device spaces; true non-device group colour-space compositing/backdrop validation and full transparency-group closure remain incomplete.

RB-04 SMask backdrop-identity continuation note: `apply_smask` now derives `/BC`/default-alpha backdrop identity before cache lookup, and `smask_group_cache_key` includes that initial-backdrop/outside-alpha identity alongside the resolved group color policy and deterministic `/G` resource fingerprint. Cached masks therefore no longer reuse across different supported backdrop/default-alpha semantics or bypass malformed backdrop/transfer-function refusals; true non-device group colour-space compositing and full transparency-group closure remain incomplete.

## Six active fallback-policy categories

1. Type 3 unsupported CharProc state/resource coverage beyond the explicit parsed-program cache lifecycle.
2. Compatibility/legacy generic bundled font substitution outside explicit `HighQualityExact` contract rendering; exact contracts now accept valid caller-registered or deterministic-system replacements and refuse this path when no valid embedded/document/registered/system replacement exists.
3. JPX codec limitation for ROI/region/progressive output; the JPX-only sampler branch is removed, guarded target-resolution reduction is active for safe downscale plans, and exact contracts now refuse visible unsupported paths when source-region/reduction support is required.
4. Portable qcms backend selection remains the default; rendering intent and color-management policy reach active ICC/named-color CMM paths and cache identity, deterministic fallback avoids ICC backend transforms, proof contracts fail closed without native CMM and use the native OutputIntent proof hook when available, overprint preview gates active DeviceCMYK graphics-state preview, and explicit unavailable `NativeLittleCms` requests are typed validation refusals, but native CMM runtime validation, separation policy, and separation-preserving overprint remain future gates.
5. SVG raster embedding.
6. PS/EPS raster embedding.

2026-08-25 proof OutputIntent source closure note: proof profile discovery now scans `/OutputIntents` for the first profile-bearing intent instead of letting an unprofiled first entry suppress a later usable profile. This is source-local selection closure only; native CMM runtime validation, proof matrix execution, and separation-preserving print output remain active blockers.

The previous recursive/excessive pattern solid fallbacks, retained missing-named-shading no-op, unsupported retained-list raw-dispatch fallback, unsupported retained-tile immediate fallback, and progressive unsupported retained-tile immediate fallback are already converted to typed failure/diagnostic behavior and are not counted as active output-degrading fallbacks.

## Immediate implementation order

1. Complete the active RenderContract/surface/cancellation foundation so bindings can share real controls.
2. Add C ABI progressive/caller-surface primitives and adapt Python/WASM/.NET/Java.
3. Convert the remaining bounded cache byte-accounting gaps and cross-binding transaction invalidation propagation to active APIs with tests.
4. Finish persistent clip hot replay/fusion and adaptive scheduler.
5. Finish remaining WASM SIMD categories beyond the implemented opaque/source-over/copy/alpha-mask/soft-mask group-alpha/common-separable-blend/premultiply/unpremultiply/RGB-BGR-BGRA/RGB-image-span/Gray8/grayscale-expansion lanes while preserving scalar equivalence.
6. Close remaining high-level packed plans and rendering-semantic fallbacks, then finish remaining SVG/PS regionalization beyond simple images and vector-safe Forms.

No performance, corpus, latency, throughput, or competitor benchmark is part of this preaudit or its implementation plan.
