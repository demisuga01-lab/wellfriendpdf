# Final renderer fallback closure report

**Repository:** `E:\wellpdfsdk`
**Closure branch:** `main`
**Scope:** source-level fallback disposition and compact functional verification before the separate benchmark task. This report contains no performance, corpus, latency, throughput, or competitor result.

The 2026-09-07 final source-closure continuation removes stale wording that described progressive retained-plan refusal as temporary incomplete coverage. Content outside the supported native retained-plan surface remains a typed `UnsupportedFeature` with no tile publication or immediate-render delegation; callers may explicitly use direct contract rendering where that policy is acceptable. Live full render-contract revision invalidates all prior publication/cache identity and does not add a fallback category.

## Summary

The previous inventory contained 12 fallback decisions, eight of which could materially change visual output. This pass removed the two tiling-pattern solid-colour degradations, made missing and malformed active pattern resources plus `/Pattern` paint without an active pattern name typed refusals instead of dropped paint, changed missing/malformed named shading, active shading dictionaries with missing required fields, unsupported or malformed functions, unavailable mesh streams, and malformed shading-pattern inputs from implicit no-ops to explicit unsupported diagnostics, changed malformed soft-mask dictionaries/references, malformed present SMask backdrop arrays, and unsupported/malformed SMask transfer functions from log-and-render-unmasked, default-backdrop, or identity-transfer behavior into typed refusals, changed active image decode/mask failures from log-and-drop or render-unmasked behavior into typed refusals, changed missing/malformed active XObject `Do` inputs, recursive or over-depth Form XObjects, malformed transparency-group `/I`/`/K` flags, and transparency-group Form offscreen allocation denials from implicit no-ops/log-and-skip/default-false behavior into typed refusals, changed selected malformed annotation appearance streams and selected malformed annotation appearance state metadata from skip/drop or fallback-state behavior into typed refusals, changed malformed optional-content OCG/OCMD visibility decisions from fail-open visible defaults into typed refusals, changed recursive Type 3 CharProcs from log-and-skip behavior into typed refusals, changed missing active ExtGState `gs` resources from log-and-default behavior into typed renderer refusals, made supported Type 3 charprocs native-first, removed Unicode-derived Type 3 CharProc aliasing, made PostScript ExtGState output safe by raster fallback rather than silently omitting transparency, made SVG/PS vector-safe Form XObjects, including no-op and opaque-inert isolated/knockout transparency-group Forms, replay natively, made simple complete inline images, inline image masks, and Image XObject stencil masks regionally embedded, made finite affine local image placements regionally embedded, made simple linear axial plus finite/invertible affine-transformed radial SVG/PS shadings native instead of forcing whole-page raster embedding, lets simple finite-matrix shading-pattern fill/stroke path and glyph-outline text paints emit native SVG gradients and PostScript `shfill`, lets safe opaque line cap/join/miter/dash/font/flatness/smoothness/default-state/identity-transfer/first-supported-normal-blend-array ExtGState dictionaries replay in SVG/PS without whole-page rasterization, carries vector-safe line styling through stroked and fill-stroke text outlines, installs native SVG/PS glyph clips for font-resolved text-clipping render modes, narrows pattern fallback to unsupported active pattern paint outside the supported shading/tiling subset rather than dead color state, composes stacked SVG clip paths instead of replacing the active clip, and removes the dense ordinary text operation-count whole-page fallback.

**Current active fallback-policy categories:** 10.
**Current materially degraded high-quality/exact output categories:** 0.
**Resolved into exact typed refusal/diagnostic rather than degraded pixels:** 17.

The 2026-08-28 progressive retained-tile refusal continuation records the existing unsupported retained display-list tile refusal as a structured fallback event in production reports. The final policy remains typed refusal with no tile publication and no immediate-render fallback, but callers now receive the same internal fields required by the fallback inventory: call site, trigger, output, degradation, Standard/high-quality availability, replacement, final policy, and tile identity.

The 2026-08-28 progressive render-contract parity continuation keeps those refusal-event policy fields aligned with the canonical contract. Progressive jobs can now start from `render_contract_json` / `contract_json`, and retained-tile refusal availability is derived from `ExactnessPolicy` instead of the legacy `RenderMode` token. This prevents contract-backed high-quality exact progressive requests from being reported as compatibility fallbacks.

The 2026-08-28 PostScript exact shading-domain regionalization continuation narrows the SVG/PS vector fallback boundary without changing the fallback counts. Axial/radial vector-shading records now preserve the shading `/Domain`, and PostScript exact Type 2/Type 3 sidecars emit `/Domain [...]` for finite non-unit domains instead of forcing that local nonlinear shading back to whole-page fallback. SVG nonlinear shadings remain typed refusal/whole-page boundaries because SVG cannot represent the nonlinear PDF function exactly.

The 2026-08-27 alpha-mask fused clip-window continuation is an allocation and clip/mask fusion source update, not a fallback-policy change. `AlphaMask::fused_clip_window` now reuses one bounded clip-opacity row across scanlines while preserving the existing SIMD/scalar fused alpha product, so this report's fallback counts are unchanged.

`HighQualityExact` policy does not silently take the removed pattern solid fallback. Missing active pattern resources, `/Pattern` paint without an active pattern name, unsupported pattern types, PatternType 1 dictionaries without streams, malformed tiling-pattern geometry, recursive pattern resources, and over-limit pattern resources return a typed `UnsupportedFeature` instead of dropping or approximating paint.
Caller-registered Rust font bytes now resolve before bundled fallback, report the `user_registered` source, and participate in font-policy/cache identity. Deterministic font replacement also carries FontDescriptor Symbolic/Nonsymbolic, Italic, ForceBold, and FontWeight hints into the provider request. Unknown non-Standard14 symbolic descriptor fonts therefore route to the symbolic coverage lookup and report a coverage-only fallback face plus provider selection reason `symbolic_flag` in compatibility mode, while `RenderMode::HighQuality` and explicit `HighQualityExact` contracts still refuse generic bundled replacement when no valid embedded, document-provided, caller-registered, or deterministic-system replacement exists.
PaintType 2 uncolored tiling patterns now also fail typed when the active pattern color has an unexpected component count; malformed two-component pattern colors no longer infer `/DeviceRGB` before tile replay.
Active `Do` of missing or malformed XObjects, missing or malformed Form `/BBox`, recursive or over-depth Form XObjects, malformed transparency-group `/I`/`/K` flags, and transparency-group Form offscreen allocation denials likewise return typed `UnsupportedFeature` instead of logging and dropping the invoked visual content, replaying unbounded Form content, or defaulting malformed group flags to `false`.
Visible annotations with missing or malformed `/Subtype` or `/Rect` likewise return typed `UnsupportedFeature` after print/form/optional-content exclusion instead of silently skipping annotation paint, rendering under ambiguous annotation semantics, or synthesizing appearances from truncated/filtered rectangle arrays.
Annotations with malformed present `/F` visibility flags likewise return typed `UnsupportedFeature` before display/print visibility decisions instead of defaulting flags to zero.
Synthesized text-markup, line, and ink annotations with malformed `/QuadPoints`, `/L`, or `/InkList` likewise return typed `UnsupportedFeature` before synthetic appearance construction instead of falling back to full-Rect marks, skipped strokes, or filtered coordinate arrays.
Synthesized markup, square, circle, line, ink, and FreeText annotations with malformed present `/C` or `/CA` likewise return typed `UnsupportedFeature` before synthetic appearance construction instead of defaulting colors or clamping opacity.
Synthesized FreeText annotations with malformed present `/Q` likewise return typed `UnsupportedFeature` before text layout instead of defaulting to left alignment.
Synthesized FreeText annotations with malformed present `/Contents` likewise return typed `UnsupportedFeature` before text layout instead of filtering malformed display arrays or rendering partial content.
Synthesized Widget appearances with malformed present `/MK /BG` or `/MK /BC` likewise return typed `UnsupportedFeature` before synthetic appearance construction instead of defaulting or clamping widget background/border colors.
Synthesized Widget appearances with malformed present `/AS` likewise return typed `UnsupportedFeature` before synthetic checkbox/radio state construction instead of ignoring the malformed state and falling back to `/V`.
Synthesized Widget appearances with malformed present `/FT` likewise return typed `UnsupportedFeature` before field-kind dispatch instead of silently skipping widget synthesis.
Synthesized Widget appearances with malformed present `/Ff`, local `/Q`, or AcroForm `/Q` likewise return typed `UnsupportedFeature` before field kind and alignment synthesis instead of defaulting to checkbox/single-line/left-aligned behavior.
Synthesized Widget appearances with malformed present AcroForm `/NeedAppearances` likewise return typed `UnsupportedFeature` before checked-state synthesis instead of defaulting the flag to `false`.
Synthesized Widget appearances with malformed present `/DA` text-style operators likewise return typed `UnsupportedFeature` before synthetic appearance construction instead of defaulting font size or clamping text color.
Synthesized Widget appearances with malformed present `/V`, `/Opt`, or `/MK /CA` display metadata likewise return typed `UnsupportedFeature` before text, choice, button, checkbox, or radio synthesis instead of filtering malformed display arrays, skipping invalid options, treating malformed button values as unchecked, or dropping malformed captions.
Selected annotation appearance streams with missing or malformed `/BBox`, decode failures, or malformed selected `/AS` state metadata likewise return typed `UnsupportedFeature` instead of silently dropping the annotation appearance, replaying it with fabricated bounds, or selecting `/Off`/first-state fallbacks.
Missing ExtGState `gs` resources likewise return typed `UnsupportedFeature` from immediate, retained descriptor replay, and display-list compilation paths instead of keeping default graphics state and continuing. Resolved ExtGState dictionaries with malformed present alpha, line style, miter, blend, rendering intent, overprint, stroke-adjustment, alpha-source, text-knockout, transfer-function, flatness, smoothness, dash, type, or font metadata now fail typed before display-list capture, immediate replay, or packed state mutation instead of retaining defaults, clamping out-of-range values, filtering non-name array entries, or accepting all-unsupported blend-mode arrays. Well-formed `/SA`, `/AIS`, and `/TK` values now enter graphics state and retained/packed draw-state identity; visible `/SA true` stroke paint, visible `/AIS true` fill/stroke/text/image/shading paint, and visible `/TK false` text paint fail typed at the paint boundary instead of being rejected as inert metadata or silently painted without those semantics.
Malformed non-text graphics-state operands and `Q` restore underflow likewise return typed descriptor/display-list/render refusals, and SVG/PS vector-output classification routes them away from native sink replay, instead of applying identity CTMs, default line width/cap/join/miter values, solid dash state, default colors, default color spaces, ignored pattern names, empty ExtGState names, or tolerant empty-stack restores.
Malformed text-state/text-showing operands and text-object sequence errors likewise return typed descriptor/display-list/render refusals, and SVG/PS vector-output classification routes them away from native text replay, instead of applying default font sizes, zero text movement/spacing, identity text matrices, empty strings, empty `TJ` arrays, ignored invalid `TJ` entries, bare text showing outside `BT`/`ET`, unmatched `ET`, nested `BT`, or unterminated text objects.
Packed retained text culling no longer uses a no-op advancement helper or returns early for culled `TJ` arrays. Non-clipping culled `Tj`, `TJ`, `'`, and `"` descriptors now advance through the active `RenderState` text path without paint, preserving decoded glyph advances, Type 3 advance metadata, spacing, and `TJ` adjustments for later visible operations; clipping text modes still replay normally because they can affect clip state.
Malformed marked-content, property-list, and compatibility-section operands plus unmatched/unterminated marked-content and compatibility-section sequences likewise return typed descriptor/display-list/render refusals, and SVG/PS vector-output classification routes them away from native replay, instead of applying empty tags, empty property lists, ignored operands, default visible state after unmatched `EMC` ends, cross-stream closures, leaked end-of-stream visibility, or unbalanced `EX`/`BX` no-ops.
Malformed optional-content visibility metadata likewise returns typed render refusals instead of treating uncertain OCG/OCMD state as visible. Malformed `/OCProperties`, malformed `/D` or `/OCGs`, malformed OCG dictionaries, missing property resources, cyclic references, non-OCG/OCMD objects, empty or malformed OCMD `/OCGs`, and unsupported OCMD `/P` policies now fail before immediate or retained marked-content visibility mutation, and object-level XObject/annotation/shading/pattern `/OC` decisions use the same strict path.
Malformed page path construction, painting, clipping operands, and dangling/repeated `W/W*` clip sequences likewise return typed display-list/render refusals, and SVG/PS vector-output classification routes malformed clip sequences away from native path replay. Valid empty or move-only clipping paths now install an explicit empty clip instead of leaving later paint visible.
Malformed resource invocation operands likewise return typed display-list/render refusals, and SVG/PS vector-output classification routes them away from regional replay, instead of silently no-oping missing `Do`/`sh` resource names, using empty resource names, or ignoring extra operands before XObject/Form/image/shading dispatch.
Type 3 path-only CharProc geometry extraction now refuses malformed path construction, graphics-state, color, transform, dash, and metric operands, and refuses path segments that need but lack a current point. This prevents cached Type 3 geometry or clipping paths from using default line state, identity `cm` operands, solid dash state, ignored path segments, or extra color operands.
Generic Type 3 `d0`/`d1` metric descriptors now also fail closed across retained descriptor compilation, display-list capture, immediate page dispatch, and SVG/PS vector-output classification. Wrong-count, wrong-type, or non-finite glyph displacement/bbox operands no longer synthesize zero widths, zero bounding boxes, or malformed native vector output. Present malformed Type 3 `/FontMatrix` values now fail before glyph CTM construction instead of defaulting to the 0.001 scale. Font-level `/FontBBox` arrays used only for bounded rendered-glyph cache surfaces now require exact four-number finite values instead of truncating overlong arrays; malformed font bounds decline the bounded cache path and leave full CharProc rendering available.
Malformed soft-mask dictionaries/references, malformed present SMask `/BC` backdrop arrays, and unsupported or malformed SMask `/TR` transfer functions likewise return typed `UnsupportedFeature` instead of rendering the source content without the requested mask, silently using a default backdrop, or silently using identity transfer; `/SMask /None` remains the explicit mask-clear case, and absent `/BC` still uses the default backdrop.
Active image decode and image mask failures, including malformed inline image sequencing (`BI`/`ID`/data/`EI` ordering), malformed inline image parameter pairs, missing or invalid inline image dimensions, missing or unsupported non-mask inline `/BitsPerComponent` metadata, malformed inline `DecodeParms`, malformed inline/Image XObject/explicit-mask `/Filter` metadata, missing or non-name non-mask inline `/ColorSpace` metadata, malformed image `/Decode` arrays with short, overlong, nonnumeric, or non-finite entries, unsupported or malformed image color spaces, unresolved Separation/DeviceN tint-space transforms, decoded-length mismatches, JPX color/alpha channel length mismatches, malformed stencil image-mask `/Decode` arrays, malformed image SMask references/decode output or `/Matte` arrays, SMask alpha-buffer/channel conflicts, pre-alpha main-image conflicts, malformed color-key `/Mask` arrays, dimension-mismatched explicit mask streams, image-mask stencil buffer mismatches, and explicit-mask composition buffer mismatches, likewise return typed `UnsupportedFeature`/`MalformedPdf` instead of dropping the image, accepting `ID` without `BI`, fabricating empty-parameter retained inline images, repairing non-name/dangling/duplicate inline parameter pairs into smaller dictionaries, treating unterminated `ID` or missing `EI` as a no-op, painting unterminated inline bytes, treating invalid inline dimensions as a no-op, defaulting or clamping ambiguous inline bpc metadata, dropping non-name filter-array entries before terminal-codec metadata decisions, defaulting ambiguous inline bytes to grayscale, treating unknown color spaces as raw RGB/gray or malformed Indexed palettes as gray, rendering unresolved tint spaces as grayscale, ignoring, filtering, or truncating the decode mapping, ignoring stencil-mask decode polarity or matte data, padding/truncating decoded pixels, synthesizing opaque JPX alpha, sampling/padding stencil or mask samples, dropping pre-existing alpha, or rendering it without its requested mask.
Active image boolean metadata with malformed present inline/Image XObject `/ImageMask` or `/Interpolate` values, or referenced explicit-mask `/ImageMask` values, likewise returns typed `MalformedPdf` before bpc/color-space planning, decode cache-key construction, mask decode planning, or scaled-image fast-path selection instead of defaulting the booleans to `false`. SVG/PS vector-output classification and regional XObject replay apply the same fail-closed boolean preflight to long and short image keys before bounded regional output. Lower-level raw image normalization and CCITT DecodeParms parsing now also require actual PDF booleans instead of accepting name-valued `true`/`false` spellings.
Active image XObjects now share the same metadata-first fail-closed policy for required `/Width` and `/Height`, non-mask `/BitsPerComponent`, and non-mask `/ColorSpace` entries. Missing or invalid dimensions no longer become `1x1` cache/planner inputs, non-mask bpc no longer defaults to 8 or clamps ambiguous values, and missing non-mask color spaces no longer default to `/DeviceRGB`; terminal JPX codestreams remain the explicit sample-depth/color-space exception.
Referenced explicit image Mask streams and image SMask streams likewise fail closed on malformed metadata before decode. Explicit masks no longer inherit the source image dimensions, non-stencil explicit masks no longer default missing bpc or color-space metadata, and image SMask streams no longer inherit source dimensions, default bpc to 8, or default color space to `/DeviceGray`.
Active color-key image `/Mask` arrays likewise require exact finite integer source-sample ranges for the resolved DeviceGray/RGB component count. Nonnumeric, non-finite, fractional, out-of-range, overlong, short, or reversed ranges return typed malformed-image refusal instead of filtering bad entries, ignoring extras, clamping, rounding, swapping, padding short source pixels, or rendering the image unmasked.
Terminal JPX, CCITT, and JBIG2 decoded-output length mismatches now fail typed from `images/jpx.rs`, `images/ccitt.rs`, and `images/jbig2.rs` instead of warning and padding or truncating synthesized pixels.
Unsupported ICC/unknown JPX channel counts now fail typed from `images/jpx.rs` instead of passing unconverted samples to renderer samplers that would paint fallback black pixels.
DCT dictionary/header dimension mismatches now fail typed from `images/decoder.rs` instead of warning and using JPEG header dimensions.
Short packed 1/2/4-bit image rows now fail typed from `images/decoder.rs` instead of zero-filling missing source bytes.
DCT component-count mismatches against the declared PDF image color space now fail typed from `images/decoder.rs`, except for the valid declared-CMYK conversion path.
Malformed 16-bit image sample byte counts now fail typed from `images/decoder.rs` instead of accepting a trailing half sample as complete output.
Converted Indexed palette length mismatches now fail typed from `images/decoder.rs` instead of synthesizing black palette entries.
Alpha-bearing JPX stored decoded-byte mismatches now fail typed from `images/jpx.rs` before alpha splitting instead of allowing `chunks_exact` to drop trailing bytes.
JPX alpha composition now validates gray/RGB color bytes and alpha bytes against declared dimensions before composing RGBA, so helper reuse cannot synthesize opaque alpha or drop malformed color tails.
SVG/PS regional image and stencil-mask emitters now validate decoded `RawImage` byte counts before SVG PNG embedding, PostScript `colorimage`, or PostScript `imagemask` output; PostScript regional colorimage output skips fully transparent RGBA, clips binary 0/255 RGBA exactly before `colorimage`, and refuses fractional RGBA instead of silently dropping alpha. Malformed regional buffers now fail typed instead of sampling missing bytes as zero or synthesizing bounded black/transparent output.
SVG/PS native calibrated shading classification now validates CalGray, CalRGB, and Lab parameter dictionaries before converting gradient endpoints; malformed calibrated shading spaces no longer use default Cal/Lab parameters for native vector gradients.
Active named color-space resolution now accepts valid bare `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` resource aliases, validates CalGray, CalRGB, and Lab parameter dictionaries/components, propagates malformed Separation/DeviceN calibrated alternates as invalid, and refuses unsupported alternate spaces instead of converting them through the default black component fallback. Active page fill/stroke paths record missing, unsupported, or invalid named color resources as typed `UnsupportedFeature`; image tint conversion fails typed on invalid named color spaces; SVG/PS/shading/vector helper paths do not substitute black for invalid named colors.
SVG/PS vector-output classification now also preflights named path/text/stencil-mask paint before native replay. Resource-named device paint remains vector-safe only with exact finite components, while unresolved resources, unsupported names, malformed resolved spaces, invalid tint/calibrated spaces, or incomplete component vectors route to the whole-page typed raster/refusal boundary before SVG/PS sinks can substitute generic black.
Image SMask `/Matte` and family-name-only luminosity SMask `/BC` group color conversion use a device-only component conversion helper; unsupported or calibrated family names in those contexts fail typed instead of using default calibrated parameters or the generic black fallback. When a luminosity SMask has a resolved non-device group color-space object, well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN `/BC` values now use the named-color/CMM resolver instead.
Device-family-only color conversion now requires exact finite component counts for `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK`; image SMask `/Matte`, SMask `/BC`, and SVG/PS named-paint preflight no longer use defaulted, padded, truncated, or non-finite device vectors when only a family name is available.
Runtime fallback policy now reports FB-06 Type 3 retained subplans as a bounded immutable retained CharProc plan cache rather than a partial sublist state machine. The material-degradation policy is unchanged: unsupported or unresolved Type 3 CharProc content remains typed refusal and ordinary font substitution remains refused.

| ID | Previous decision | Current source disposition | Exact source | Classification | Verification |
|---|---|---|---|---|---|
| FB-01 | Unsupported retained list delegates to immediate renderer | **Removed.** Compatibility and high-quality retained full-page, tile, band, cached, contract, display-list, and progressive render paths now return typed `UnsupportedFeature` for unsupported retained display-list replay instead of delegating to canonical raw/immediate dispatch | `render/page_renderer.rs`, `render_page_cancellable_with_mode`, `render_page_cancellable_with_mode_and_cache`, `render_page_cancellable_with_contract_policies_and_cache`, `render_page_display_list_with_mode`, `render_display_list_cancellable_with_mode`, `render_display_list_cancellable_with_mode_and_cache`, `render_page_display_list_tile_cancellable_with_mode_and_cache`; `render/progressive.rs` | Typed unsupported retained-list refusal | `high_quality_display_list_page_helpers_refuse_unsupported_list`, `high_quality_display_list_tile_refuses_unsupported_list`, `compat_display_list_bands_refuse_unsupported_list_without_immediate_fallback`, and progressive unsupported-list tests |
| FB-02 | Unsupported retained tile delegates to immediate tile renderer | **Removed.** Compatibility and high-quality retained tile rendering return typed `UnsupportedFeature`; public tile helpers and band rendering propagate that refusal instead of using canonical immediate-tile fallback | `render/page_renderer.rs`, `render_page_display_list_tile_cancellable_with_mode_and_cache`; `render_page_tile_cancellable_with_mode`; `render_page_bands_with_mode` | Typed unsupported retained-tile refusal | `high_quality_display_list_tile_refuses_unsupported_list` and `compat_display_list_bands_refuse_unsupported_list_without_immediate_fallback` |
| FB-03 | Unsupported progressive tile delegates to immediate tile renderer | **Removed.** Compatibility and high-quality progressive rendering now propagate typed `UnsupportedFeature`, transition the job to `Failed`, and record no fallback publication when retained display-list tile replay is unsupported | `render/progressive.rs` | Typed unsupported retained-tile refusal | `compat_progressive_refuses_unsupported_display_list_tile_without_immediate_fallback`, progressive unit tests, and `high_quality_progressive_refuses_unsupported_display_list_tile` |
| FB-04 | Recursive pattern painted a solid colour or malformed active pattern paint could be skipped | **Removed.** Root renderer records typed missing-resource, missing active pattern-name, unsupported PatternType, non-stream PatternType 1, malformed tiling geometry including `/BBox`, `/XStep`, `/YStep`, `/PaintType`, `/TilingType`, and `/Matrix`, and recursive-pattern failures; retained display-list compilation also refuses missing active pattern resources; no solid paint or dropped-paint no-op is emitted | `render/display_list.rs`, `push_stateful_path_run`; `render/page_renderer.rs`, `paint_pattern_*`, `paint_tiling_pattern_with_device_clip` | Exact typed refusal | `missing_pattern_resource_returns_typed_refusal`, `missing_pattern_resource_is_explicitly_unsupported`, `pattern_color_space_without_pattern_name_returns_typed_refusal`, `unsupported_pattern_type_returns_typed_refusal`, `tiling_pattern_without_stream_returns_typed_refusal`, `malformed_tiling_pattern_geometry_returns_typed_refusal`, `recursive_tiling_pattern_returns_typed_refusal` |
| FB-05 | Excessive pattern cell count painted a solid colour | **Removed.** Root renderer records a typed exact render-limit failure | `render/page_renderer.rs`, pattern cell-limit branch | Typed resource-limit refusal | source guard; shared failure propagation |
| FB-06 | Type 3 compatibility font could precede native charproc rendering, path-only geometry extraction could default malformed path-state operands, generic `d0`/`d1` metric handling could default malformed glyph metrics, present malformed `/FontMatrix` could default to the 0.001 scale, and overlong font-level `/FontBBox` could seed truncated rendered-glyph cache bounds | **Removed as a silent degraded fallback.** Type 3 glyphs render only from PDF Encoding CharProc names; missing names, unsupported charprocs, unresolved programs, recursive CharProcs, malformed path-only geometry, malformed generic `d0`/`d1` metric operators, and present malformed `/FontMatrix` values return typed `UnsupportedFeature`, display-list unsupported diagnostics, whole-page vector classification reaching typed refusal, or decline native geometry/CTM extraction instead of using Unicode-derived aliases, ordinary font bytes, skipped glyph paint, default line state, identity transforms, solid dash state, ignored path segments, fabricated color operands, zero widths, zero bounding boxes, or default glyph CTM scale. Malformed font-level `/FontBBox` values decline bounded rendered-glyph cache admission instead of truncating source bounds, and parsed CharProc/geometry caches now track explicit `Unvisited`/`Compiling`/`Compiled`/`Failed` lifecycle states with retained failure reasons. | `render/page_renderer.rs`, `render_text_string`, `render_type3_glyph`, `render_type3_charproc_full_with_ctm`, `Type3PathCollector`, `type3_font_matrix`, `bbox_from_pdf_array`; `render/plan.rs`, `type3_glyph_metric_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::dispatch`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Typed refusal for unresolved/recursive Type 3, malformed clipping geometry, malformed metric descriptors, and malformed present FontMatrix values; exact full CharProc rendering remains available when malformed font-level FontBBox declines bounded cache admission; parsed program cache lifecycle is explicit; resource-backed image, Form XObject, and named shading CharProcs have retained-path coverage; broader unsupported CharProc state/resource coverage remains partial | `type3_charproc_renders_resource_xobject_image`, `type3_charproc_renders_resource_form_xobject`, `type3_charproc_renders_resource_shading`, `type3_missing_encoding_name_returns_typed_refusal`, `recursive_type3_charproc_returns_typed_refusal`, `type3_path_collector_rejects_malformed_graphics_state_operands`, `type3_path_collector_rejects_path_segments_without_current_point`, `type3_clipping_charproc_malformed_line_width_returns_typed_refusal`, `type3_glyph_metric_operand_validator_rejects_malformed_operands`, `malformed_type3_glyph_metric_marks_display_list_unsupported`, `malformed_type3_glyph_metric_operator_returns_typed_refusal`, `malformed_type3_font_matrix_returns_typed_refusal`, `type3_font_bbox_requires_exact_numeric_array`, `type3_program_cache_tracks_explicit_lifecycle_states`, `malformed_operands_stay_whole_page_vector_fallback`, `type3` |
| FB-07 | Bundled font substitution | Compatibility-mode deterministic replacement remains for missing/non-embedded fonts; substitution events now report high-level reason, provider selection reason, metric posture, Standard14, document-provided, caller-registered, deterministic system, and bundled resolution sources; caller-registered Rust font bytes are selected before bundled fallback and salted into font-policy/cache identity; FontDescriptor Symbolic/Nonsymbolic, Italic, ForceBold, and FontWeight hints feed provider selection so unknown non-Standard14 symbolic descriptor fonts route to the symbolic coverage face and record selection reason `symbolic_flag`; high-quality render mode plus explicit `HighQualityExact` contract rendering accept valid caller-registered and deterministic-system replacements but refuse generic bundled replacement with a typed unsupported error when no valid embedded/document/system/registered replacement exists | `render/font_rasterizer.rs`; `fonts/provider.rs`; `page_renderer.rs`; `render/font_substitution_report.rs` | Compatibility diagnostic fallback; high-quality typed refusal for generic bundled replacement | font resolver/report tests, exact-contract refusal test, `font_substitution_report`, `registered_provider_matches_subset_names_and_fingerprints_bytes`, `high_quality_render_accepts_caller_registered_font_mapping`, `document_font_cache_key_includes_contract_budget_and_font_provider_identity`, `symbolic_descriptor_hint_routes_unknown_fonts_to_symbol_coverage_face`, `symbolic_descriptor_routes_generic_compat_fallback_to_coverage_face`, and `high_quality_render_refuses_generic_bundled_font_substitution` |
| FB-08 | Missing/malformed active shading silently painted nothing or direct helpers substituted local defaults | **Removed from retained and immediate replay for active malformed-resource cases, and tightened below the public validator for direct helpers.** Display-list build records `UnsupportedRenderOp { operator: "sh", reason: ... }`; immediate named-shading paint, Type 1-7 required-field preflight failures, unsupported or malformed functions, unavailable mesh streams, unsupported shading types, and malformed shading-pattern paint record typed `UnsupportedFeature` errors instead of logging and continuing. Direct Type 1-3 helpers now also refuse singular/non-finite paint CTMs and singular Type 1 shading matrices instead of substituting identity inverse transforms, and mesh vertex/patch readers use exact validated `/Decode` indices without per-component defaults | `render/display_list.rs`, `push_native_shading`; `render/page_renderer.rs`, `handle_sh`, `paint_shading_pattern_with_device_clip`; `render/shading.rs`, `invert_transform_or_decline`, `MeshDecode::from_dict` | Explicit unsupported diagnostic or local direct-helper refusal | `missing_named_shading_is_explicitly_unsupported`, `missing_named_shading_render_returns_typed_refusal`, `malformed_named_shading_render_returns_typed_refusal`, `malformed_active_shading_dictionary_returns_typed_refusal`, `malformed_shading_pattern_returns_typed_refusal`, `direct_shading_helpers_reject_singular_paint_ctm`, `shading_` |
| FB-09 | Former JPX-only image sampler branch | Removed as a sampler fallback. JPX decoded pixels now use the canonical image sampler, guarded downscale plans use `hayro-jpeg2000` target-resolution decode when no image mask or full-image postprocessing step requires the original sample grid, and codec-native component/tile/progressive limitations are explicit capability fields; codec-native ROI/region, tile pixel output, and progressive output remain incomplete and capability-reported | `render/image_painter.rs`; `render/page_renderer.rs`; `images/jpx.rs`; `images/decoder.rs`; `render/image_decode_planning.rs` | No sampler-specific material quality degradation remains; JPX ROI/region/tile/progressive codec behavior is now reported as a capability/resource coverage limit rather than a visual fallback policy | source branch removal plus JPX capability/planner/reduced-finalizer/component/tile tests |
| FB-10 | Portable qcms colour path | Retained as an explicit supported portable backend; rendering intent reaches active ICC/named-color CMM paths and transform cache identity; explicit `ColorManagementPolicy` now selects the active ICC backend policy for contract paint/image paths and raw image cache identity, `DeterministicFallback` avoids ICC backend transforms, unavailable `NativeLittleCms` fails typed, and `Proof` contracts fail closed unless native CMM/OutputIntent proofing is available; `OverprintPolicy::Preview` gates active PDF graphics-state DeviceCMYK overprint preview while separation-preserving overprint remains typed unsupported; overprint-preview CMYK dispatch now requires exactly four finite active DeviceCMYK components before preview blending instead of padding malformed internal state with zero inks; native LittleCMS feature source remains available | `render/cmm.rs`; `render/colorspace.rs`; `images/decoder.rs`; `render/page_renderer.rs`; `render/print_profile.rs`; `native-cmm-lcms2` | Supported backend selection, active intent/CMM/overprint-preview policy for portable ICC/DeviceCMYK paths, plus typed unavailable-backend, proof-CMM, separation-preserving refusals, and helper-level malformed CMYK preview decline | focused `rendering_intent`, `color_management_policy`, `deterministic_fallback_policy_disables_icc_backend`, `overprint_preview`, `device_cmyk_components_requires_exact_finite_state`, image cache identity, `native_littlecms`, `preserve_separations`, proof CMM validation tests |
| FB-11 | SVG whole-page raster embed | Compatibility SVG export may still emit an explicit full-page raster when unsupported local vector constructs cannot be represented, but strict SVG export now refuses that path with typed `UnsupportedFeature`; finite affine image XObjects including stencil masks, complete finite-affine inline images, inline image masks, vector-safe Forms including no-op, opaque-inert isolated/knockout transparency-group Forms, opaque normal DeviceGray/DeviceRGB/DeviceCMYK group-colour transparency Forms, and opaque normal well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN group-colour transparency Forms, simple linear DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved CalGray/CalRGB/Lab/Separation axial shadings, finite/invertible affine-transformed DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved CalGray/CalRGB/Lab/Separation radial shadings, simple finite-matrix shading-pattern fill/stroke path paints and glyph-outline text paints, normal-blend scalar `CA`/`ca` ExtGState opacity, safe line cap/join/miter/dash/font/flatness/smoothness/default-state/identity-transfer/first-supported-normal-blend-array ExtGState dictionaries, stroked/fill-stroke text outlines with vector-safe line styling, font-resolved text-clipping glyph clips, composed SVG clips, dense ordinary text, and overwritten dead pattern color state are native/bounded where supported; accepted shading `/BBox` clip materialization still fails typed if SVG replay cannot flatten the device-space clip | `render/vector_fallback.rs`; `render/svg.rs`; `engine.rs`; `runtime.rs` | No high-quality/exact material degradation; compatibility-only raster export remains explicit and reports `is_rasterized` | `calibrated_group_color_space`, `indexed_group_color_space`, `iccbased_group_color_space`, `device_group_color_space`, `separation_group_color_space`, `devicen_group_color_space`, `svg_strict_refuses_alpha_group_whole_page_raster_fallback`, runtime policy test, SVG output, resource-bearing tiling-cell, regional fallback, and `shading_bbox_clip_refuses_unflattenable_geometry` tests |
| FB-12 | PS/EPS raster embed / missing ExtGState semantics | Compatibility PS/EPS export may still emit an explicit full-page raster when unsupported local vector constructs cannot be represented, but strict PS/EPS and strict PS-document export now refuse that path with typed `UnsupportedFeature`; finite affine image XObjects including stencil masks, complete finite-affine inline images, inline image masks, vector-safe Forms including no-op, opaque-inert isolated/knockout transparency-group Forms, opaque normal DeviceGray/DeviceRGB/DeviceCMYK group-colour transparency Forms, and opaque normal well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN group-colour transparency Forms, simple linear DeviceGray/DeviceRGB/DeviceCMYK plus direct/resource-resolved CalGray/CalRGB/Lab/Separation axial shadings, direct DeviceRGB/DeviceGray Type 2 exponent axial/radial shadings, exact DeviceCMYK Type 2 shadings, exact Type 3 stitching shadings, safe opaque ExtGState dictionaries, stateful exact no-paint PostScript alpha/blend operations, fully transparent regional RGBA no-ops, binary 0/255 regional RGBA alpha clips, stroked/fill-stroke text outlines, font-resolved text-clipping glyph clips, PostScript `imagemask` stencil emission, dense ordinary text, and overwritten dead pattern color state are native/bounded where supported | `render/vector_fallback.rs`; `render/postscript.rs`; `engine.rs`; `runtime.rs` | No high-quality/exact material degradation; compatibility-only raster export remains explicit and reports `is_rasterized` | `calibrated_group_color_space`, `indexed_group_color_space`, `iccbased_group_color_space`, `device_group_color_space`, `separation_group_color_space`, `devicen_group_color_space`, `ps_strict_refuses_alpha_group_whole_page_raster_fallback`, `ps_strict_refuses_normal_alpha_extgstate_whole_page_raster_fallback`, runtime policy test, `regional_vector_fallback`, `visible_postscript_alpha_or_blend_paint_stays_whole_page`, `postscript_transparent_non_normal_blend_paint_is_stateful_vector_noop`, resource-bearing tiling-cell, alpha, and PostScript structural tests |
FB-12 exact DeviceCMYK function-array continuation: direct `/DeviceCMYK` Type 2 function arrays now stay regional for PostScript/EPS when all four one-component functions are finite and unit-domain. Same-exponent arrays collapse to one exact CMYK `/FunctionType 2` sidecar, while mixed-exponent arrays serialize as exact per-component `/Function` arrays under `/ColorSpace /DeviceCMYK`; SVG and inexact nonlinear cases remain conservative.

| FB-13 | Missing active ExtGState resource kept default state, and malformed render-state metadata could preserve, clamp, or ignore default alpha/blend/font/line/semantic state | **Removed.** Display-list compilation records an unsupported `gs` diagnostic for missing or malformed resource names, and immediate/packed descriptor replay records a typed `UnsupportedFeature` instead of logging and continuing with default alpha/blend/font/line state. Present alpha, line style, miter, blend, rendering intent, overprint, stroke-adjustment, alpha-source, text-knockout, transfer-function, flatness, smoothness, dash, type, and font metadata must be well formed before state mutation. Well-formed paint-dependent `/SA`, `/AIS`, and `/TK` values are retained in graphics state and draw-state identity; visible paint that would observe unsupported stroke-adjustment, alpha-source, or disabled-text-knockout semantics fails typed at active, retained, and packed replay paint boundaries | `content/state.rs`, `try_apply_ext_g_state`; `render/display_list.rs`, `apply_ext_g_state`; `render/page_renderer.rs`, `RenderState::apply_ext_g_state`, `RenderStatePlanAdapter::{ApplyExtGState,dispatch_fill,dispatch_stroke}`; `render/plan.rs`, `PackedDisplayList::compile_with_resources` | Exact typed refusal | `missing_extgstate_resource_returns_typed_refusal`, `malformed_extgstate_blend_mode_returns_typed_refusal`, `malformed_extgstate_state_metadata_returns_typed_refusal`, `try_apply_ext_gstate`, `extgstate_stroke_adjustment_refuses_only_visible_stroke_paint`, `extgstate_alpha_source_refuses_visible_nonstroking_paint`, and `extgstate_disabled_text_knockout_refuses_visible_text_paint` |
| FB-14 | Malformed soft masks rendered source content without a mask, used default backdrop, or used identity transfer | **Removed for malformed SMask dictionaries/references, backdrop colors, and transfer functions.** Missing `/G`, non-dictionary SMask references, unsupported SMask values, malformed or unsupported `/S` subtypes, `/G` not resolving to a Form stream, `/G` decode/parse failures, malformed present `/BC` arrays, unsupported or malformed `/TR` transfer functions, and offscreen allocation refusal now record typed `UnsupportedFeature`; `/SMask /None` remains explicit mask clearing, and absent `/BC` still uses the default backdrop | `render/page_renderer.rs`, `apply_ext_g_state_smask`, `apply_smask`, `smask_backdrop_color`, `build_transfer_lut` | Exact typed refusal | `smask_missing_group_returns_typed_refusal`, `unsupported_smask_subtype_returns_typed_refusal`, `malformed_smask_subtype_returns_typed_refusal`, `alpha_smask_malformed_backdrop_color_returns_typed_refusal`, `unsupported_smask_transfer_function_returns_typed_refusal`, `smask` focused tests |
| FB-15 | Active image decode or image mask failures dropped paint, fabricated malformed inline images, treated invalid inline image dimensions as no-ops, defaulted/clamped missing or unsupported inline image bpc metadata, defaulted missing inline image color metadata, ignored filter/decode/color/matte data, rendered unresolved tint spaces as grayscale, padded/truncated decoded bytes, sampled or padded the wrong mask channel data, dropped pre-existing alpha, or rendered unmasked | **Removed for active image decode/mask failures.** Malformed inline image sequence boundaries (`BI`/`ID`/data/`EI`), malformed inline image parameter pairs, missing or invalid inline image `/Width` or `/Height`, missing or unsupported non-mask inline `/BitsPerComponent` except terminal JPX codestreams, malformed inline `DecodeParms`, malformed inline/Image XObject/explicit-mask `/Filter` metadata, malformed active, lower-level decoder, CCITT DecodeParms, and SVG/PS regional image boolean metadata, missing or non-name non-mask inline `/ColorSpace` metadata, malformed image `/Decode` arrays with short, overlong, nonnumeric, or non-finite entries, unsupported or malformed image color spaces, unresolved Separation/DeviceN tint-space transforms, decoded-length mismatches, malformed inline/XObject/explicit image-mask `/Decode` arrays, inline image decode, image XObject decode, image XObject SMask loading, explicit `/Mask` loading, non-stream `/Mask` references, unsupported `/Mask` values, malformed image SMask references/decode output or `/Matte` arrays, SMask alpha-buffer/channel conflicts, pre-alpha main-image conflicts, malformed color-key `/Mask` arrays, dimension-mismatched explicit mask streams, short image-mask stencil buffers, and short explicit-mask composition buffers now record typed `UnsupportedFeature`/`MalformedPdf` or retained unsupported diagnostics instead of continuing with no-op, accepted out-of-order inline markers, repaired non-name/dangling/duplicate parameter pairs, empty-parameter inline images, unterminated-payload paint, default-bpc, clamped-bpc, missing, default-mapped, malformed-filter-entry dropping, boolean defaulting, filtered/truncated decode mappings, raw-color-space, grayscale-tint-space, padded/truncated, incorrectly-unmatted, wrong-polarity stencil, wrong-alpha-channel, alpha-dropping, or unmasked pixels | `render/display_list.rs`, `push_native_inline_image`; `render/page_renderer.rs`, `dispatch_all`, `paint_inline_image`, `handle_do_image`, `scheduled_load_explicit_image_mask`, `image_mask_paints_ones`, `inline_image_mask_paints_ones`, `image_mask_to_stencil_rgba`, `combine_explicit_image_mask`; `render/vector_fallback.rs`, `classify_ops_for_vector_output`; `render/svg.rs`, `emit_regional_image`; `render/postscript.rs`, `emit_regional_image`; `images/decoder.rs`, `build_raw_image`, `ccitt_decode_params`, `apply_decode_array`, `decode_indexed`, `tint_space_to_rgba`, `ensure_decoded_len`; `images/smask.rs`, `load_and_combine_with_limits`, `combine_rgba_with_matte`, `smask_matte_rgb` | Exact typed refusal | `malformed_inline_image_parameters_return_parse_error`, `malformed_inline_image_parameters_return_typed_refusal`, `renderer_inline_image_malformed_parameter_pairs_return_typed_refusal`, `malformed_inline_image_parameters_stay_whole_page_fallback`, `malformed_inline_image_sequence_marks_display_list_unsupported`, `malformed_inline_image_sequence_returns_typed_refusal`, `inline_image_triggers_whole_page`, `inline_image`, `renderer_inline_image_missing_width_returns_typed_refusal`, `renderer_inline_image_zero_height_returns_typed_refusal`, `renderer_inline_image_missing_bits_per_component_returns_typed_refusal`, `renderer_inline_image_unsupported_bits_per_component_returns_typed_refusal`, `renderer_inline_image_missing_color_space_returns_typed_refusal`, `renderer_inline_image_mask_without_color_space_is_allowed`, `inline_image_decode_params_without_filter_returns_typed_refusal`, `renderer_inline_image_malformed_filter_returns_typed_refusal`, `renderer_image_xobject_malformed_filter_returns_typed_refusal`, `image_xobject_explicit_mask_malformed_filter_returns_typed_refusal`, `renderer_inline_image_malformed_booleans_return_typed_refusal`, `renderer_image_xobject_malformed_booleans_return_typed_refusal`, `image_xobject_explicit_mask_malformed_boolean_returns_typed_refusal`, `classifier_malformed_image_xobject_boolean_metadata_stays_whole_page`, `classifier_malformed_inline_image_metadata_stays_whole_page`, `build_raw_image_rejects_malformed_image_mask_boolean`, `ccitt_params_reject_name_valued_booleans`, `build_raw_image_rejects_malformed_decode_array`, `build_raw_image_rejects_mismatched_buffers`, `build_raw_image_uses_one_channel_for_image_mask`, `resource_separation_image_color_space_uses_tint_transform`, `malformed_resource_separation_image_color_space_returns_typed_refusal`, `image_mask_to_stencil_rejects_short_buffer`, `explicit_image_mask_rejects_short_main_buffer`, `explicit_image_mask_rejects_short_mask_buffer`, `image_xobject_malformed_decode_array_returns_typed_refusal`, `image_xobject_unknown_color_space_returns_typed_refusal`, `image_xobject_malformed_indexed_color_space_returns_typed_refusal`, `image_xobject_mask_malformed_decode_array_returns_typed_refusal`, `image_xobject_explicit_mask_malformed_decode_array_returns_typed_refusal`, `inline_image_mask_malformed_decode_array_returns_typed_refusal`, `image_xobject_decode_failure_returns_typed_refusal`, `image_xobject_smask_dimension_mismatch_returns_typed_refusal`, `image_xobject_smask_empty_matte_returns_typed_refusal`, `combine_rgba_rejects_short_mask_buffer`, `combine_rgba_rejects_multichannel_mask_buffer`, `combine_rgba_rejects_prealpha_main_image`, `smask_matte_rgb_rejects_empty_matte_array`, `image_xobject_malformed_color_key_mask_returns_typed_refusal`, `image_xobject_mask_dimension_mismatch_returns_typed_refusal`, `image_xobject`, `inline_image`, `xobject_resource_`, `decode_array`, `malformed_filter`, `malformed_booleans`, and `smask_combine_rgba_rejects_dimension_mismatch` focused tests |
| FB-16 | Malformed active graphics-state operands or restore underflow substituted identity/default state | **Removed for non-text graphics-state operands and stream-local `Q` underflow.** Wrong-count, nonnumeric, non-finite, out-of-range, negative, malformed dash-array, malformed name, malformed color-component, and malformed pattern-name operands for `q`, `Q`, `cm`, `w`, `J`, `j`, `M`, `d`, `ri`, `i`, direct device colors, `CS`, `cs`, `SC/SCN/sc/scn`, and `gs` now produce `GraphicsStateDescriptor::Unsupported`, display-list `UnsupportedRenderOp`, active render `UnsupportedFeature`, or whole-page vector-output classification before state mutation or SVG/PS replay; `Q` with no saved graphics state for the current stream/Form likewise fails typed instead of calling the tolerant empty-stack restore path | `render/plan.rs`, `graphics_state_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::dispatch`; `render/page_renderer.rs`, `RenderState::dispatch`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Exact typed refusal | `graphics_state_descriptor_rejects_malformed_state_operands`, `malformed_graphics_state_marks_display_list_unsupported`, `malformed_graphics_state_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, `graphics_state` |
| FB-17 | Malformed active text operands or text-object sequences substituted empty/default text state | **Removed for text-state/text-showing operands and `BT`/`ET` object boundaries.** Wrong-count, non-string, non-array, nonnumeric, non-finite, out-of-range text rendering modes, malformed `TJ` array entries for `BT`, `ET`, `Tf`, `Td`, `TD`, `Tm`, `T*`, `Tc`, `Tw`, `Tz`, `TL`, `Tr`, `Ts`, `Tj`, `TJ`, `'`, and `"`, nested `BT`, unmatched `ET`, showing/positioning operators outside an active text object, and unterminated `BT` now produce `GraphicsStateDescriptor::Unsupported`, display-list `UnsupportedRenderOp`, active render `UnsupportedFeature`, or whole-page vector-output classification before retained capture, text paint, state mutation, or SVG/PS text replay | `render/plan.rs`, `text_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::dispatch`, `RetainedTextOp::from_content_operation`; `render/page_renderer.rs`, `RenderState::dispatch`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Exact typed refusal | `text_operand_validator_rejects_malformed_text_operands`, `malformed_text_marks_display_list_unsupported`, `malformed_text_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, `text_is_replayable_as_native_operation`, `text` |
| FB-18 | Malformed active marked-content operands or sequences substituted empty/default marked-content state or unbalanced compatibility-section no-ops | **Removed for marked-content, property-list, compatibility-section operands, and balanced sequence boundaries.** Wrong-count and wrong-type operands for `BMC`, `BDC`, `EMC`, `MP`, `DP`, `BX`, and `EX`, unmatched active `EMC` ends, cross-stream `EMC` closures, unmatched `EX`, and unterminated `BMC`/`BDC`/`BX` begin operators now produce `GraphicsStateDescriptor::Unsupported`, display-list `UnsupportedRenderOp`, active render `UnsupportedFeature`, or whole-page vector-output classification before retained capture, optional-content visibility mutation, compatibility-section no-oping, or SVG/PS replay | `render/plan.rs`, `marked_content_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::{dispatch,finish}`; `render/page_renderer.rs`, `RenderState::{dispatch,dispatch_all}`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Exact typed refusal | `marked_content_operand_validator_rejects_malformed_operands`, `malformed_marked_content_marks_display_list_unsupported`, `unterminated_marked_content_marks_display_list_unsupported`, `unbalanced_compatibility_section_marks_display_list_unsupported`, `malformed_marked_content_operator_returns_typed_refusal`, `unterminated_marked_content_operator_returns_typed_refusal`, `unbalanced_compatibility_section_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, `marked_content` |
| FB-19 | Malformed active path operands dropped construction input, fabricated current points, or left empty clips visible | **Removed for page path construction, painting, clipping operands, and clip sequence boundaries.** Wrong-count, nonnumeric, non-finite, or extra operands for `m`, `l`, `c`, `v`, `y`, `h`, `re`, `S`, `s`, `f/F`, `f*`, `B`, `B*`, `b`, `b*`, `n`, `W`, and `W*`, dangling or repeated `W/W*`, plus continuation operators without active current points, now produce display-list `UnsupportedRenderOp`, active render `UnsupportedFeature`, or whole-page vector-output classification before retained capture, path mutation, retained clip replay, or SVG/PS path replay; valid empty or move-only clipping paths install an explicit empty clip | `render/plan.rs`, `path_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::{dispatch,finish}`; `render/page_renderer.rs`, `RenderState::{dispatch,dispatch_all,apply_transformed_path_clip}`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Exact typed refusal or exact empty clip | `path_operand_validator_rejects_malformed_operands`, `malformed_path_marks_display_list_unsupported`, `malformed_path_operator_returns_typed_refusal`, `empty_path_clip_installs_empty_clip`, `malformed_operands_stay_whole_page_vector_fallback`, `path` |
| FB-20 | Malformed active resource invocation operands were silently ignored or partially consumed | **Removed for XObject and shading resource invocation operands.** Missing, wrong-type, or extra operands for `Do` and `sh` now produce display-list `UnsupportedRenderOp` or active render `UnsupportedFeature` before retained capture or resource dispatch, and SVG/PS vector-output classification routes malformed invocations away from regional replay into the whole-page raster path that reaches the typed renderer refusal | `render/plan.rs`, `resource_invocation_operand_refusal`; `render/display_list.rs`, `DisplayListBuilder::dispatch`; `render/page_renderer.rs`, `RenderState::dispatch`; `render/vector_fallback.rs`, `classify_ops_for_vector_output` | Exact typed refusal | `resource_invocation_operand_validator_rejects_malformed_operands`, `malformed_resource_invocation_marks_display_list_unsupported`, `malformed_resource_invocation_operator_returns_typed_refusal`, `malformed_resource_invocation_stays_whole_page_vector_fallback`, `xobject_resource`, `named_shading` |

FB-15 also covers the active image XObject metadata preflight added on 2026-08-18: missing/invalid image XObject dimensions, missing/unsupported non-mask image XObject bpc outside terminal JPX, and missing or malformed non-mask image XObject color-space metadata record typed renderer refusals before cache-key construction or decode planning. Focused evidence includes `renderer_image_xobject_missing_width_returns_typed_refusal`, the `renderer_image_xobject_` metadata refusal group, and the `image_xobject` focused test group.

FB-15 also covers the inline image sequence preflight added on 2026-08-18: retained display-list capture and active page dispatch now reject malformed `BI`/`ID`/data/`EI` ordering, including payloads without `ID`, `ID` without `BI`, dangling `BI`, pending `ID`, missing payload bytes, and payloads without closing `EI`, before they can become empty-parameter retained images, unterminated paint, or no-ops at stream end. SVG/PS vector classification keeps those malformed sequences on the whole-page typed fallback path. Focused evidence includes `malformed_inline_image_sequence_marks_display_list_unsupported`, `malformed_inline_image_sequence_returns_typed_refusal`, `inline_image_triggers_whole_page`, and `inline_image`.

FB-15 also covers inline image parameter-pair preflight: parser normalization, active renderer helper paths, and SVG/PS regional classification now reject non-name keys, dangling keys, duplicate canonical keys, and short/long alias duplicates before those operands can be repaired into a smaller valid image dictionary. Focused evidence includes `malformed_inline_image_parameters_return_parse_error`, `malformed_inline_image_parameters_return_typed_refusal`, `renderer_inline_image_malformed_parameter_pairs_return_typed_refusal`, and `malformed_inline_image_parameters_stay_whole_page_fallback`.

FB-15 also covers the explicit image Mask and image SMask metadata preflight added on 2026-08-18: missing/invalid explicit mask dimensions, missing/unsupported non-stencil explicit mask bpc/color-space metadata, invalid stencil-mask bpc, and missing/invalid image SMask dimensions, bpc, or color-space metadata record typed renderer refusals before decode. Focused evidence includes `image_xobject_explicit_mask_missing`, `image_xobject_smask_missing`, `smask_`, and the broader `image_xobject` focused test group.

FB-15 also covers the color-key image Mask range preflight added on 2026-08-18: color-key `/Mask` arrays must have exactly `2 * component_count` finite integer entries within the source sample range and in low-to-high order. Focused evidence includes `explicit_color_key_mask_rejects_nonnumeric_range_entry`, `explicit_color_key_mask_rejects_overlong_range_array`, `explicit_color_key_mask_rejects_out_of_range_or_fractional_entries`, `explicit_color_key_mask_rejects_reversed_range`, and the `color_key` focused test group.

FB-15 also covers the image Filter metadata preflight added on 2026-08-19: active inline images, Image XObjects, and referenced explicit-mask streams now require `/Filter` metadata to be absent, a single name, or an all-name array before terminal-codec bpc/color-space exceptions, cache-key construction, or mask decode planning. Focused evidence includes `renderer_inline_image_malformed_filter_returns_typed_refusal`, `renderer_image_xobject_malformed_filter_returns_typed_refusal`, `image_xobject_explicit_mask_malformed_filter_returns_typed_refusal`, and the `malformed_filter` focused test group.

FB-15 also covers the image boolean metadata preflight added on 2026-08-19: active inline images and Image XObjects now require present `/ImageMask` and `/Interpolate` entries, and referenced explicit Mask streams require present `/ImageMask`, to be booleans before bpc/color-space planning, decode-cache identity, mask decode planning, or scaled-image fast-path selection. Focused evidence includes `renderer_inline_image_malformed_booleans_return_typed_refusal`, `renderer_image_xobject_malformed_booleans_return_typed_refusal`, `image_xobject_explicit_mask_malformed_boolean_returns_typed_refusal`, and the `malformed_booleans`/`image` focused test groups.

FB-16 covers the active graphics-state operand preflight added on 2026-08-18: descriptor compilation, display-list capture, immediate page rendering, and SVG/PS vector-output classification now reject malformed non-text state operands before any default graphics state can be applied or replayed natively. Focused evidence includes `graphics_state_descriptor_rejects_malformed_state_operands`, `malformed_graphics_state_marks_display_list_unsupported`, `malformed_graphics_state_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, and `graphics_state_descriptor`.

FB-17 covers the active text operand preflight added on 2026-08-18: retained text capture, descriptor compilation for text state, immediate page text rendering, and SVG/PS vector-output classification now reject malformed text operands before any default text state, empty text capture, or native text replay can be applied. Focused evidence includes `text_operand_validator_rejects_malformed_text_operands`, `malformed_text_marks_display_list_unsupported`, `malformed_text_operator_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, and the broader `text` filter.

FB-18 covers the active marked-content and compatibility-section operand/sequence preflight added on 2026-08-18: descriptor compilation, display-list capture, immediate page rendering, packed plan replay, and SVG/PS vector-output classification now reject malformed marked-content/property operands and unbalanced sequence boundaries before any empty tag, empty property list, ignored compatibility-section operand, unmatched `EMC` default-visible state, leaked end-of-stream visibility, cross-stream closure, unmatched `EX`, unterminated `BX`, unbalanced compatibility-section no-op, or malformed native replay can be applied. Focused evidence includes `marked_content_operand_validator_rejects_malformed_operands`, `malformed_marked_content_marks_display_list_unsupported`, `unterminated_marked_content_marks_display_list_unsupported`, `unbalanced_compatibility_section_marks_display_list_unsupported`, `malformed_marked_content_operator_returns_typed_refusal`, `unterminated_marked_content_operator_returns_typed_refusal`, `unbalanced_compatibility_section_returns_typed_refusal`, `malformed_operands_stay_whole_page_vector_fallback`, and the broader `marked_content` filter.

FB-19 covers the active path operand and clipping-sequence preflight added on 2026-08-18: display-list capture, immediate page rendering, retained clip replay, and SVG/PS vector-output classification now reject malformed page path construction, painting, clipping operands, dangling `W/W*`, and repeated `W/W*` before malformed construction can be ignored, malformed paint/clip operators can affect output, empty clipping paths can remain visible, missing current points can be synthesized, or malformed paths can be emitted natively. Valid empty or move-only clipping paths now install an explicit empty clip. Focused evidence includes `path_operand_validator_rejects_malformed_operands`, `malformed_path_marks_display_list_unsupported`, `malformed_path_operator_returns_typed_refusal`, `empty_path_clip_installs_empty_clip`, `malformed_operands_stay_whole_page_vector_fallback`, and the broader `path` filter.

FB-20 covers the active resource invocation operand preflight added on 2026-08-18: display-list capture, immediate page rendering, and SVG/PS vector-output classification now reject malformed `Do` and `sh` operands before missing names can no-op, empty names can reach resource lookup, regional replay can emit malformed vector output, or extra operands can be ignored. Focused evidence includes `resource_invocation_operand_validator_rejects_malformed_operands`, `malformed_resource_invocation_marks_display_list_unsupported`, `malformed_resource_invocation_operator_returns_typed_refusal`, `malformed_resource_invocation_stays_whole_page_vector_fallback`, `xobject_resource`, and `named_shading`.

FB-04 also covers the PaintType 2 uncolored tiling-pattern color preflight added on 2026-08-18: active pattern colors must have 1, 3, or 4 numeric components so the renderer can reconstruct DeviceGray, DeviceRGB, or DeviceCMYK. Unexpected component counts now record typed renderer refusals instead of being inferred as `/DeviceRGB`. Focused evidence includes `uncolored_tiling_pattern_invalid_component_count` and the `tiling_pattern` focused test group.

FB-06 also covers the Type 3 path-collector operand preflight added on 2026-08-18: path-only CharProc geometry now requires exact finite operands for supported path construction, line state, transforms, dash arrays, colors, and `d0`/`d1` metrics, and it requires current points for curve/line continuation operators. Malformed Type 3 clipping geometry reaches the existing typed glyph-render refusal instead of silently publishing a defaulted clip. Focused evidence includes `type3_path_collector_rejects_malformed_graphics_state_operands`, `type3_path_collector_rejects_path_segments_without_current_point`, `type3_clipping_charproc_malformed_line_width_returns_typed_refusal`, and the `type3` focused test group.

FB-06 also covers the generic Type 3 metric operand preflight added on 2026-08-18: `type3_glyph_metric_operand_refusal` validates retained/immediate/vector `d0` and `d1` operands before metric descriptor capture or native vector classification. Malformed glyph displacement/bbox operands now fail typed or unsupported instead of defaulting to zero metrics. Focused evidence includes `type3_glyph_metric_operand_validator_rejects_malformed_operands`, `malformed_type3_glyph_metric_marks_display_list_unsupported`, `malformed_type3_glyph_metric_operator_returns_typed_refusal`, and `malformed_operands_stay_whole_page_vector_fallback`.

FB-06 also covers the Type 3 FontMatrix preflight added on 2026-08-18: present malformed `/FontMatrix` arrays now fail typed before glyph CTM construction, cached geometry painting, rendered-glyph cache lookup, or full CharProc replay can default to the 0.001 scale. Focused evidence includes `malformed_type3_font_matrix_returns_typed_refusal`.

FB-06 also covers the Type 3 FontBBox cache-bound exactness added on 2026-08-19: rendered-glyph cache bounds now require exact four-number finite font-level `/FontBBox` arrays when CharProc-level bounds are absent. Malformed or overlong font bounds decline bounded cache admission instead of truncating source metadata; full CharProc rendering remains available. Focused evidence includes `type3_font_bbox_requires_exact_numeric_array` and the `type3` focused test group.

FB-06 also covers the Type 3 parsed-program lifecycle added on 2026-08-25: parsed CharProc and geometry caches now use explicit `Unvisited`, `Compiling`, `Compiled`, and `Failed` states. Cache misses mark a program as compiling before collection, then replace that state with either a compiled `Arc` or a retained failure reason; child render-state absorption preserves those lifecycle states through bounded LRU admission. Focused evidence includes `type3_program_cache_tracks_explicit_lifecycle_states`, `type3_program_cache`, and the `type3` focused test group.

## Fallback reporting contract

- `DisplayList::unsupported` is the retained source-level classification boundary.
- `ProgressiveRenderStepReport::fallback_events` preserves structured progressive fallback/refusal codes, while `fallback_event_details` records call site, trigger, output, degradation, Standard/HighQuality availability, replacement, final policy, and tile identity for counted diagnostic events. Unsupported retained progressive tiles now fail typed without publishing a fallback tile; step reports also carry deterministic job/tile publication identities and serialized visible/adjacent/background priority classes for caller stale-tile rejection.
- Runtime capabilities expose retained replay, fallback reporting, progressive lifecycle and publication-identity status, SIMD fallback semantics, versioned contract, and packed-vector-plan status.
- No fallback removed by this pass is relabeled as native support when its semantics are unavailable. Missing/malformed active patterns, missing active pattern names, missing ExtGState resources, malformed graphics-state/text/marked-content/path/resource-invocation/Type 3 metric operands, unbalanced marked-content and compatibility-section sequences, malformed SMask dictionaries/references, backdrop colors, and transfer functions, active image decode/filter/mask failures, unresolved tint-space image transforms, decoded-length mismatches, SMask or explicit-mask alpha/channel/length conflicts, pattern cycles, and exactness limits fail closed instead.

## Remaining closure blockers

The residual policies above, incomplete native compiled payloads for remaining high-level retained operations, remaining vector regionalization for SVG nonlinear shadings, PostScript calibrated nonlinear shadings outside the exact Type 2/Type 3 sidecar subsets, multi-input/calculator named-color shadings, mesh/malformed radial shadings, unsupported/advanced pattern paint, transparency, unsupported inline-image constructs, and JPX ROI/region/progressive decode remain benchmark-readiness blockers. Public progressive session surfaces and full-field binding contract builders are source-active across the listed bindings; their remaining external runtime matrices are deferred validation rather than source implementation blockers. Registered-font external runtime matrices are likewise deferred validation.

## 2026-08-11 local fallback-policy update

This local source-only continuation did not run a corpus or benchmark campaign. It changed fallback posture in the following renderer-adjacent areas:

| Area | Previous fallback or limitation | Current disposition |
|---|---|---|
| Caller-owned byte order | `reverse_byte_order` was rejected by the CPU caller-surface encoder. | Implemented for supported contract pixel formats; no fallback or refusal for byte-order-only layout changes. |
| Screen halftone | `HalftonePolicy::Screen` was not renderer-active. | Implemented as deterministic ordered raster screening for RGB output. `Screen + PreserveSeparations` remains a typed exactness refusal. |
| Progressive public sessions | Public server/session exposure was incomplete. | Server lifecycle routes are active and report structured progressive fallback/refusal diagnostics where a counted event exists. Unsupported retained tile rendering fails typed without fallback publication. C/Python/WASM/.NET/Java source surfaces also expose progressive session handles. |
| Adaptive tile sizing | Fixed tile iteration only. | Adaptive tile size is selected deterministically from 128/192/256/384/512 when requested. |
| Image tile culling | Non-zero tile-origin image planning failed open and decoded. | Tile-origin-aware metadata culling skips images outside the active tile without invoking decode. |
| SMask image classification | Soft-mask references were discovered by a second reader pass. | SMask references are classified during the primary XObject walk. |
| Progressive fallback/publication reporting | Progressive fallback events were string-only event codes. | Step reports now retain the code list, add structured fallback/refusal policy details with call site, trigger, output, degradation, availability, replacement, final policy, and tile identity, and expose deterministic job/tile publication identities plus serialized priority classes for caller stale-tile rejection. Compatibility and high-quality progressive unsupported-list tiles now fail typed without recording or publishing an immediate-tile fallback event. |

**Current materially degrading high-quality fallbacks remaining:** none in the runtime fallback-policy matrix. Compatibility SVG/PS/EPS export still has explicit whole-page raster output for legacy conversion, but strict SVG/PS/EPS/PS-document APIs now refuse that path with typed `UnsupportedFeature`; generic bundled font replacement remains compatibility-only and high-quality/exact paths refuse it. JPX still lacks codec-native ROI/region/progressive pixel output, but guarded JPX downscale uses target-resolution decode, guarded DCT/JPEG and JPX reduced tiers now require both axes to cover the requested target before cache admission, the JPX-only sampler fallback has been removed, and exact unsupported source-region/reduction-required paths fail typed. Type 3 unresolved or recursive glyph programs, exact-contract unsupported retained display-list replay, and high-quality unsupported retained page/display-list/tile/progressive replay now fail typed instead of degrading to compatibility output.

**2026-08-26 strict vector-output update:** `render_page_svg_strict`, `render_page_ps_strict`, `ContentEngine::render_page_svg_strict`, `ContentEngine::render_page_ps_strict`, `render_document_ps_strict`, and `render_page_eps_strict` now provide exact vector-output entry points that reject whole-page raster fallback. The CLI `render --strict-vector` flag wires that refusal policy to SVG, PS, and EPS output, reports it in JSON, and is rejected for raster output. Runtime `renderer_fallback_policies` now reports zero material-degrading high-quality rows and zero high-quality canonical-immediate routes. Evidence: `svg_strict_refuses_alpha_group_whole_page_raster_fallback`, `ps_strict_refuses_alpha_group_whole_page_raster_fallback`, `ps_strict_refuses_normal_alpha_extgstate_whole_page_raster_fallback`, `render_strict_vector_modes_run_and_report`, `render_strict_vector_rejects_raster_formats`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

**2026-08-26 transparent-blend no-paint update:** PostScript vector classification now removes the no-paint alpha/blend fallback case statefully: supported alpha and blend metadata can be accepted, then each fill/stroke/text/image/shading operation refuses only if the relevant paint is visible and would require fractional alpha or non-normal blending. Exact no-paint fills and strokes whose relevant alpha is zero remain vector no-ops even when the unused alpha channel is fractional or `/BM` is non-normal. Top-level transparent-page-group planning uses the same complete no-paint test, so fully transparent non-normal blend state no longer allocates a transparent page backdrop. Malformed alpha values and visible unsupported paint still fail closed, and the conservative classifier still refuses those dictionaries. Evidence: `postscript_transparent_non_normal_blend_paint_is_stateful_vector_noop`, `postscript_stroke_noop_ignores_fractional_fill_alpha`, `visible_postscript_alpha_or_blend_paint_stays_whole_page`, `postscript_visible_non_normal_blend_or_alpha_remains_conservative`, `transparent_page_group_ignores_complete_no_paint_blend_state`, and `vector_fallback`.

**2026-08-12 SVG/PS update:** `render/vector_fallback.rs` now performs form-aware classification for SVG/PS with lossless Form stream decode, parse, scoped resource merge, Form matrix/BBox handling, and depth/cycle refusal. `render/svg.rs` and `render/postscript.rs` replay vector-safe Forms natively, including no-op and opaque-inert isolated/knockout transparency-group Forms whose content is otherwise vector-safe; alpha/backdrop-observable semantic transparency groups remain limited to constructs the current vector sinks still cannot express safely and use whole-page raster embedding.

**2026-08-26 calibrated/Indexed/ICCBased inert group-colour Form/SMask update:** `render/vector_fallback.rs` now accepts explicit DeviceGray, DeviceRGB/RGB, DeviceCMYK/CMYK, well-formed calibrated CalGray/CalRGB/Lab, structurally exact Indexed, and structurally valid ICCBased transparency-group `/CS` values for vector-safe Forms only after proving the Form stream is opaque, normal-blend, and vector-safe. Resource-resolved non-device group spaces use the same proof. The active raster renderer now applies the same opaque/normal proof for well-formed CalGray/CalRGB/Lab/Indexed/ICCBased Form group `/CS` values through an explicit non-device-inert policy, and `/S /Alpha` SMask `/G` groups with no `/BC` can use that policy because mask extraction consumes only alpha. `/S /Luminosity` SMask `/BC` now resolves well-formed CalGray/CalRGB/Lab/Indexed/ICCBased/Separation/DeviceN group spaces through the named-color/CMM resolver and salts cache identity with the resolved color-space fingerprint. Active DeviceGray/DeviceCMYK Form groups now also require that opaque-normal inert proof; alpha/backdrop-observable non-RGB group spaces fail typed before offscreen allocation instead of silently using RGB group-space compositing. Alpha-bearing non-device or non-RGB device group-colour Forms, alpha SMask `/G` non-device groups with `/BC`, malformed calibrated dictionaries, malformed Indexed palettes, malformed tint transforms, malformed ICCBased profile references or `/N` metadata, and richer non-device group colour spaces still use the explicit typed refusal or vector fallback/refusal boundary. Focused evidence includes `calibrated_group_color_space`, `indexed_group_color_space`, `iccbased_group_color_space`, `device_group_color_space`, `transparency_group_device_cmyk`, `transparency_group_form_xobject`, `transparency_group_calibrated`, `transparency_group_indexed`, `transparency_group_iccbased`, `transparency_group_color_space`, `alpha_smask_calibrated`, `alpha_smask_indexed`, `alpha_smask_iccbased`, `luminosity_smask_calibrated`, `luminosity_smask_indexed`, `luminosity_smask_separation`, `luminosity_smask_devicen`, `smask_group`, and `smask_backdrop_color`.

**2026-08-18 Form BBox update:** active Form XObject, annotation appearance, and SMask `/G` Form paths now require exact four-number finite `/BBox` arrays before stream decode/cache admission, retained/vector replay, offscreen allocation, or child rendering. SVG/PS vector Form classification requires the same exact BBox before native regional Form replay. Focused evidence includes `malformed_form_xobject_bbox_returns_typed_refusal`, `smask_group_malformed_bbox_returns_typed_refusal`, `vector_form_bbox_extractor_requires_exact_numeric_array`, and the `bbox`, `form_xobject`, `smask_`, and `vector_form` focused test groups.

**2026-08-19 transparency-group flag update:** active Form XObject transparency groups now require present `/I` and `/K` entries to be booleans before group backdrop selection, knockout setup, offscreen allocation, or child rendering. Missing flags keep the PDF default `false`; malformed present flags record typed renderer refusals instead of silently defaulting to `false`. Focused evidence includes `transparency_group_malformed_flags_return_typed_refusal`.

**2026-08-23 retained XObject subtype update:** retained display-list compilation now refuses missing XObject resources, XObject streams with no `/Subtype`, and unsupported XObject subtypes before the `Do` operation can be represented as a native Form descriptor. Packed-plan compilation also refuses stale/manual Image/Form descriptors whose resolved XObject subtype does not match the descriptor kind. Focused evidence includes `malformed_xobject_subtype_marks_display_list_unsupported` and `packed_plan_refuses_resolved_xobject_subtype_mismatch`.

**2026-08-18 annotation Rect update:** visible annotations now require exact four-number finite `/Rect` arrays before appearance selection, synthetic appearance construction, cache lookup, or appearance rendering. Focused evidence includes `annotation_missing_or_malformed_rect_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-19 annotation visibility flag update:** annotation rendering now requires present `/F` visibility flags to be integers before display/print exclusion checks. Focused evidence includes `annotation_malformed_flags_return_typed_refusal` and the `annotation` focused test group.

**2026-08-19 annotation Subtype update:** visible annotations now require `/Subtype` to be present and name-valued before FormRenderPolicy, optional-content checks, appearance selection, or synthesis. Focused evidence includes `annotation_missing_or_malformed_subtype_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-19 annotation appearance state update:** selected appearance state dictionaries now require present `/AS` metadata to be a name, require the named state to exist, and require the selected state object to resolve to a stream. Focused evidence includes `annotation_malformed_appearance_state_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-18 synthesized annotation geometry update:** synthesized text-markup, line, and ink appearances now preflight `/QuadPoints`, `/L`, and `/InkList` before construction. Focused evidence includes `markup_annotation_malformed_quadpoints_returns_typed_refusal`, `line_annotation_malformed_l_returns_typed_refusal`, `ink_annotation_malformed_inklist_returns_typed_refusal`, `annotation_malformed`, and the `annotation` focused test group.

**2026-08-18 synthesized annotation paint update:** synthesized annotation appearances now preflight present `/C` color arrays and `/CA` opacity values before construction. Focused evidence includes `synthesized_annotation_malformed_color_or_opacity_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-19 SVG-in-OpenType color-glyph paint update:** static SVG color-glyph parsing now rejects out-of-range `rgb()` components, percentages, and opacity attributes. Present malformed paint metadata fails typed instead of being clamped into plausible color or alpha output, while omitted optional opacity still uses SVG defaults.

**2026-08-19 synthesized FreeText alignment update:** synthesized FreeText appearances now require present `/Q` values to be integers before text layout. Focused evidence includes `freetext_annotation_malformed_alignment_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-18 widget MK paint update:** synthesized Widget appearances now resolve `/MK` through the reader and preflight present `/BG` and `/BC` color arrays before construction. Focused evidence includes `widget_malformed_mk_colors_returns_typed_refusal`, the `widget` focused test group, and the `annotation` focused test group.

**2026-08-19 widget DA paint update:** synthesized Widget appearances now preflight inherited field or AcroForm `/DA` strings before parsing text style. Focused evidence includes `widget_malformed_default_appearance_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 widget appearance state update:** synthesized Widget appearances now require present `/AS` values to be names before checkbox/radio state synthesis. Focused evidence includes `widget_malformed_synthesized_appearance_state_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 widget field type update:** synthesized Widget appearances now require present inherited `/FT` values to be names before field-kind dispatch. Focused evidence includes `widget_malformed_field_type_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 widget field integer update:** synthesized Widget appearances now require present inherited `/Ff`, inherited `/Q`, and AcroForm `/Q` values to be integers before field kind/alignment synthesis. Focused evidence includes `widget_malformed_field_integer_metadata_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 widget NeedAppearances update:** synthesized Widget appearances now require present AcroForm `/NeedAppearances` values to be booleans before checked-state synthesis. Focused evidence includes `widget_malformed_need_appearances_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 widget display-value update:** synthesized Widget appearances now require present `/V`, choice `/Opt`, and pushbutton `/MK /CA` display metadata to be structurally displayable before construction. Focused evidence includes `widget_malformed_display_text_returns_typed_refusal` and the `widget` focused test group.

**2026-08-19 FreeText contents update:** synthesized FreeText appearances now require present `/Contents` display metadata to be structurally displayable before construction. Focused evidence includes `freetext_annotation_malformed_contents_returns_typed_refusal` and the `annotation` focused test group.

**2026-08-13 SVG/PS inline-image update, amended 2026-08-23:** `render/vector_fallback.rs` now accepts complete DeviceGray/DeviceRGB/DeviceCMYK inline images except CCITT/JBIG2 terminal RGB/CMYK declarations, simple resource names that resolve to those device color spaces, direct/resource-resolved CalGray/CalRGB/Lab inline-image color spaces, direct/resource-resolved Indexed inline-image color spaces, direct/resource-resolved ICCBased inline-image color spaces, preflighted opaque direct/resource-resolved Separation/DeviceN inline-image color spaces, DCT-filtered opaque resource-resolved Separation and one-colorant DeviceN inline images, DCT-filtered calibrated/Indexed/ICCBased inline images, and simple inline image masks for regional fallback. `render/svg.rs` embeds color images and stencil masks as bounded PNG data URIs; `render/postscript.rs` embeds color images as bounded `colorimage` regions and stencil masks as bounded `imagemask` regions painted with the active fill color. `/None` or unresolved Separation/DeviceN/tint-space inline images, multi-component DeviceN terminal-codec tint-space inline images, non-DCT terminal-codec tint-space inline images, CCITT/JBIG2 terminal inline images declared as non-gray device color spaces, non-DCT terminal-codec calibrated/Indexed/ICCBased inline images, active pattern-painted stencil masks, and other unsupported inline-image shapes remain whole-page raster cases.

**2026-08-23 monochrome terminal image decode guard update:** `images/decoder.rs` now rejects CCITT/JBIG2 terminal image decode for non-`DeviceGray`/`G` declared or resolved color spaces before full inline decode, Image XObject decode, inline CCITT source-window decode, or Image XObject CCITT source-window decode returns monochrome samples. These non-gray terminal-codec image declarations now fail typed instead of being rendered as plausible grayscale/RGB/CMYK pixels with unsupported color semantics.

**2026-08-23 JPX dimension-consistency update:** `images/decoder.rs` now rejects JPX terminal image decode when the decoded JPX dimensions disagree with the PDF inline image or Image XObject dictionary `/Width` and `/Height`, before `/SMaskInData` handling or downstream paint/cache behavior. This closes the JPX counterpart to the existing DCT dimension-consistency guard without claiming JPX ROI/region/progressive decode support.

**2026-08-23 JPX SMaskInData metadata update:** `images/decoder.rs` now rejects malformed JPX `/SMaskInData` metadata and non-zero internal-soft-mask declarations without decoded alpha samples before downstream paint/cache behavior. This closes a metadata fail-open path where declared internal JPX alpha could be ignored, without claiming JPX ROI/region/progressive decode support.

**2026-08-23 JPX finalizer invariant update:** `images/decoder.rs` now rejects malformed normalized JPX finalizer output, including non-8-bit samples, zero-channel output, decoded byte-length mismatches, and decoded dimensions over the image decode budget before downstream paint/cache behavior. This aligns JPX finalization with the existing exact image decode boundary without claiming JPX ROI/region/progressive decode support.

**2026-08-23 JPX target-resolution reduction update:** `images/jpx.rs` now exposes guarded `hayro-jpeg2000` target-resolution decode for downscaled JPX images. `images/decoder.rs` validates the original JPX codestream dimensions against the PDF dictionary before accepting a smaller reduced output, `render/image_decode_planning.rs` marks safe JPX downscale plans as native reduction, `page_renderer.rs` routes JPX Image XObject and inline-image plans through the reduced decoder, and `runtime.rs` reports JPX reduction as guarded while ROI/region and progressive output remain unavailable. Focused evidence includes `jpx_downscale`, `reduced_jpx_finish`, `render::image_decode_planning`, and `renderer_capabilities_disclose_fallback_and_progressive_limits`.

**2026-08-27 progressive bounded image-plan lifecycle update:** `render/image_decode_planning.rs` now treats planned native source-window, reduced-resolution, and component-subset image decode work as completable progressive lifecycle items. `ProgressiveImageDecodeSession::continue_decode` returns `planned_partial_decode_complete` for those bounded plans instead of leaving them in `full_decode_required`, while full-output JPEG/JPX/JBIG2/lossless paths with no native progressive/partial pixel continuation still report typed full-decode-required status. Focused evidence includes `progressive_native_window_plan_completes_without_full_decode_requirement`, `progressive_native_reduction_plan_completes_without_full_decode_requirement`, `progressive_native_component_subset_plan_completes_without_full_decode_requirement`, `progressive_image_decode_session_reports_full_decode_required`, and `image_decode_planning`.

**2026-08-23 DCT inline ColorSpace update:** `images/decoder.rs` now routes legacy inline `DCTDecode` through the checked DCT finalizer, so decoded JPEG component counts must match the declared inline image color space before output reaches paint/cache behavior. This closes a legacy inline path that could return plausible bytes under the wrong PDF color semantics.

**2026-08-23 image normalized-length update:** `images/decoder.rs` now rejects normalized sample-buffer length mismatches before decode arrays or color conversion. This prevents CMYK and other converted image paths from dropping malformed trailing bytes during chunked color conversion, and keeps DCT CMYK finalization on the same exact decode boundary.

**2026-08-23 CMYK converter length update:** `images/decoder.rs` now rejects short or trailing malformed DeviceCMYK and fallback ICCBased-CMYK input before `ColorSpaceConverter::convert_with_options` calls the raw CMYK conversion helper. This closes another path where chunked conversion could ignore incomplete source samples.

**2026-08-23 color-glyph JPEG invariant update:** `render/color_glyph.rs` now rejects malformed decoded JPEG color-glyph output before returning a glyph `RawImage` or converting CMYK samples. Oversized dimensions, unsupported channel counts, and decoded byte-length mismatches fail typed instead of reaching chunked conversion or downstream glyph paint.

**2026-08-23 color-glyph PNG invariant update:** `render/color_glyph.rs` now rejects malformed decoded PNG color-glyph output before returning a glyph `RawImage`. Grayscale-alpha PNG samples are checked before RGBA expansion, so short or trailing malformed samples fail typed instead of being hidden by chunked conversion.

**2026-08-23 color-glyph bitmap exactness update:** `render/color_glyph.rs` now rejects overlong BGRA, gray8, gray subbyte, and mono bitmap color-glyph payloads before expansion. Supported bitmap glyph payloads must match the computed image byte length exactly instead of being sliced with ignored trailing bytes.

**2026-08-23 sub-byte row exactness update:** `images/decoder.rs` now rejects overlong decoded 1/2/4 bpc image data before sample unpacking. Sub-byte image rows must match the computed packed row-byte length exactly instead of allowing trailing decoded bytes to be ignored before `RawImage` construction.

**2026-08-23/24 raw window source-length update:** `images/decoder.rs` now rejects overlong unfiltered 1/2/4/8/16-bit raw source-window inputs before cropping inline images, Image XObjects, or supported 1 bpc ImageMask stencils. Full source image stream length must match dimensions and channels exactly instead of allowing trailing raw bytes to be ignored.

**2026-08-24 raw explicit Mask source-window update:** `render/page_renderer.rs` now keeps guarded raw source-window decode available for clipped unfiltered Image XObjects whose `/Mask` postprocessing can be cropped with the same source region: color-key mask arrays and same-sized referenced 1 bpc stencil mask streams. Referenced mask streams are still parsed through the existing fail-closed metadata boundary, unsupported mask shapes remain full-image/typed-unavailable, and malformed masks still fail typed instead of being rendered unmasked.

**2026-08-26 image SMask source-window update:** `render/page_renderer.rs` now keeps guarded source-window decode available for clipped unfiltered raw Image XObjects and monochrome CCITT Image XObjects whose `/SMask` postprocessing can be cropped with the same source region. The SMask stream must be same-sized, unfiltered, grayscale, and 1/2/4/8/16 bpc; the CCITT main image must be terminal `CCITTFaxDecode`/`CCF` with only known lossless stream wrappers and `DeviceGray`/`G` metadata. Other SMask shapes remain full-image/typed-unavailable and malformed masks still fail typed instead of rendering unmasked.

**2026-08-23 Indexed lookup exactness update:** `images/decoder.rs` and `render/colorspace.rs` now reject overlong `/Indexed` lookup tables before palette conversion or active color resolution. Lookup byte length must exactly match `(hival + 1) * base_channels` instead of allowing malformed trailing bytes to be ignored.

**2026-08-23 Indexed arity exactness update:** `images/decoder.rs` and `render/colorspace.rs` now reject `/Indexed` color-space arrays whose length is not exactly four entries. Overlong arrays fail typed instead of allowing trailing metadata to be ignored after the lookup table.

**2026-08-23 calibrated arity exactness update:** `render/cmm.rs`, `render/colorspace.rs`, and `render/vector_fallback.rs` now reject overlong CalGray, CalRGB, and Lab color-space arrays before image parameter parsing, active/named color conversion, or calibrated vector-shading regionalization. Extra array entries no longer act as ignored metadata.

**2026-08-23 color-glyph sbix parser fail-closed update:** `render/color_glyph.rs` now rejects malformed sbix strike headers, glyph-offset tables, strike records, zero pixels-per-em values, and malformed duplicate glyph records before payload selection. Malformed sbix color-glyph metadata no longer defaults, becomes an unsupported dummy payload, or disappears as absent glyph data.

**2026-08-23 PDF function input-arity update:** `render/function.rs` now rejects malformed Type 0/2/3/4 function shapes and missing/non-finite function inputs before evaluation. Function evaluation no longer paints or converts colors from implicit zero inputs or defaulted required domain/encode metadata.

**2026-08-23 SMask transfer backdrop update:** `render/page_renderer.rs` now rejects unsupported, malformed, or empty-output alpha soft-mask `/TR` functions during `/BC` backdrop default-alpha probing. Malformed transfer output no longer behaves like `TR(0)=0`.

**2026-08-23 utility image sample-length update:** `utilities.rs` now rejects malformed decoded sample lengths before image-to-PDF utility JPEG CMYK conversion, PNG grayscale/grayscale-alpha/RGB/RGBA expansion, and rendered-page RGBA/gray-to-RGB conversion. This closes high-level utility paths where chunked expansion helpers could otherwise ignore incomplete samples.

**2026-08-17 SVG/PS affine-image update:** finite, non-degenerate image XObject and inline-image placements now carry an affine device matrix into SVG `<image transform="matrix(...)">` and PostScript `concat` regional output. Color image regions use PostScript `colorimage`; inline and XObject stencil masks use PostScript `imagemask` with a mask scratch buffer. Rotated/skewed local image regions and stencil Image XObjects no longer force whole-page raster embedding; degenerate or unresolvable image transforms still fail closed to the whole-page fallback.

**2026-08-17 SVG/PS active-pattern-paint update:** the shared vector fallback classifier no longer treats selecting pattern state as an immediate whole-page fallback when later color state overwrites it before paint. Actual active pattern fill, stroke, fill-stroke, text paint, or stencil mask fill still failed closed to whole-page raster embedding at that checkpoint because the SVG/PS sinks did not yet implement pattern paint semantics.

**2026-08-18 SVG/PS shading-pattern update:** simple `/PatternType 2` shading patterns with absent/finite `/Matrix` and active CTM, and vector-safe axial or finite/invertible affine-transformed radial shading dictionaries now keep fill/stroke path paints and glyph-outline text paints native. SVG emits a clipped gradient rectangle and PostScript/EPS emits clipped LanguageLevel 3 `shfill`; unsupported advanced pattern-space transforms, nested pattern-paint/color-setting/unsupported-resource tiling cells, patterned text outside the supported shading/tiling glyph-outline subset, stencil masks whose fill paint requires unsupported active pattern semantics, transparency, and other unsupported pattern constructs still fail closed to whole-page raster embedding.

**2026-08-18 SVG alpha ExtGState update:** SVG and PostScript classification now use sink-specific ExtGState safety. SVG accepts finite normal-blend `CA`/`ca` opacity and emits native opacity for path, text, regional image, and direct shading output; PostScript/EPS still treats fractional non-opaque alpha as whole-page raster fallback because the current PostScript sink has no exact native transparency operator.

**2026-08-19/23 PostScript alpha update:** PostScript classification accepts `CA`/`ca` values of exactly `0` or `1`, and regional decode now distinguishes fully transparent, binary 0/255, and fractional RGBA buffers. The native sink skips fully transparent path, text, shading, image, stencil-mask paint, and fully transparent decoded regional RGBA as exact no-ops; binary 0/255 regional RGBA is clipped through a device-space cell path before `colorimage`; fractional alpha remains whole-page raster fallback or typed unsupported output.

**2026-08-16/25 retained routing update, amended 2026-08-27:** default, cached, contract full-page, retained tile, direct tile, progressive supported-list rendering, and CLI `render-corpus --pipeline display-list` now build or reuse a retained display list and route fully supported pages/tiles through `RenderPlan::compile_with_resources` / typed packed descriptor replay with caller cancellation propagation. Supported tiling-pattern streams cache resource-scoped retained plans and replay with a tile-CTM-aware packed adapter. Compatibility and high-quality/exact rendering now refuse unsupported retained display-list, retained tile, retained subprogram, progressive retained tile, and render-corpus display-list page replay with typed errors instead of dispatching raw page operations or publishing immediate fallback tiles.

**2026-08-16 Type 3 retained update, amended 2026-08-26:** Type 3 CharProcs with clips, typed text/image/Form/shading/inline/pattern resource ops, Type 3 `d0`/`d1` metrics descriptors, and vector path paints now compile to guarded retained plans. The retained adapter tracks per-paint inherited versus explicit fill/stroke colors so mixed inherited/explicit path-color CharProcs preserve caller text color and CharProc-local colors without raw replay. Focused synthetic tests now prove resource-backed image, Form XObject, and named shading CharProcs render through this retained route.

**2026-08-16 SVG/PS axial/radial shading update:** the shared vector fallback classifier now accepts simple linear DeviceGray/DeviceRGB/DeviceCMYK axial shading resources with `Extend [true true]` and Type 2 `N=1` functions, plus finite/invertible affine-transformed DeviceGray/DeviceRGB/DeviceCMYK radial shadings with the same function boundary. SVG emits native `<linearGradient>`/`<radialGradient>` fills, PostScript/EPS emits LanguageLevel 3 `shfill`, and unsupported/non-linear/malformed radial shading resources continue to fail closed to whole-page raster embedding.

**2026-08-23/24 PostScript exact function shading update:** direct DeviceRGB/DeviceGray axial/radial shadings whose single Type 2 function has a finite unit-domain exponent now stay regional for PostScript/EPS, direct DeviceCMYK axial/radial shadings whose single Type 2 function has a finite unit-domain exponent now stay regional as native `/ColorSpace /DeviceCMYK` `shfill`, direct DeviceRGB function arrays whose one-component Type 2 functions share the same finite unit-domain exponent collapse to one exact RGB Type 2 function, direct DeviceRGB function arrays whose components have different finite unit-domain exponents now stay regional as exact PostScript per-component function arrays, direct DeviceCMYK function arrays whose one-component Type 2 functions share the same finite unit-domain exponent collapse to one exact CMYK Type 2 sidecar, direct DeviceCMYK function arrays whose components have different finite unit-domain exponents now stay regional as exact `/ColorSpace /DeviceCMYK` per-component function arrays, and direct DeviceRGB/DeviceGray Type 3 stitching functions whose Type 2 subfunctions are exact unit-domain segments now stay regional even when the stitch is discontinuous. `render/vector_fallback.rs` carries exact Type 2 `/N`, exact per-channel RGB/CMYK function-array metadata, exact direct CMYK components, and Type 3 segment metadata only for those DeviceRGB/DeviceGray/DeviceCMYK unit-domain cases, expands DeviceGray functions to exact RGB sidecars, `render/postscript.rs` emits native LanguageLevel 3 `/FunctionType 2`, function-array, or `/FunctionType 3` `shfill`, and SVG plus calibrated/named, ranged, non-unit-domain, mesh, malformed, or otherwise inexact nonlinear shadings still fail closed instead of being approximated.

**2026-08-24 PostScript exact stitching update:** direct `/DeviceRGB`, `/DeviceGray`, and `/DeviceCMYK` Type 3 stitching functions whose Type 2 segments are finite and unit-domain now stay regional for PostScript/EPS as native `/FunctionType 3` `shfill`, preserving non-linear segment `/N` exponents exactly. CMYK stitching uses `/ColorSpace /DeviceCMYK`; SVG, calibrated/named, ranged, non-unit-domain, mesh, malformed, or otherwise inexact stitching cases remain fail-closed.

**2026-08-26 PostScript exact named-color shading update:** one-component `/Separation` and one-colorant `/DeviceN` axial/radial shadings now stay regional for PostScript/EPS when the source shading tint function and color-space tint transform can both be serialized exactly as finite Type 2/Type 3 PostScript functions. The PS sink emits native `[/Separation ...]` or single-colorant `[/DeviceN ...]` color spaces in the `shfill` dictionary instead of flattening endpoint RGB. SVG nonlinear tint transforms, multi-input/calculator DeviceN transforms, unsupported alternates, malformed functions, and unresolved metadata remain fail-closed.

**2026-08-17 SVG/PS DeviceCMYK/calibrated shading update:** simple supported DeviceCMYK axial/radial shading stops are converted through the same deterministic DeviceCMYK preview transform used by the renderer's image/color paths, direct or page-resource-resolved CalGray/CalRGB/Lab shading color-space arrays use the active calibrated conversion helpers, and supported Separation color spaces use the existing tint-transform resolver; the resulting RGB stops are emitted as SVG/PS DeviceRGB gradients instead of forcing whole-page raster embedding.

**2026-08-16/19 SVG/PS safe ExtGState update:** the shared vector fallback classifier now accepts resolved opaque line cap/join/miter/dash/font/flatness/smoothness/default-state/identity-transfer ExtGState dictionaries (`LW`, `LC`, `LJ`, `ML`, `RI`, finite `/D` dash arrays, finite non-negative `/Font [/F size]`, finite non-negative `FL`/`SM`, `OP false`, `op false`, `OPM 0/1`, `SA false`, `AIS false`, `TK true`, opaque `CA`/`ca` for both sinks, normal or first-supported normal-compatible `BM`, `/SMask /None`, `TR`/`TR2` `/Identity`, and four-entry `TR`/`TR2` `/Identity` arrays) and the SVG/PS sinks apply that state before native path replay. SVG additionally accepts finite normal-blend `CA`/`ca` opacity; PostScript accepts only opaque alpha or exact zero-alpha no-op paint. SVG emits `stroke-linecap`, `stroke-linejoin`, `stroke-miterlimit`, dash array, dash phase, and opacity attributes; PostScript emits `setlinecap`, `setlinejoin`, `setmiterlimit`, and `setdash`. Transparency-bearing constructs other than SVG scalar normal alpha, PostScript zero-alpha no-op paint, overprint-enabled, invalid overprint-mode, stroke-adjustment-enabled, alpha-source-enabled, text-knockout-disabled, soft-mask, non-identity transfer-function, halftone, invalid line-style values, invalid-dash, invalid font arrays, invalid flatness/smoothness, unknown-key, unresolved, and visible non-normal-blend ExtGState paint still fails closed to whole-page raster embedding.

**2026-08-17 SVG/PS text-outline and clipping update:** stroked text outlines now carry vector-safe line cap/join/miter/dash state, fill-stroke text modes emit both paints in SVG and PostScript, and font-resolved text-clipping render modes 4-7 accumulate glyph outlines into native SVG `clipPath` / PostScript `clip` state before subsequent vector paint. Text clipping still fails closed when font preflight is unavailable or glyph outlines cannot be resolved.

**2026-08-23 SVG/PS vector text metric-source update:** SVG and PostScript native text replay now requires a PDF glyph width, a strict sfnt/bare-CFF font-program advance, or a valid vertical advance before moving the text matrix. The vector classifier refuses ordinary text when metric preflight is unavailable, and the sinks record typed output errors instead of synthesizing `500` horizontal or `-1000` vertical advances. Existing best-effort raster-compatible font helpers keep their compatibility defaults, but SVG/PS vector output no longer uses those defaults as native text metrics.

**2026-08-23 exact raster text metric-source update:** High-quality/exact raster text now uses PDF widths, native Type 3 metrics, or strict variation-aware sfnt/bare-CFF advances before moving the text matrix. If exact raster text has no real horizontal metric source it records a typed `HighQualityExact` refusal instead of accepting the compatibility `500` fallback, and vertical writing now requires an explicit vertical advance instead of falling back to `-1000`. Compatibility rendering still keeps its documented deterministic best-effort movement for metricless ordinary fonts.

**2026-08-23 direct path-mask cache update:** Direct CPU fill/stroke path-mask cache pressure is no longer handled by clearing the entire map. The caches now refresh hit recency, update byte charges when replacing keys, and evict least-recently-used masks one at a time under entry or byte pressure. This is a cache-pressure implementation change, not a new pixel fallback category.

**2026-08-23 packed missing-resource descriptor update:** Packed Image XObject, Form XObject, and named shading compilation now records typed compile refusals when `compile_with_resources` has a resource table but the retained resource name is absent. Packed execution dispatches the refusal instead of deferring to runtime resource-name lookup.

**2026-08-18 SVG/PS dense-text update:** the shared vector fallback classifier no longer rasterizes a whole page merely because ordinary text-showing operation count exceeds an internal scope limit. Dense ordinary text remains vector-classified; pattern-painted text outside the supported shading/tiling glyph-outline subset and unresolved/preflight-unavailable text clipping still fail closed to whole-page raster embedding.

**2026-08-18 SVG/PS regional image replay update:** SVG/PS regional Image XObject replay now preserves the source image dictionary's color space and filter metadata, and approved regional image or inline-image replay failures now return typed output errors instead of silently omitting a bounded image while reporting regional output. The active whole-page renderer also resolves resource-named inline image color spaces before decode, so unsupported regional inline cases such as `/Separation /None` can still reach the canonical whole-page fallback without treating `/CS0` as an unknown device color space.

**2026-08-23 SVG/PS regional Image XObject color-space preflight update:** regional Image XObject classification now resolves non-mask `/ColorSpace` metadata through page resources and checks the resolved space with the same vector-output support predicate used for regional inline images. Unsupported direct names, unsupported resource aliases, and malformed color-space arrays stay on the whole-page typed raster/refusal boundary before bounded SVG/PostScript image emission; resource-named `/DeviceRGB` regional SVG and PostScript output remains accepted.

**2026-08-18/23 SVG/PS regional image/mask fail-closed update:** SVG regional stencil conversion, PostScript regional `colorimage`, and PostScript regional `imagemask` now validate decoded `RawImage` buffer shape before sampling. Regional color images require exact 8-bit gray/RGB/RGBA buffers; regional stencil masks require exact one-channel 8-bit buffers; and PostScript regional colorimage skips fully transparent RGBA, clips binary 0/255 RGBA exactly, and refuses fractional RGBA because the sink cannot represent fractional local alpha in that path. The previous defensive sampling could synthesize zero samples or drop bounded-region alpha.

**2026-08-18 SVG/PS calibrated shading update:** native SVG gradient and PostScript `shfill` classification now requires well-formed CalGray, CalRGB, and Lab shading color-space parameter dictionaries. Missing required `WhitePoint`, malformed gamma/matrix/black-point arrays, and malformed or inverted Lab ranges fail closed to the existing explicit vector-output fallback rather than silently using default calibrated-space parameters.

**2026-08-18 active named-color update:** active bare `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` resource aliases resolve through the named-color resolver, while active named CalGray, CalRGB, and Lab paint conversion requires well-formed parameter dictionaries and exact finite components. Malformed calibrated paint, malformed calibrated Separation/DeviceN alternates, missing named color resources, and unsupported named paint spaces record typed renderer refusals instead of reaching black/default component fallback.

**2026-08-19 named-color component exactness update:** named-color resolution now requires exact finite component vectors for bare device aliases, CalGray/CalRGB/Lab paint, Separation tint input, DeviceN tint input, and device alternate-space output. ICCBased scalar conversion also requires the supplied vector length to match profile `/N`. Overlong vectors record typed invalid/unsupported named-color outcomes instead of being truncated by finite-prefix checks or later device conversion.

**2026-08-24 render-facing device color-state update:** `ColorSpaceHandler::strict_to_render_color` now protects active page paint, retained display-list simple-color capture, Type 3 inherited vector color replay, and SVG/PostScript vector sinks below PDF operand validation. Malformed internal `/DeviceGray`, `/DeviceRGB`, or `/DeviceCMYK` graphics-state vectors now fail typed or decline retained metadata instead of padding missing components, truncating extras, or emitting fallback pixels. Focused evidence includes `strict_to_render_color_requires_exact_finite_device_state`, `display_list_simple_color_requires_exact_finite_device_state`, `svg_device_color_resolution_rejects_malformed_state_locally`, and `postscript_device_color_resolution_rejects_malformed_state_locally`.

**2026-08-19 SVG/PS named-paint update:** SVG/PS vector-output classification now checks named ordinary fill/stroke/fill-stroke paint, text rendering modes, Image XObject stencil masks, and inline stencil masks before native vector replay. Valid resource-named device paint requires exact finite components; unresolved, unsupported, invalid, or incomplete named paint now fails closed to the whole-page typed raster/refusal path instead of reaching generic black conversion in the vector sinks.

**2026-08-18 SMask matte/backdrop color update:** image SMask `/Matte` and luminosity SMask `/BC` conversion now reject unsupported/calibrated family-name-only color spaces rather than defaulting calibrated parameters or converting unknown names to black.

**2026-08-18 active shading ColorSpace update:** active shading dictionaries now validate `/ColorSpace` before paint and validate sampled function outputs against the resolved component count. Unsupported shading color spaces, malformed calibrated/ICCBased/Separation/DeviceN color spaces, non-finite device components, and too-short function output arrays record typed shading refusals instead of reaching generic black/default color conversion.

**2026-08-19 active shading device-component exactness update:** bare and array-wrapped `/DeviceGray`, `/DeviceRGB`, and `/DeviceCMYK` shading color spaces now require sampled function outputs to contain exactly the device component count and only finite values. Overlong device output arrays record typed shading refusals instead of being truncated by generic device-color conversion.

**2026-08-18 malformed RawImage sampler update:** public image sampling helpers now read through `RawImage::pixel` and return transparent samples for short buffers or unsupported channel counts. The active image painter still rejects invalid decoded image shapes before paint; the helper no longer has an opaque-black defensive branch for malformed direct samples.

**2026-08-18 inline image metadata update:** inline images now require positive integer `/Width` and `/Height` values before decode planning, non-mask inline images require `/BitsPerComponent` to be one of 1, 2, 4, 8, or 16 unless a terminal JPX filter carries sample depth, and non-mask inline images require a named `/ColorSpace` entry. Missing/invalid dimensions record a typed renderer refusal instead of returning as a zero-sized no-op; missing/unsupported bpc records a typed renderer refusal instead of defaulting to 8 or clamping; missing or non-name color-space metadata records a typed renderer refusal instead of defaulting to `/DeviceGray`; inline image masks remain allowed to omit `/BitsPerComponent` and `/ColorSpace` because they use the 1-bpc stencil-mask path.

**2026-08-18 image XObject metadata update:** active image XObjects now require positive integer `/Width` and `/Height` values before cache-key construction and decode planning. Non-mask image XObjects require `/BitsPerComponent` to be one of 1, 2, 4, 8, or 16 unless a terminal JPX filter carries sample depth, and require `/ColorSpace` to be a named color space or a color-space array headed by a name unless a terminal JPX filter carries the color model. Missing/invalid dimensions record a typed renderer refusal instead of becoming `1x1`; missing/unsupported bpc records a typed renderer refusal instead of defaulting or clamping; missing/malformed color-space metadata records a typed renderer refusal instead of defaulting to `/DeviceRGB`.

**2026-08-18 explicit image Mask and image SMask metadata update:** referenced explicit `/Mask` streams now require positive integer `/Width` and `/Height` values instead of inheriting the source image dimensions. Non-stencil explicit masks require supported bpc and valid color-space metadata, while stencil masks continue to use the 1-bpc path and reject any present non-1 bpc. Image SMask streams now require positive dimensions, supported `/BitsPerComponent`, and valid `/ColorSpace` metadata instead of inheriting dimensions, defaulting bpc to 8, or defaulting color space to `/DeviceGray`.

**2026-08-19 image Filter metadata update:** active inline images, Image XObjects, and referenced explicit image Mask streams now require `/Filter` metadata to be absent, a single name, or an all-name array before terminal-codec metadata exceptions, cache-key construction, or mask decode planning. Malformed filter arrays now record typed renderer refusals instead of silently dropping non-name entries.

**2026-08-18 uncolored tiling-pattern color update:** PaintType 2 tiling-pattern paints now require active pattern-color component counts of 1, 3, or 4 before tile replay. Unexpected counts record a typed renderer refusal instead of falling through to `/DeviceRGB` inference.

**2026-08-17 SVG clip composition update:** stacked SVG clips now register child `clipPath` definitions that reference the previous active clip, so subsequent native vector elements use the composed clip id instead of silently dropping the earlier clip.

**2026-08-17 SVG/PS ExtGState dash update:** `/D [[... ] phase]` dash arrays now update renderer graphics state and remain vector-safe when the pattern has at most 64 finite non-negative entries, is not a nonempty all-zero pattern, and the phase is finite and non-negative. SVG emits `stroke-dasharray`; PostScript emits `setdash` for the regional ExtGState path.

**2026-08-17 SVG/PS ExtGState default-state update:** default-equivalent `OP false`, `op false`, valid `OPM 0/1`, `SA false`, `AIS false`, and `TK true` entries no longer force whole-page rasterization when the rest of the ExtGState is vector-safe. True overprint flags, invalid overprint mode, stroke-adjustment-enabled, alpha-source-enabled, and text-knockout-disabled states remain unsupported for SVG/PS regional replay.

**2026-08-17 SVG/PS ExtGState Font update:** `GraphicsState::apply_ext_g_state` now applies valid `/Font` arrays to the current text font and size, page-resource parsing normalizes indirect ExtGState font references back to font resource names where possible, and SVG/PS regional replay accepts finite non-negative font entries without rasterizing the whole page. Malformed, unresolved, non-finite, or negative vector ExtGState font entries still fail closed to whole-page raster embedding.

**2026-08-17 graphics-state flatness/smoothness update:** the PDF `i` operator and safe ExtGState `/FL` entries now update `GraphicsState` flatness, and path fill/stroke rasterization, transformed path clip-node cache identity, path mask cache identity, SVG path replay, and PostScript path replay consume the active normalized tolerance. Safe ExtGState `/SM` entries are also retained in graphics state and now feed bounded Coons/tensor patch-mesh subdivision tolerance; Type 1-3 shadings remain per-pixel sampled and do not need subdivision adjustment. Negative or non-finite `/FL` and `/SM` entries still fail closed for SVG/PS regional replay.

**2026-08-23 annotation display-text exactness update:** synthesized annotation/widget display values now reject non-finite internal `Real` values before appearance text serialization, and the shared non-strict display helper no longer keeps earlier array text while silently dropping malformed later members. Explicit `/Off` remains the accepted empty state. This closes a local synthesis-helper defaulting boundary without adding corpus validation or external runtime parity.

**2026-08-23 Type 4 calculator output update:** PDF Function Type 4 numeric operators now require finite numeric operands, control flow requires boolean conditions, and final function outputs must be the top numeric stack values. Boolean stack values are no longer coerced to `0`/`1`, numeric truthiness no longer controls `if`/`ifelse`, and final procedures are no longer filtered while earlier numbers are returned. This removes malformed calculator-function coercion before shading, tint-transform, soft-mask transfer, or other function consumers can treat it as valid output.

**2026-08-23 image filter-chain capability update:** image decode planning now treats any unknown filter in the filter chain as `UnknownCodec` instead of ignoring that filter and inheriting source-window or reduction capability from a later recognized terminal filter. Unknown wrappers therefore cannot produce false native CCITT/raw/JPEG capability claims; valid fully known CCITT chains keep the guarded source-window path.

**2026-08-25/2026-08-26 component-selective image decode capability update:** component-subset image decode requests now participate in the exact image decode refusal model instead of only salting cache identity. `ImageDecodePlan` records `requires_component_decode`, `ImageDecodeCapabilityReport` exposes `component_decode`, exact-mode limitation summaries include `component-selection`, and the document-level capability report exposes `native_component_decode_count`. The unfiltered raw adapter implements native component-selected 1/2/4/8/16-bit source-window output for XObjects and inline images, including full-window raw component requests and `/Decode` application by original source component index. Explicit all-component requests for known source shapes no longer require subset support, invalid known raw component indexes fail exact planning instead of being advertised as native, and image-reference capability reports mark shape-supported raw plus monochrome CCITT source windows and trivial single-component selection as native. JPEG/JPX/JBIG2 and filtered-lossless adapters still report unavailable native component-subset decode, and invalid/nontrivial CCITT subset requests fail typed, so requested subsets cannot silently run an all-component decode under a subset cache identity.

**2026-08-26 CCITT capability-shape amendment:** the planner and image-reference capability report now apply the same terminal monochrome shape boundary as `images/decoder.rs` before advertising native CCITT source-window support. CCITT streams declared as RGB/CMYK report `MonochromeTerminalShapeUnsupported` and fail exact source-region planning instead of exposing a native region path that would later hit the decoder guard.

**2026-08-26 ExtGState paint-dependent semantic update:** well-formed `/SA`, `/AIS`, and `/TK` values now enter active graphics state and retained/packed draw-state identity. Visible `/SA true` stroke paint, `/AIS true` nonstroking/stroking/text/image/shading paint, and `/TK false` text paint fail typed at active, retained, and packed replay paint boundaries instead of being rejected as inert metadata or silently rendered without those semantics; non-observing cases such as `/SA true` fill-only paint stay renderable.

**2026-08-23 active shading Domain sample exactness update:** `render/page_renderer.rs` now uses strict finite numeric array parsing for active shading `/Domain` sample inputs. Present malformed Domain arrays cannot be filtered down to numeric prefixes or treated as absent defaults during shading function output validation.

**2026-08-25 active shading function-array update:** active raster shading no longer treats all `/Function` arrays as unsupported. `render/function.rs::eval_function_or_array_n` evaluates a bounded array of one-output component functions, `render/shading.rs` uses it for Type 1, axial/radial, and mesh sampling, and `validate_shading_dictionary_for_paint` still fails closed for empty, oversized, malformed, or multi-output component arrays before paint. The remaining fallback boundary is malformed or unsupported component functions, not well-formed PDF function arrays.

**2026-08-25 render-contract color-scheme update:** active CPU render contracts now implement `ColorScheme::Dark` and `ColorScheme::ForcedMonochrome` by applying deterministic RGB inversion or luma conversion to the canonical RGBA surface before canonical return or caller-owned surface encoding. Unsupported semantic contract policies remain typed refusals, but the active refusal now lists the exact divergent fields instead of using a generic semantic-policy fallback bucket.

**2026-08-25 render-contract image-smoothing update:** active CPU render contracts now implement `image_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`. The accepted policy is carried through retained plan identity, active page rendering, annotation replay, and nested soft-mask/transparency-group states; malformed `/Interpolate` still fails typed before policy application, `Disabled` forces nearest-neighbour image sampling, and `Antialiased`/`Subpixel` preserve PDF interpolation semantics without a typed smoothing-policy refusal.

**2026-08-25 render-contract text-smoothing update:** active CPU render contracts now implement `text_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`, plus the separate `subpixel_text` policy. The accepted policies are carried through retained plan identity, active page rendering, annotation replay, and nested soft-mask/transparency-group states; glyph-mask cache keys include binary-alpha state; disabled smoothing thresholds cached glyph masks and direct ordinary/color glyph fill fallback coverage to binary alpha; subpixel text uses cached or direct glyph alpha masks through an RGB LCD-style per-channel compositor.

**2026-08-25 render-contract path-smoothing update:** active CPU render contracts now implement `path_smoothing` for `Disabled`, `Antialiased`, and `Subpixel`. The accepted policy is carried through retained plan identity, active page rendering, annotation replay, and nested soft-mask/transparency-group states; path fill/stroke mask cache keys include binary-alpha state; disabled smoothing thresholds cached path masks and routes direct RGB plus CMYK overprint-preview path fallback coverage to binary alpha; `Subpixel` uses the existing device-space subpixel antialias coverage path instead of a typed smoothing-policy refusal.

**2026-08-25 render-contract ScalarReference backend update:** active CPU render contracts now implement `BackendSelection::ScalarReference`. The contract path installs a scoped scalar-compositor guard so backend reporting returns `scalar`, architecture SIMD and portable-wide row helpers decline during that render, and the existing scalar row compositors produce pixels. Caller-owned contract surfaces now use a backend-aware row encoder so ScalarReference rows call the scalar row loop directly after render-time buffering as well. The backend policy is part of retained plan contracts, active/child render state, annotation replay, SMask identity, and inline/Image XObject decode planning/cache identity. The later 2026-08-26 continuation makes `ResearchHybrid` an active guarded CPU hybrid-dispatch backend with separate contract/cache identity.

**2026-08-25 native alpha-mask SIMD update:** native x86/x86_64 render-simd builds now route opaque-destination alpha/glyph-mask rows through an SSE2 kernel when available instead of returning scalar-only from `blend_alpha_mask_opaque_destination`. The kernel preserves the existing rounded mask-alpha contract, keeps scalar tails for uneven rows, and uses the scalar compositor as a debug oracle.

**2026-08-26 native mixed-destination alpha-mask SIMD update:** native x86/x86_64 render-simd builds now route `blend_alpha_mask_normal` through an SSE2-dispatched four-pixel load/store row path when available. The helper preserves the existing floating-point normal Compat source-over byte contract for mixed destination alpha, with scalar debug oracles and scalar tails.

**2026-08-26 native separable blend SIMD update:** native x86/x86_64 render-simd builds now route the full `blend_separable_opaque_destination` PDF separable set through an SSE2-dispatched row path when available. Multiply, Screen, Overlay, Darken, Lighten, HardLight, Difference, and Exclusion use exact integer lane math; ColorDodge, ColorBurn, and SoftLight use SSE float lanes with grouped gather/scatter. The helper retains scalar debug oracles and scalar tails.

**2026-08-26 opaque-background flatten SIMD update:** Compat page-background flattening now calls `render-simd::flatten_opaque_background` before the existing portable-wide fallback. Native x86/x86_64 SSE2 and wasm32+`simd128` row groups preserve the scalar byte source-over contract, force opaque alpha, retain scalar debug oracles and scalar tails, and report `opaque_background_flatten_rows` through runtime capabilities.

**2026-08-25 native caller-surface SIMD update:** native x86/x86_64 render-simd builds now route `copy_rgba` and `rgba_to_opaque_rgba` through SSE2 row kernels when available. The helpers preserve exact byte-copy and alpha-forcing semantics, retain scalar debug oracles and scalar tails, and keep caller-owned straight/opaque RGBA surface output on a typed, tested row path.

**2026-08-25 native BGRA conversion SIMD update:** native x86/x86_64 render-simd builds now route `rgba_to_bgra8` through an SSE2 row kernel when available. The helper preserves straight-alpha and forced-opaque semantics, reuses the scalar oracle in debug builds, keeps scalar tails for uneven rows, and removes another native caller-surface channel-conversion scalar-only path.

**2026-08-25 native RGB/BGR channel SIMD update:** native x86/x86_64 render-simd builds now route `rgba_to_rgb8`, `rgba_to_bgr8`, and `rgb8_to_opaque_rgba` through SSSE3 byte-shuffle kernels when available. The helpers preserve exact RGB/BGR triplet packing and opaque-RGBA image-span expansion semantics, retain scalar debug oracles and scalar tails, and remove another native scalar-only boundary from caller-owned RGB/BGR surfaces and one-to-one RGB image rows.

**2026-08-25 native grayscale SIMD update:** native x86/x86_64 render-simd builds now route `rgba_to_gray8`, `rgba_to_gray_rgb8`, `rgba_to_gray_rgba8`, `rgba_to_gray_bgra8`, and premultiplied grayscale alpha-bearing rows through guarded SSSE3 luma kernels when available. The helpers preserve the existing `77/150/29` contract luma weights, forced-alpha behavior, premultiplied gray rounding, scalar debug oracles, and scalar tails.

**2026-08-25 native SSSE3 backend reporting update:** `SimdBackend` and engine `PixelCompositorBackend` now expose `ssse3`, and detected/native backend reporting checks SSSE3 before SSE2. This keeps the new native SSSE3 channel/grayscale row paths visible in runtime/capability surfaces instead of collapsing them into the older SSE2 label.

**2026-08-25 native premultiply SIMD update:** native x86/x86_64 render-simd builds now route `premultiply_rgba` and `premultiply_bgra8` through SSE2 row kernels when available. The helpers preserve scalar div255 rounding, reorder premultiplied RGBA words into BGRA for BGRA output, retain scalar debug oracles and scalar tails, and keep caller-owned premultiplied surfaces on an active native SIMD path.

**2026-08-26 native unpremultiply SIMD update:** native x86/x86_64 render-simd builds now route `unpremultiply_rgba` through an SSE2-dispatched four-pixel load/store row path when available. The reciprocal conversion remains scalar per channel to preserve the existing exact byte contract, with scalar debug oracles, exact alpha-zero handling, and scalar tails.

**2026-08-23 visual font metric exactness update:** `fonts/resolver.rs` and `render/text_decode.rs` now fail typed before visual glyph placement when present simple `/Widths`, CID `/DW`/`/W`, or CID vertical `/DW2`/`/W2` metric metadata is malformed. Active text rendering no longer filters malformed width arrays or substitutes default vertical triples for malformed present entries.

**2026-08-23/26 exact-tile invalidation update:** `render/invalidation.rs`, `render/transaction_invalidation.rs`, and `render/page_renderer.rs` now separate retained page-artifact pruning from raster tile invalidation for transaction plans that include explicit dirty tiles. Affected pages still clear page-scoped retained artifacts, but valid exact dirty/source-tile plans no longer expand raster invalidation to every recorded page tile. Page-0 or zero-sized exact-tile entries are ignored for coverage and application, so malformed binding-safe plans fall back to recorded page-tile invalidation instead of masking stale raster tiles. Evidence: `exact_tile`, `explicit_tiles_with_reported_page_do_not_expand_all_page_tiles`, `render_invalidation_plan_json_with_page_uses_exact_tiles`, `zero_sized_explicit_tile_does_not_mask_page_wide_invalidation`, and `render_invalidation_plan_json_zero_sized_tile_does_not_cover_page`.

**2026-08-23 packed state-resource descriptor update:** resource-aware packed graphics-state descriptors now fail closed when `/ExtGState`, `/Font`, named non-device color-space, pattern color, or marked-content `/Properties` resources are absent. Retained text-state `/Tf` descriptors use the same missing-font refusal. These cases are now explicit packed compile refusals instead of late runtime name lookup or skipped vector-state replay.

**2026-08-25 Type 3 retained exactness update:** Type 3 CharProc full replay now fails typed in compatibility and high-quality/exact rendering when the nested retained CharProc plan is unavailable or contains a packed compile refusal. Unsupported retained Type 3 subplans no longer dispatch through immediate CharProc replay. Evidence: `high_quality_type3_charproc_refuses_unsupported_retained_replay`, `compatibility_type3_charproc_refuses_unsupported_retained_replay`, and the `type3` focused gate.

**2026-08-25 retained subprogram exactness update:** high-quality/exact and compatibility Form XObject, transparency Form group, annotation appearance, tiling-pattern, and soft-mask `/G` Form replay now fail typed when the subprogram has no retained packed plan or contains a packed compile refusal. Unsupported retained subprograms no longer dispatch through immediate replay in compatibility mode. Evidence: `high_quality_form_xobject_refuses_unsupported_retained_replay`, `compatibility_form_xobject_refuses_unsupported_retained_replay`, `high_quality_form_group_refuses_unsupported_retained_replay`, `compatibility_form_group_refuses_unsupported_retained_replay`, `high_quality_annotation_appearance_refuses_unsupported_retained_replay`, `compatibility_annotation_appearance_refuses_unsupported_retained_replay`, `high_quality_smask_group_refuses_unsupported_retained_replay`, `compatibility_smask_group_refuses_unsupported_retained_replay`, `high_quality_tiling_pattern_refuses_unsupported_retained_replay`, `compatibility_tiling_pattern_refuses_unsupported_retained_replay`, and the `unsupported_retained` focused gate.

**2026-08-23 SMask cache identity update:** soft-mask `/G` cache keys now include structural clip-state fingerprints and explicit CMM/overprint policies, preventing same-bounds different-shape clips or policy changes from reusing cached masks. Evidence: `smask_group_cache_key_includes_contract_output_identity`.

**Superseded checkpoint verdict:** `IMPLEMENTATION_INCOMPLETE`

## 2026-09-07 final source-closure reconciliation

Subsequent source slices removed supported retained-to-immediate delegation,
completed the packed/clip/cache/invalidation/binding architecture, activated
the guarded codec-native reduction and source-window paths available through
the selected adapters, and converted unsupported exact operations to typed
refusal. The remaining compatibility categories are explicit opt-in policies
or accurately reported third-party capability boundaries. The runtime
fallback-policy matrix contains no silent material-degrading high-quality
fallback.

**Current verdict:** `IMPLEMENTATION_COMPLETE_AWAITING_FINAL_VERIFICATION`
