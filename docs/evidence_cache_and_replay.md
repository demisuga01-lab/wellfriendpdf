# Evidence Cache And Replay

Evidence records are content-addressed and store type, hash, source URI, retrieval time, HTTP metadata, parsed identity, freshness timestamps, and validation status. Import checks schema, hashes, duplicate IDs, traversal, and size/count limits.
